# UE5 已完成里程碑（歸檔）

> 最後歸檔：2026-06-25（第八次，debug-checklist + NPC Disposition 快照修復後超過 8 筆觸發歸檔，保留最新 5 筆於 `實作進度.md`）

| W-A~W-G More World Setting（docs/plan-more-world-setting.md） | `Source/SkillCreatorCore/Public/MaterialType.h`（Stone_Cobble/Dirt_Dry/FallenLeaf）`ItemId.h`（OakLog/FallenLeaf/OakSapling/OakFruit/Weed）`WorldHierarchy.h`（新增）`ICollectible.h`（新增）`AWeedEntity.h/.cpp`（新增）`MaterialRegistry.cpp`（GetDefaultDrops 更新）`MapGenerator3D.cpp`（PlantTreesForChunk）`AVoxelWorldActor.h/.cpp`（GrassRegrowthSystem）`ASkillCreatorCharacter.h/.cpp`（W-D 採集 / W-G TickWeedGrowth） | W-A~W-G 7 項 Build 0 錯誤 0 警告。W-A 材質子類型；W-B 樹木生成；W-C 樹崩塌；W-D ICollectible 採集；W-E 草地再生（180s）；W-F 世界層級分類 enum；W-G AWeedEntity 隨機蔓延。 |
| M-NPC-4 Prompt 組裝＋回應解析 + M-NPC-6 行動系統（plan-npc-brain.md） | `NPCActionTypes.h`（新增）`FPromptBuilder.h/.cpp`（新增）`FNPCResponseParser.h/.cpp`（新增）`ANPCCharacter.h/.cpp`（TriggerDialogue/DispatchBrainAction）`UBTTask_NPCFlee/Follow.h/.cpp`（新增） | FPromptBuilder 組裝 system+user 兩層訊息；FNPCResponseParser strip think-tag + JSON 解析四欄；DispatchBrainAction Attack→Hostile/Flee/Follow/stub；UBTTask_NPCFlee/Follow tile-step。Build 0 錯誤 0 警告。 |
| W-10 角色創建系統（實機 PIE 確認） | — | 使用者 2026-06-25 實機 PIE 測試通過，11 步創角流程確認可用。 |
| D-3 爆炸聚合事件 + V-0~V-4 體素化管線（plan-voxelization.md） | `AVoxelWorldActor.h/.cpp`（OnExplosionComplete/OnVoxelDestructionComplete delegate + TriggerVoxelDestruction）`UDroppedItemManager.h/.cpp`（HandleExplodeComplete/HandleVoxelDestruction）`VoxelAsset.h/.cpp`（FVoxelCell/UVoxelAsset/FVoxelAssetEntry）`VoxParser.h/.cpp`（.vox 150 格式解析器）`FVoxelInjector.h/.cpp`（tile 注入）`AVoxelizableActor.h/.cpp`（EVoxelizationMode 基底）`ADestructibleMeshActor.h/.cpp`（Route B Actor） | D-3 delegate 廣播鏈全通；V-0~V-4 體素化管線完整建立。Build 0 錯誤（RMC 4 個 C4996 為既有第三方警告）。 |

| 功能 | 關鍵檔案 | 摘要 |
|------|---------|------|
| K-23 自訂鍵位開機自動套用（啟動時載入持久化 INI） | `Source/SkillCreatorRuntime/Public/ASkillCreatorPlayerController.h`（新增 `OnPossess` override）`Source/SkillCreatorRuntime/Private/ASkillCreatorPlayerController.cpp`（`OnPossess` 實作） | **根因**：`UInputSettingsWidget::LoadAndApplyBindings()` 原本只在 `NativeOnInitialized()`（widget 開啟時）執行，玩家上次改的鍵位下次開遊戲要等打開設定面板才生效，否則還是預設鍵位。對照 Godot `InputBindings.cs`（遊戲啟動 `_Ready()` 就從 `input_bindings.json` 讀入）行為不一致。**修法**：在 `ASkillCreatorPlayerController::OnPossess()` 中讀取 `Saved/Config/PlayerInputBindings.ini`，迭代 `ASkillCreatorCharacter::GetDefaultMappingContext()` 的全部 `FEnhancedActionKeyMapping`，對 INI 裡有記錄的 Action 執行 `UnmapKey`+`MapKey`，最後 `RemoveMappingContext`+`AddMappingContext` 重整 subsystem 使生效。`OnPossess` 選擇在 possess pawn 後執行，確保 `GetDefaultMappingContext()` 可用（`BeginPlay()` 時 pawn 尚未 possess）。Build 0 錯誤 0 警告。 |
| 預檢腳本/單元測試 3 項改善 | `preflight-check.ps1`（Check 7g + 13b 誤判修正）`ExecutionLoop.cpp`（default UE_LOG）`AbilitySystemTests.cpp`（T11 S-2/T12 S-12）`SkillCreatorCoreTests.cpp`（T05 ResolvePhysicalDmg） | **preflight-check.ps1**：Check 7g 由偵測 `EnsureChunkAt`（Godot API 名）改為 `EnsureChunksAround\|IsChunkGenerated`（UE5 等義 API），解除 1 個 false-positive FAIL。Check 13b 新增對 `FSafetyGuard::MaxComboDepth` 引用模式的識別（SpellRunner 引用常數比硬寫數字更好，腳本之前的 regex `ComboDepth < N` 無法解析此模式），將 WARN 升格為 Pass。整體 PASS 27→30，FAIL 1→0，WARN 6→4。**ExecutionLoop.cpp** default case 補 `UE_LOG(Warning)` 於 `++Ctx.PC` 之前，對應 Godot 已修復的 B-2 同類問題（未知 OpCode 無聲跳過）。**AbilitySystemTests.cpp** T11：S-2 回歸守衛——編譯 `DetectHpThreshold(30.0)` 後驗證 `WaitCondition.Threshold==0.3f`（防止未來無意間拿掉 /100 修復）；T12：S-12 回歸守衛——`FSpellSlotSync::TotemToContainer` 對 nullptr/Projectile/summon_turret/summon_guardian/summon_basic 五種輸入分別驗證映射正確性。**SkillCreatorCoreTests.cpp** T05：`FCharacterStats::ResolvePhysicalDmg` 以 `Atk=nullptr`（確定性路徑，跳過隨機分支）驗證兩步防禦公式：100-30=70→70-10=60；atk<def→0；def=0 時只剩 DamageReduction 一步。Build 0 錯誤 0 警告。 |
| 技能/死亡系統 8 項確認 bug 修復（D-1/D-2/D-3/D-4/S-2/S-7/S-8/S-12） | `ASkillCreatorCharacter.cpp`（FillSaveData/ApplyCharacterSaveData）`ASkillCreatorGameMode.h/.cpp`（OnPlayerDied/OnRespawnRequested）`UPlayerHUDWidget.h/.cpp`（OnRespawnButtonClicked/FSimpleDelegate/按鈕取代提示文字）`SpellCompiler.cpp`（DetectHp/MpThreshold÷100）`USpellCaster.cpp`（NearbyR 動態/EntityQuery 排序）`SpellSlotSync.h`（TotemId 細分） | **D-1**：`FillSaveData` 死亡時儲存 `MaxHpBase` 而非 0（對應 Godot Main.cs:244），`ApplyCharacterSaveData` 加 `FMath::Max(1.f,...)` 守衛（Main.cs:355）。**D-2/D-4**：新增 `UFUNCTION OnPlayerDied`（`OnCharacterDied.AddDynamic` 綁定）：`SetPause(true)` + `FInputModeUIOnly` + `ShowMouseCursor(true)` + `ShowDeathScreen(true)`。**D-3**：死亡遮罩舊版只有提示文字「前往遊戲流程選單重生」（無法點擊）→改為真實 `UButton`「重生」+ `UFUNCTION OnRespawnButtonClicked` → `OnRespawnRequested.ExecuteIfBound()`；GameMode `OnRespawnRequested()` 回滿 HP/MP/法力插槽、傳送到 `CurrentWorldSave.PlayerSpawn`、`SetPause(false)` + `FInputModeGameOnly`。**S-2**：`DetectHpThreshold/MpThreshold` 儲存原始百分比（如 30.0），但 `PlayerStatsQuery("hpPct")` 回傳 [0,1]，補 `A.Threshold /= 100.f`（對應 Godot SpellCompiler.cs:224-229）。**S-7**：`static constexpr float NearbyR = 3.f` 移除；改在 `ApplyModsToNearbyEnemies` 內動態計算 `4.f + Mods.DmgBonus * 3.f`（Godot SpellCaster.cs:684）。**S-8**：`EntityQuery` lambda 在 `return Out` 前加距離升序 `Out.Sort`，確保 `QueryNearest` 取 `[0]` 為最近敵人（Godot SpellCaster.cs:401-406）。**S-12**：`TotemToContainer` 新增 `TotemId == "summon_turret"` → `SummonTurret`、`"summon_guardian"` → `SummonGuardian` 分支（Godot AbilityEditorUI.cs:1232-1234）。Build 0 錯誤 0 警告，打包成功。 |
| 全域架構健檢 8 項修復（C-1/C-2/H-1/H-2/H-3/M-1/M-2/M-4） | `CharacterStats.h`（`ResolvePhysicalDmg` 靜態方法）`ASpellProjectile.h/.cpp`（AttackerStats 值拷貝/bUseAttackerStats/OriginEnemy）`AEnemy.cpp`（共用函式/IsAlive守衛/Patrol計時/OnRecoveryEnd清target/Ranged傷害修復）`ANPCCharacter.h/.cpp`（ActionBus/IsAlive守衛）`ASkillCreatorCharacter.cpp`（TakePhysicalDamage共用函式）`ASkillCreatorGameMode.cpp`（移除診斷log）`AVoxelWorldActor.cpp`（移除診斷log）`UBTTask_AttackPlayer.cpp`（Ranged補C-1/C-2修復；Patrol→BeginMeleeAttack） | **C-1**：ASpellProjectile AttackerStats 改值拷貝（避免 Enemy 死亡後 UaF）。**C-2**：S-4 彈反傳 OriginEnemy.Get() 而非投射物本身（Cast<AEnemy>(projectile) 失敗→彈反無效 bug）。**H-1**：物理傷害公式抽 `FCharacterStats::ResolvePhysicalDmg()` 共用靜態方法，消除 3 處 copy-paste。**H-2**：ANPCCharacter::TakeDamageAmount 補 `ActionBus.DispatchPlayerDamage()` + `!IsAlive()` 守衛（對齊 AEnemy）。**H-3**：AEnemy::TakeDamageAmount 加 `!IsAlive()` 守衛（防同幀多次死亡）。**M-1**：Ranged 敵人 `Stats.Power=12`（原=0），`GetAttackDamage()` 回傳 `Stats.Power`，投射物設 `bUseAttackerStats=true`（暴擊管線生效）。**M-2**：Patrol 型加入 S-2 相位（前搖 0.25/攻擊幀 0.15/後搖 0.3s），BTTask 統一走 `BeginMeleeAttack`。**M-4**：移除出生點材質診斷/StartGameplayWithWorld 結束診斷/VoxelWorldActor slot 診斷 Warning log。Build 0 錯誤 0 警告，打包 background 進行中。 |
| Melee 敵人 S-2 攻擊框架 + Z 疾跑 toggle（plan-player-actions.md） | `Source/SkillCreatorRuntime/Public/AEnemy.h`（`#include AttackTypes.h`、`ASkillCreatorCharacter` 前置宣告、`AttackPhase`、`BeginMeleeAttack()`、私有計時器/target）`Source/SkillCreatorRuntime/Private/AEnemy.cpp`（`#include ASkillCreatorCharacter.h`、`BeginMeleeAttack`/`OnWindupEnd`/`OnActiveEnd` 三函式）`Source/SkillCreatorRuntime/Private/UBTTask_AttackPlayer.cpp`（Melee 分支改呼叫 `BeginMeleeAttack`，其餘保持 stub）`Source/SkillCreatorRuntime/Private/ASkillCreatorPlayerController.cpp`（Z 改 toggle：`OnSprintPressed` 偵測 Sprinting/SuperSprinting → `EndSprint`，否則 `StartSprint`；移除 IE_Released 綁定） | **Melee 敵人 S-2 攻擊框架**：`AEnemy::BeginMeleeAttack()` 進入 `WindingUp`(0.4s) → `OnWindupEnd()`：設 `Active`，計算世界距離（`GetAttackRange()*TileSizeCm` cm）確認玩家仍在範圍，呼叫 `Player->TakePhysicalDamage(GetAttackDamage(), &Stats, this)`——走 B-3 完整管線（命中/閃避→物理2步防禦→暴擊），自動支援 S-4 彈反（`bInParryWindow`→凍結攻擊者 3s，普通防禦→50% 傷害），`Attacker=this` 讓彈反能凍結正確的敵人 → 0.2s 攻擊幀後 `OnActiveEnd()`：設 `Recovering` → 0.4s 後回 `None`。`UBTTask_AttackPlayer` Melee 分支改呼叫 `BeginMeleeAttack`（fire-and-forget，BT 攻擊冷卻由 decorator 負責）；Ranged 維持投射物；其餘 stub。舊版 `TakeDirectDamage` 僅剩非 Melee 分支使用。**Z 疾跑 toggle 修正**：舊版需持續按住 Z；新版 Z 為 toggle：第一次按進入疾跑＋計時 1s→升超速，再按一次（無論 Sprinting 或 SuperSprinting）取消回 Grounded，玩家不需一直按住。Build 0 錯誤 0 警告，打包成功（exit code 0）。⚠️ 待實機驗證攻擊相位流程+彈反互動。 |
| S-1 完整狀態機 + S-2 完整攻擊框架（plan-player-actions.md） | `Source/SkillCreatorRuntime/Public/ASkillCreatorCharacter.h`（`EPlayerMovementState` 新增 4 狀態、`MaxJumpCount`、`StartSprint`/`EndSprint`/`StartChargingAttack`/`ReleaseAttack`/`PerformHeavyAttack`/`CancelAttack`/`OnAttackHit`/`PerformCrouch`/`EndCrouch`/`PerformRoll`/`PerformSlide`/`EndSlide`/`StartFastFall`/`Landed` override）`Source/SkillCreatorRuntime/Private/ASkillCreatorCharacter.cpp`（`ApplyMovementState` 全狀態、`StartSprint`+SprintTimer 1s 超速、雙跳 JumpCount、Tick 終端速度限制+蓄力計時、攻擊方法熱鍵欄 AtkMult、NavAgentProps.bCanCrouch、ActionBus 翻滾無敵）`Source/SkillCreatorRuntime/Public/ASkillCreatorPlayerController.h`（`OnSprintPressed`/`OnSprintReleased`/`OnGuardBreakOrFly`/`OnXPressed`/`OnXReleased`/`OnAttackPressed`/`OnAttackReleased`）`Source/SkillCreatorRuntime/Private/ASkillCreatorPlayerController.cpp`（Z/K/X/J IE_Pressed+Released 全鍵位綁定）`Source/SkillCreatorCore/Public/AttackTypes.h`（`EAttackPhase` 新增） | **S-1 完整**：4 個新移動狀態（`SuperSprinting/Crouching/Rolling/Sliding`）；Z 改 hold-based（IE_Pressed→`StartSprint`/IE_Released→`EndSprint`），持續 1s 自動升為超速 4×走速，`SprintTimer` 取消；X 按情境分派（飛行→`FlyDown`/下墜→`StartFastFall`/疾跑→`PerformSlide`/蹲→`EndCrouch`/有移動→`PerformRoll`/靜止→`PerformCrouch`）；翻滾對 ActionBus 設 0.3s DamageShield Cancel（無敵幀）；雙跳：`JumpCount` 計數，`Landed()` 重設，最多 2 跳；Tick 限制下墜速度 ≤ 3×走速（緩降）；`NavAgentProps.bCanCrouch=true`。**S-2 完整**：`StartChargingAttack()`→開始計時/設 `bChargingAttack`；`ReleaseAttack()`：持續 < 0.5s→普通輕攻(20×AtkMult)，≥0.5s→蓄力攻(線性插值20→80×AtkMult)；J+U+持續≥0.3s→`PerformHeavyAttack()`(50×AtkMult)；`CancelAttack()` 清狀態；`OnAttackHit` delegate 供外部（CombatState/技能編輯器）訂閱；`EAttackPhase` 4 階段 enum；AtkMult 查熱鍵欄目前武器（`FItemData::AtkMult * bIsWeapon`，對應舊 `GetTotalAtkMult()` 改動）。Build 0 錯誤 0 警告。⚠️ 待實機驗證。 |
| S-4 防禦/彈反 + S-6/S-7/S-8 接口（plan-player-actions.md） | `Source/SkillCreatorRuntime/Public/ASkillCreatorCharacter.h`（`Guarding` state/`PerformGuard`/`EndGuard`/`IsGuarding`/`PerformBackDash`/`OnParrySuccess` delegate/`bInParryWindow`/`ParryWindowTimer`/`PotionBagComp`/`MapComp`/`AfterimageComp`）`Source/SkillCreatorRuntime/Private/ASkillCreatorCharacter.cpp`（`PerformGuard`/`EndGuard`/`PerformBackDash`/`TakePhysicalDamage` S-4 判定）`Source/SkillCreatorRuntime/Public/UPotionBagComponent.h`（新增）`Source/SkillCreatorRuntime/Public/UMapComponent.h`（新增）`Source/SkillCreatorRuntime/Public/UAfterimageFXComponent.h`（新增）`Source/SkillCreatorRuntime/Public/ASkillCreatorPlayerController.h`（`OnGuardReleased`/`OnBackDash`/`OnOpenPotionPanel`/`OnOpenMap`）`Source/SkillCreatorRuntime/Private/ASkillCreatorPlayerController.cpp`（X IE_Released/L/Y/M 綁定，`OnLightAttack` X+J 偵測，`OnFlyDown` 地面=防禦） | **S-4 防禦/彈反**：`EPlayerMovementState::Guarding`；X（地面 IE_Pressed）→ `PerformGuard()`（進入 Guarding+開啟 6 幀彈反窗口計時器）；X（IE_Released）→ `EndGuard()`（清計時器，回到 Grounded）；J 鍵（X 同時按住地面）→ `PerformGuard()` 而非輕攻；`TakePhysicalDamage()` 加 Attacker 參數，彈反窗口命中→完全無傷+`AuraComp->ApplyFreeze(3s, enemy)`+廣播 `OnParrySuccess`，普通防禦中→50% 傷害。**S-6 接口**（stub）：`UPotionBagComponent`（UseAllBags/TogglePanel）；Q→UseAllBags，Y→TogglePanel。**S-7 接口**（stub）：`UMapComponent`（ToggleMap）；M→ToggleMap。**S-8 接口**（stub）：`UAfterimageFXComponent`（SpawnAfterimage/ApplyInvincibility）；L→`PerformBackDash()`（後撤衝量 4×走速+呼叫 AfterimageComp stub）。Build 0 錯誤 0 警告，打包 552 packages 成功（exit code 0）。⚠️ 待實機驗證。 |
| S-1 狀態機骨架 + S-2 攻擊框架 + S-3 鎖定目標 + 飛行模式（plan-player-actions.md 第三批） | `Source/SkillCreatorCore/Public/AttackTypes.h`（新增）`Source/SkillCreatorRuntime/Public/ASkillCreatorCharacter.h`（`EPlayerMovementState`/`LockedTarget`/`ToggleSprint`/`ToggleFlight`/`FlyDown`/`TryToggleLockTarget`/`SwitchToNextLockTarget`/`PerformLightAttack`）`Source/SkillCreatorRuntime/Private/ASkillCreatorCharacter.cpp`（S-1/飛行/S-3/S-2 邏輯）`Source/SkillCreatorRuntime/Public/ASkillCreatorPlayerController.h`（Z/K/X/J/E/Tab 重整）`Source/SkillCreatorRuntime/Private/ASkillCreatorPlayerController.cpp`（新鍵位綁定）`Source/SkillCreatorRuntime/Private/ASkillCreatorHUD.cpp`（鎖定框 DrawHUD） | **S-1 狀態機骨架**：`EPlayerMovementState`（Grounded/Sprinting/Flying/FastFlying）；Z 改為 toggle 切換（IE_Pressed 單次，Grounded↔Sprinting 或 Flying↔FastFlying），速度 400/800/1000/2000 cm/s；`ApplyMovementState()` 統一套用；修正舊版「按住 Z 2x 速度」的錯誤設計。**飛行模式**：K 鍵 toggle，進入 `MOVE_Flying`+`GravityScale=0`，WASD 改用完整 `GetControlRotation()`（含 Pitch）以便上下飛行；X 鍵退出飛行並 `LaunchCharacter(FVector(0,0,-3x速度))`；Z 在飛行中切換 Flying↔FastFlying（1000→2000 cm/s）。**S-3 鎖定目標**：E 鍵 `TryToggleLockTarget()`（距離+螢幕偏移加權評分，max 25 game units 範圍）；Tab 鍵 `SwitchToNextLockTarget()`；`Tick()` 中 `SetControlRotation` lerp 追蹤目標 Yaw（不影響 Pitch）；自動解鎖條件 `!IsAlive()` 或超出 30 tiles；`ASkillCreatorHUD::DrawHUD()` 在目標頭頂畫紅色 L 型括號（4 個 `DrawRect`）。**S-2 攻擊骨架**：新增 `AttackTypes.h`（`EAttackType` 7 種 / `FAttackHitbox` struct）；J 鍵 `PerformLightAttack()`（1.5 game unit 前向球形 SweepMulti + `TakePhysicalDamage`）。Build 0 錯誤 0 警告，打包 545 packages 成功（exit code 0）。⚠️ 待實機驗證：E 鎖定/Tab 切換/K 飛行/Z toggle sprint/X 退飛行/J 攻擊。 |
| 採掘系統 3 個真根因修復（掉落表/採掘高亮/出生點嵌地） | `Plugins/VoxelWorld/Source/VoxelWorld/Private/MaterialRegistry.cpp`（`GetDefaultDrops` 改回 BlockXxx+修正礦石數量）`AVoxelWorldActor.h/.cpp`（`ShowHighlight` 改吃 `TArray<FGridPos>`）`Source/SkillCreatorRuntime/Public/ASkillCreatorCharacter.h`/`Private/ASkillCreatorCharacter.cpp`（新增 `ComputeHighlightTiles()`）`Source/SkillCreatorRuntime/Private/ASkillCreatorGameMode.cpp`（角色放置加垂直偏移） | 對照 Godot `DroppedItemManager.cs`/`MaterialRegistry.cs` 才發現 UE5 單格採掘掉落表（`GetDefaultDrops`）把 Dirt/Stone/Wood/Sand/Ash 誤填成 `FragmentXxx`，Godot 原版掉 `BlockXxx`（完整方塊）；碎片系統（`SpawnFragments`，材質單位換算）在 Godot 本身就是死碼，從未被呼叫過，不是 UE5 才有的缺陷。順帶修正 Coal/Copper/Iron（1~3→1~2）/MagicCrystal（1~2→1~1）跟 Godot 不一致的掉落數量；Grass（UE5 新增材質，Godot 沒有）原本沒有掉落表項目，補上跟 Dirt 同款。高亮系統：對照 Godot `Main.cs:2935 ComputeHighlightTiles()` 發現 UE5 原本只顯示準心對準的單一格，但預設形狀（Cube 半徑5）實際會摧毀 1331 格，且完全沒做「不可挖/工具不足/超出範圍/滑鼠在熱鍵欄上」的隱藏判定；改成 `ShowHighlight(TArray<FGridPos>)`，中心格沿用單格青框，其餘元素算 bounding box 畫黃色外框概括真正影響範圍，新增 `ComputeHighlightTiles()` 補齊全部過濾條件+完美移除分支。出生點：`SetActorLocation(TileToWorld(PlacementTile))` 混淆了「tile 頂面座標」跟「膠囊體中心」兩種語意（對照 `GreedyMesher.cpp:144` 頂點公式反推 `TileToWorld` 給的是頂面），導致角色腳底嵌入地表下方約 93.75cm；補上 `CapsuleHalfHeight-TileSizeCm` 垂直偏移修正。三項 Build 0 錯誤 0 警告。⚠️ 全部未經實機驗證。 |
| 物品分類擴充 + 加工/合成系統（docs/plan-item-crafting-system.md 全 7 步） | `Source/SkillCreatorCore/Public/ItemId.h`/`ItemData.h`/`MaterialType.h`/`EquipmentSlot.h`（新增）/`RecipeRegistry.h`（新增）`Source/SkillCreatorRuntime/Public/IInteractable.h`（新增）`APlacedFixtureActor.h/.cpp`（新增）`UChestWidget.h/.cpp`（新增）`UCraftingStationSubsystem.h/.cpp`（新增）`UCraftingPanelWidget.h/.cpp`（新增）`UEquipmentSlotWidget.h/.cpp`（新增）等 20+ 檔案 | 7 步全部完成：①武器/工具/裝備/材料/可放置分類擴充 `EItemId`+`EToolCategory`+`EMaterialCategory`（鎬/鏟/斧分類對材質才有採礦加速）；②裝備欄改 `FEquipmentSlotRegistry`（5 槽位資料表）取代硬編 3 槽 enum，`UEquipmentComponent` 改用 `TMap<FName,EItemId>`；③新增 `IInteractable` 介面 + `ASkillCreatorCharacter::UpdateInteractableTrace()`，右鍵優先判定可互動物再判定放置；④泛型 `TSubclassOf<AActor> FItemData::PlaceAsActor` + `FItemRegistry::SetPlaceAsActor()` 後置註冊模式（避免 SkillCreatorCore 反依賴 Runtime），新增 `AChestActor`/`AWorkbenchActor`；⑤寶箱開啟雙欄 `UChestWidget`（`UInventoryWidget` 新增 `PairedWidget`+`OnCrossSwapRequested` 跨欄拖放）；⑥`UCraftingStationSubsystem`（工作臺/寶箱鄰近度註冊）+ `FRecipeRegistry`（8 條合成配方）+ `UCraftingPanelWidget`（玩家身邊常駐面板，含工作臺門檻判定）；⑦左鍵 Tap/Hold 雙軸分派：`IA_Mine` 加 `UInputTriggerHold`(0.2s 門檻)，新增 `IA_PrimaryTap`(`UInputTriggerTap`)，`bIsConsumable` 物品兩種觸發都改成 `UseConsumable()`，否則短按為攻擊 stub（傷害公式待 `plan-player-actions.md` 確定）、長按為原採礦邏輯。Build 0 錯誤 0 警告。⚠️ 全部未經實機驗證（待使用者下次能測試時確認）；攻擊 stub 只佔位不傷血，等攻擊框架文件確定再接。 |
| NPC 分類架構 + 遊蕩詩人（docs/plan-base-npc-system.md 全 8 步） | `Source/SkillCreatorCore/Public/NPCKind.h/.cpp`（新增）`Plugins/NPCBrain/Source/NPCBrain/Public/NPCIdentity.h`（擴充）`UNPCIdentityGeneratorSubsystem.h/.cpp`（改簽名）`Source/SkillCreatorRuntime/Public/FWorldInterfaceAdapter.h/.cpp`（新增）`UMobMeshRegistry.h/.cpp`（新增）`ANPCCharacter.h/.cpp`（新增）`ANPCAIController.h/.cpp`（新增）`UBTTask_WanderRandomly.h/.cpp`（新增）`UBTTask_NPCCounterAttack.h/.cpp`（新增）`ANPCSpawnController.h/.cpp`（新增） | 8 步全部完成：①`FNPCKindRegistry`（7 大類 18 子類純資料表，僅 WanderingBard bImplemented=true）；②`FNPCIdentity` 補 `Appearance/RaceId/SubtypeId`，`LoadOrGenerate()` 新增 `SubtypeId` 參數+生成前用 `FRaceRegistry` 隨機抽 `RaceId` 餵進 LLM prompt（不再讓 LLM 自由發明種族）；③`FWorldInterfaceAdapter` 補上 `plan-npc-brain.md` 點名已久的 M-NPC-3 遺留缺口（`IWorldInterface` 從未被任何地方實作），`GetEntitiesNear`/`GetMaterialAt` 為真實實作（橋接 `AVoxelWorldActor`+`AEnemyManager`），其餘介面方法給最小化但正確的實作（無呼叫端需要的功能誠實回傳預設值，不假裝支援）；④`UMobMeshRegistry`（`UDataTable`+`FMobMeshEntry`，查無資料 fallback 不崩潰，吸取本次 session 修 `TileMaterialRegistry` 的教訓直接用 `LoadSynchronous()`）；⑤`ANPCCharacter`（`APawn`+`ICreature`+`IElementalTarget`+`ISnapshottable`，`TakePhysicalDamage` 公式照抄 `AEnemy` B-3 管線）；⑥`ANPCAIController`+`UBTTask_WanderRandomly`（隨機遊蕩，自包含不重用 `AEnemy` 專屬的 `UBTTask_MoveOnGrid`）；⑦`Disposition` 狀態機（受傷→Hostile+寫入 `UNPCMemoryComponent` 記憶事件，冷卻倒數回 Neutral）+ `UBTTask_NPCCounterAttack`（追擊+近戰反擊）；⑧`ANPCSpawnController`（仿 `AMobSpawnController` 環形生成/超距 despawn，獨立不跟敵人共用）。Build 過程中順手修掉 `NPCBrain`/`SkillCreatorCore` 兩個同名 `FWorldSnapshot` 撞名（改 `FNPCWorldSnapshot`）+ 補 `SkillCreatorRuntime.Build.cs` 缺的 `NPCBrain` 依賴。Build 0 錯誤 0 警告。⚠️ 全部未經實機驗證；身分生成需要 llama-server 啟動才有效果，沒啟動時 NPC 會站著（Stats/Identity 維持預設值）但不會崩潰；`BT_WanderingBard` 行為樹 `.uasset` 本體（Selector/Service 節點連線）仍需在 Editor 手動搭一次，屬 CLAUDE.md「建立 Behavior Tree .uasset 本體」的合法例外，C++ Task/Service 節點已就位。 |
| MainMap World Settings GameMode 覆寫清除（HUD 系列 bug 真正根因） | `Content/MainMap.umap`（World Settings `DefaultGameMode` 清為 None） | 診斷 log 證實 `BP_SkillCreatorHUD_C`（內含 `HUDWidgetClass=WBP_PlayerHUD_C`）才是實際執行的 HUD，繞過所有 C++ 修復；往上查 `BP_SkillCreatorGameMode_C`（World Settings 覆寫指向它）二進位內容含 `ReceiveBeginPlay`/`EventGraph` 字串，是早期未轉 C++ 前殘留的 Blueprint Event Graph。用一次性 Python 腳本（`UnrealEditor-Cmd.exe -ExecutePythonScript`）清除 World Settings 的 `DefaultGameMode` 覆寫，改 fallback 到 `DefaultEngine.ini` 的純 C++ `SkillCreatorGameMode`。準心/物品熱鍵欄/生存條/法術熱鍵欄文字等一系列 HUD bug 的共同根因。 |
| TileMaterialRegistry 真根因修復（全地圖格子狀地面）+ HUD 鏈路診斷 log | `Plugins/VoxelWorld/Source/VoxelWorld/Private/TileMaterialRegistry.cpp`（`GetSurface`/`GetEmissive` 改 `LoadSynchronous()`）`Source/SkillCreatorRuntime/Private/ASkillCreatorHUD.cpp`/`ASkillCreatorPlayerController.cpp`/`UPlayerHUDWidget.cpp`（新增診斷 `UE_LOG`） | `TSoftObjectPtr::Get()` 不會觸發載入，只有「已被別處載入」才回傳非 null；全專案沒有任何地方真正預載過這些材質軟參照（`plan-asset-registry.md` §6 規劃的非同步預載從未實作），Editor/PIE 測試因縮圖快取等因素常駐記憶體造成假象，Standalone 封裝版每次都拿到 nullptr → 全部 fallback 到「材質為 null」的引擎預設外觀（整片地面格子狀）。改用 `LoadSynchronous()` 修復。HUD 準心/熱鍵欄/生存條仍未解之謎（程式碼鏈路讀起來應該正確但畫面外觀疑似舊版 Blueprint），加了診斷 log 待下一輪打包測試後讀 log 確認。 |
| 地表水池重寫（死碼+邏輯錯誤修復）+ 積木卡片 MP 類型改下拉選單 | `Plugins/VoxelWorld/Source/VoxelWorld/Public/SurfaceWaterPool.h`（重寫）`Plugins/VoxelWorld/Source/VoxelWorld/Private/SurfaceWaterPool.cpp`（重寫）`Plugins/VoxelWorld/Source/VoxelWorld/Public/MapGenerator3D.h`/`.cpp`（接入 ComputeChunkData 懶加載查詢）`Source/SkillCreatorRuntime/Private/FlowSaveSystem.cpp`（接上 PostProcessRegion 呼叫）`Source/SkillCreatorRuntime/Private/UBlockCardWidget.cpp`（MP 類型 dropdown） | 水池從建立以來（06-18）從未在世界中出現，根因是 `PostProcessRegion()` 零呼叫處（死碼）+ `FSurfaceWaterPool` 的 `Initialize/Prepare` 簽章設計跟 Godot 完全不同（從未真正生成池子資料，且被設計成「每 chunk 重新隨機」而非「全域一次性佈局」）。對照 Godot `Scripts/World/Terrain/SurfaceWaterPool.cs` 全文＋`MapGenerator3D.cs:104-113/144-160/286-315` 呼叫鏈重寫：`Initialize()` 全域隨機佈局（保底池距出生點≥64 tiles，無邊界世界改用 spawn 為中心固定散布半徑）、`Prepare()` 純算水面 Y、新增 `static QueryOverride()`（對應 Godot `GetSurfaceOverride`+`BowlSurface`，UE5 Y 增大=向下跟 Godot Y-up 相反，公式加減號全部反過來推導驗證）。UE5 沒有 Godot 的初始條帶/懶加載雙路徑，水池查詢改接進 `ComputeChunkData()` 每格生成迴圈（唯一生成路徑），`EnsureChunksAround()` 拷貝佈局資料進背景 thread 閉包確保 thread-safe。`PostProcessRegion()` 移除水池呼叫只留連通性 FloodFill，並在 `FlowSaveSystem::PregenerateSpawnArea()` 補上實際呼叫（這部分也是死碼）。刪除不再使用的 `FTerrainFeature` 抽象基類。MP 類型：Totem 卡片原本用純文字框讓使用者手打 key，改用既有的 `UParamDropdownWidget` + `FManaTypeRegistry::GetSortedForHud()` 列出真正可選項目（對應 Godot `ScratchCanvas.cs:824-841` OptionButton）。⚠️ 純 `.cpp`/.h 純資料類異動（無 UCLASS/UPROPERTY/UENUM），但仍需關閉 Editor 完整 Rebuild 再重新打包驗證；水池佈局是隨機的，需要在世界中走動探索才會遇到。 |
| Shift 游標模式切換 | `Source/SkillCreatorRuntime/Public/ASkillCreatorPlayerController.h` `Source/SkillCreatorRuntime/Private/ASkillCreatorPlayerController.cpp` | 遊戲中按 **LeftShift** 一鍵喚出系統游標，同時用 `SetIgnoreLookInput(true)` 凍結鏡頭旋轉（Look 的 `AddControllerYawInput/PitchInput` 受 `bIgnoreLookInput` 阻斷）；再按一次關閉游標並恢復鏡頭。新增 `bCursorMode` 成員記錄狀態，面板（積木編輯器/技能列表）關閉後 `SetShowMouseCursor(bCursorMode)` 恢復正確狀態（而非 hardcoded false）。⚠️ `.h` 加了非 UPROPERTY 成員，需 Rebuild。 |
| HUD 全面渲染修復 (Bug H-4) + 採掘高亮 + 格子地面根因 | `Source/SkillCreatorRuntime/Private/UPlayerHUDWidget.cpp` `Plugins/VoxelWorld/Source/VoxelWorld/Private/AVoxelWorldActor.cpp` | **Bug H-4 UBorder 渲染**：全專案所有 `SetBrushColor()` 呼叫改為 `SetBrush(MakeSolid({RoundedBox,TintColor=color}))`。根因：`UBorder::SetBrushColor` 設 `BrushColor`；Slate 最終色 = `BrushColor × Background.TintColor`；但 UE5.7 的 `T_DefaultDiffuse_D` ConstructorHelpers 失效，`Background.ResourceObject=null` + `DrawAs=Image` → 什麼都不渲染。`RoundedBox` 是純程式繪製，不需貼圖。影響範圍：準心、生存條 BarBg/Fill、XP 條 Bg/Fill、法力條 BarBg/Fill、死亡遮罩、浮動 Tooltip、物品欄 Icon 色塊。**Bug H-4 Fill resize**：`BuildSurvivalBars` / `BuildLevelHud` 的 Fill 原本加在 BarBg(UBorder)->AddChild()，Slot=UBorderSlot，Cast<UCanvasPanelSlot> 永遠 null → UpdateSurvival/UpdateLevelHUD 的 SetSize 從未執行。修復：Fill 直接加到 Root(UCanvasPanel)，同座標疊在 BarBg 上，Slot=UCanvasPanelSlot ✓。法力條用 USizeBox+UCanvasPanel+BarBg+Fill 結構（HorizontalBox 內無法直接放 Root）。ManaHudVBox Align 改為 `{0,1}`（pivot=底邊），修正 VBox 超出畫面下緣的問題。**採掘高亮**：`ShowHighlight()` 補 `DrawDebugBox(Cyan,0.05s,1.5px)`，確保在任何材質狀態下均可見。**格子地面根因**：`BeginPlay()` 開頭補 `if(!TileMaterialRegistry) LoadObject<>(…)` 保護，防止 Blueprint CDO 將 Registry 覆蓋為 null 後 fallback 到 VoxelMaterial（WorldGridMaterial）。⚠️ 純 `.cpp`，需關閉 Editor + Rebuild。 |
| 物品熱鍵欄外框不可見 (Bug H-3) + 法術熱鍵欄文字強制隱藏 | `Source/SkillCreatorRuntime/Private/UPlayerHUDWidget.cpp` | **Bug H-3**：`BuildItemHotbar()` 對外框 `UBorder` 呼叫 `SetBrushColor(dark)`，之後 `UpdateItemHotbar()` 每幀呼叫 `SetBrush({RoundedBox, TintColor=dark})`，Slate 最終色 = `BrushColor × TintColor ≈ 0.01`（近黑不可見）。修復：移除 `BuildItemHotbar()` 的 `SetBrushColor` 呼叫，`BrushColor` 維持預設 White，最終色 = `White × TintColor = TintColor` ✓。**法術熱鍵欄文字**：`HotBarBox`（`UpdateSpellHotBar` 格式「[1] — ◄ [2] — [3]...」三行文字）強制設 `Collapsed`，施法 U/I/O/P 偵測獨立不受影響。⚠️ 純 `.cpp` 修改，需關閉 Editor + Rebuild。 |
| B-1~B-4 基本數值系統 + 浮空方塊 bug 修復 | `Source/SkillCreatorCore/Public/CharacterStats.h`（B-1：大改）`Source/SkillCreatorRuntime/Public/CharacterSaveData.h`（B-2）`Source/SkillCreatorRuntime/Public/ASkillCreatorCharacter.h`（B-3 API）`Source/SkillCreatorRuntime/Private/ASkillCreatorCharacter.cpp`（B-1g/B-3/B-4）`Plugins/VoxelWorld/Source/VoxelWorld/Private/MapGenerator3D.cpp`（GetHeightAt bug）`Source/SkillCreatorRuntime/Private/UStatsWidget.cpp` | ① GetHeightAt noise 參數對齊 ComputeChunkData（freq/octave/公式全改），修復敵人浮空/生成失敗。② B-1 拆分物理/能量防禦欄位，新增 Strength/EnergyAtk/抗超暴/社會-法則-能量三項 stub/能力點 stub（~30 個新 UPROPERTY）。③ B-2 CharacterSaveData 加年齡欄位。④ B-3 TakePhysicalDamage/TakeEnergyDamage 完整 4/2 步公式+暴擊/閃避。⑤ B-4 HP/MP regen 改為 0.5s FTimerHandle（rate 為 /秒，每跳 ×0.5f）。⚠️ 需關閉 Editor + 完整 Rebuild。B-5 HUD 生存條待後續。 |
| SpellList E 鍵/點擊卡死 3 個 bug 修復 | `Source/SkillCreatorRuntime/Private/ASkillCreatorPlayerController.cpp`（UIOnly→GameAndUI；OnAddSpellRequested BindLambda）`Source/SkillCreatorRuntime/Private/ASkillCreatorHUD.cpp`（DrawHUD 移除每幀 RefreshSpellList） | ① `FInputModeUIOnly()` 封鎖所有 legacy BindKey 事件（含 E 鍵 OnOpenEditor callback）→ 改 `FInputModeGameAndUI()`，兩處（SpellList 開啟、BlockEditor 開啟）都修。② `OnAddSpellRequested` delegate 未綁定 → `+` 圓球點擊空轉；在 OnOpenEditor 首次綁定時 `BindLambda`：掃第一個空 active slot 並呼叫 `OpenBlockEditorForSlot`（對應 Godot Main.cs:1801-1820 OnListAddSpellClicked）。③ DrawHUD 每幀 `RefreshSpellList()`（`ClearChildren`+重建全部圓球 widget）→ 移除，只在開啟面板/切換組別時刷新。Build 0 錯誤 0 警告。 |
| 採掘無反應 + 熱鍵欄佈局/邊框 3 個確定 bug 修復 | `Source/SkillCreatorRuntime/Private/ASkillCreatorCharacter.cpp`（採掘+高亮 raycast 距離）`Source/SkillCreatorRuntime/Private/UPlayerHUDWidget.cpp`（熱鍵欄 inner canvas + active 邊框） | ① 採掘完全無反應：Raycast 10 tiles = 62.5cm，射線根本打不到方塊（MiningRangeTiles=192 tiles=12m）；兩處改成 `static_cast<float>(WorldScale::MiningRangeTiles)`（OnMine + Tick highlight）。② 熱鍵欄 icon/數量/鍵位標籤位置全部失效：`UBorder` 單子節點、`AddChild` 後 Slot 是 `UBorderSlot` 不是 `UCanvasPanelSlot`，Cast 失敗 → `SetOffsets` 從未執行；新增中間層 `UCanvasPanel`（HotbarBorder 唯一子節點），Icon/Count/Key 改加到內層 canvas。③ active slot 缺金黃邊框（Godot BorderColor=0.95,0.80,0.20+Width=2）：改用 `SetBrush(FSlateBrush)` 含 `RoundedBox OutlineSettings`。Build 0 錯誤 0 警告。⚠️ 待 PIE 確認。 |
| USpellListWidget 重寫對齊 Godot SpellListUI.cs | `Source/SkillCreatorRuntime/Public/USpellCircleWidget.h`（新增）`Source/SkillCreatorRuntime/Private/USpellCircleWidget.cpp`（新增）`Source/SkillCreatorRuntime/Public/USpellListWidget.h`（全改）`Source/SkillCreatorRuntime/Private/USpellListWidget.cpp`（全改） | 原版完全憑記憶寫，與 Godot SpellListUI.cs 有 6 個主要差異（標題錯/固定 10 格/組別另起一行用符號/缺提示文字/缺底部欄/方形按鈕）。逐行對照 SpellListUI.cs(454 行)重寫：① 標題改「技能創建空間」(22px)；② 新增 `USpellCircleWidget` 260×260 全圓角按鈕（`ESlateBrushDrawType::RoundedBox`+`CornerRadius=130`，對應 Godot `CircleStyle(cs:438-446)`，主動藍框/被動青框/超限灰框/hover 淡色各對應 Godot 色值）；③ 圓球列表動態建立（`RebuildCircles()`）：只渲染有技能的槽位、被動後渲染、列尾加「+」新增圓球（對應 Godot cs:249-297）；④ 組別按鈕「1」~「5」移回標題列同一行（對應 Godot header HBox cs:94-111）；⑤ 提示文字「點擊圓球編輯技能　E 關閉」(13px 灰，對應 Godot cs:115-119)；⑥ 底部欄（對應 Godot cs:166-200）：「主動上限 10　被動上限 20」+暫時訊息 MsgLabel（黃色，`ShowMsg()` 後 3 秒消失）；⑦ Tooltip 從硬字串改為 `CanvasPanel` 子 `UBorder`，`NativeTick()` 追蹤滑鼠偏移 (14,14) 更新 `UCanvasPanelSlot::SetPosition()`（對應 Godot `_Process` cs:341-358）；新增 `OnAddSpellRequested` delegate。TFunction 模式同 `UFlowCardWidget`（dynamic delegate 不支援 AddLambda，UFUNCTION 中繼→TFunction）。Build 0 錯誤 0 警告（2 次修正：`FVector4f`→`FVector4` UE5.7 double 精度；`SetScrollbarVisibility`→`SetScrollBarVisibility`）。⚠️ 待使用者 PIE 確認圓球外觀/Tooltip 跟滑鼠/底部訊息列。 |
| 修復熱鍵欄/準心/敵人不出現 4 個真 bug（HUD class 被舊 Blueprint 覆寫、敵人生成高度查未載入 chunk、敵人完全無視覺） | `ASkillCreatorPlayerController.h/.cpp`（新增 `SpawnDefaultHUD()` override）`AMobSpawnController.cpp`（改用 `GetHeightAt` 純函數+同步排程 chunk）`AEnemy.h/.cpp`（新增 `MeshComp`+`ApplyBodyColor()`）`AEnemyManager.cpp`（Spawn 後補呼叫 `ApplyBodyColor()`） | 使用者回報熱鍵欄/準心/可挖掘提示/採掘/敵人全部消失，逐一查出 4 個獨立根因：①**熱鍵欄+準心完全不建立**——`Content/MainMap.umap` World Settings 的 `DefaultGameMode` 被存成 `BP_SkillCreatorGameMode_C`（覆寫 `GlobalDefaultGameMode`），其 `HUDClass` 又被覆寫成 `BP_SkillCreatorHUD_C`，`HUDWidgetClass` 又指到舊版 `WBP_PlayerHUD_C`（用 PowerShell 讀 uasset 二進位字串確認只有 `HpBar`/`MpBar`/`StaminaBar`/`MoodBar`/`HotBarBox` 幾個綁定元件，是 C++ 純建構 HUD 出現前的舊設計）；`UPlayerHUDWidget::NativeOnInitialized()`（`UPlayerHUDWidget.cpp:118-130`）偵測到 `HpBar` 非 null 就走「Blueprint 已綁定」分支，Pin 完位置直接 `return`，永遠不會呼叫 `BuildCrosshair()`/`BuildItemHotbar()`。World Settings 的 GameMode 覆寫序列化在 `.umap` 二進位裡，沒有安全的純文字/C++方式改回去，改在 `ASkillCreatorPlayerController` 覆寫 `SpawnDefaultHUD()`（`PlayerController.h:2002`），不呼叫 `Super::`，直接無條件 `SpawnActor<AHUD>(ASkillCreatorHUD::StaticClass())`，繞過 GameMode CDO 上不管哪個 Blueprint 子類設的 `HUDClass`。②**敵人從未真正生成**——對照 Godot `MobSpawnController.cs:124` `GetTerrainY?.Invoke(tx,tz)`→`Main.cs:413` `spawner.GetTerrainY=(wx,wz)=>_mapGen.GetHeightAt(wx,wz)`（純雜訊函數，不依賴 chunk 是否載入），UE5 `AMobSpawnController::TryFindSpawnPos` 原本用 `TW->HeightEstimator(TX,TZ)` 掃描「實際已載入的 tile 資料」找地表——但生成點距玩家 32~128 tile 外幾乎必定還沒被 streaming 載入（`GetTile` 對未載入 chunk 一律回傳 Air），導致 `HeightEstimator` 永遠掃到 `Height-1`、後續 `SurfaceY>=H-1` 檢查必定失敗、24 次嘗試全部 `continue`，`TryFindSpawnPos` 永遠回傳 false。改用 `AVoxelWorldActor::GetMapGenerator().GetHeightAt()`（`MapGenerator3D.cpp:31-42`，同樣是純 FastNoiseLite 函數，跟 Godot 對應）算地表高度，並在找到候選點後用 `EnsureChunksAround`+忙等 `ApplyPendingChunks`（比照 `FFlowSaveSystem::PregenerateSpawnArea` 既有的同步排程模式）確保該 chunk 真的有地形資料可供站立判定，而非只是空 chunk。③**敵人即使生成位置正確也完全不可見**——`AEnemy`（純 `APawn` 子類）C++ constructor 從未建立任何網格元件，`Content/BP_Enemy.uasset` 子類也只覆寫了 `AIControllerClass`（PowerShell 讀字串確認 SCS 只有一個 `DefaultSceneRoot`，無 mesh），對應 Godot 視覺其實是 `Main.cs:1716 CreateEnemyMesh()` 外部建立的純色 `BoxMesh`（per-type 顏色，`SyncEnemyMeshes()` 每幀同步位置），`Enemy.cs` 本體完全沒有視覺元件，UE5 卻把 `AEnemy` 做成有自己 Transform 的 `APawn`，沒有對應補上視覺。比照 `ASkillCreatorCharacter`/`AVoxelWorldActor::HighlightMesh` 既有模式，在 `AEnemy` constructor 新增 `UStaticMeshComponent`（引擎 Cube + `GrainCurrent×TileSizeCm` 縮放，相對位置往上半個單位對齊腳底）當 RootComponent，新增 `ApplyBodyColor()`（依 `Type` 設 `UMaterialInstanceDynamic` 顏色，對應 Godot `CreateEnemyMesh` 的 4 色表），`BeginPlay()`+`AEnemyManager::Spawn()`（`Type` 在 `SpawnActor` 後才設定，`BeginPlay` 已跑完預設色，需再呼叫一次）都呼叫一次。雙 target Rebuild 0 錯誤 0 警告。⚠️ **左鍵採掘無反應**仍待使用者實機驗證：`OnMine()`（`ASkillCreatorCharacter.cpp:818-940`）邏輯逐行比對 Godot `PlayerController.cs:42` `MiningRange`/`Main.cs:1197-1287` 後結構正確（範圍/形狀/工具判定/完美移除分流皆對齊），Enhanced Input 綁定（`IA_Mine`→`EKeys::LeftMouseButton`）與其他確認可用的按鍵（WASD/跳躍）走同一套機制，純讀程式碼找不到反向 bug；由於上述 HUD class 被整個替換掉，採掘/準心提示的「沒有視覺回饋」可能讓使用者誤判「沒有反應」，建議先確認本次 HUD 修復後準心/高亮是否正常出現，再重新測試左鍵採掘是否依然無效。 |
| UInventoryWidget hover tooltip | `Source/SkillCreatorRuntime/Private/UInventoryWidget.cpp` | `NativeTick` 新增每幀 `GetSlotUnderMouse()` 偵測：命中非空格時 `ShowFloatTooltip(FItemRegistry::Get(ItemId).DisplayName, MousePos)`，離開時 `HideFloatTooltip()`。對應 Godot Main.cs:2589-2594 `slot.MouseEntered/MouseExited → ShowFloatTooltip(DisplayName)`。純 .cpp 修改（無 .h 變更），Live Coding 安全。物品欄→熱鍵欄拖曳確認已正常（熱鍵欄為物品欄前 10 格，`OnSlotSwapRequested` 已接通 `SwapSlots`）。同步修復 SetOffsetsInViewport API 不存在編譯錯誤（`ASkillCreatorGameMode.cpp`+`ASkillCreatorPlayerController.cpp` 各移除一行）。Build 0 錯誤 0 警告（UE5.7）。 |
| W-10｜角色創建系統 11 步精靈（架構 Phase A~E 全完成，PIE 實測修了 4 個真 bug） | `UPlayerCreateWidget.h/.cpp`（新增）`UStatAllocatorWidget.h/.cpp`（新增）`RaceRegistry.h/.cpp`（新增，SkillCreatorCore；2026-06-22 重排序+補種族）`UFlowCardWidget.h/.cpp`（加 bShowDeleteButton 參數）`CharacterSaveData.h`（加 RaceId+7 項 BasePoint_*）`FlowSaveSystem.h/.cpp`（加 CreateNewCharacter overload）`UGameFlowWidget.h/.cpp`（移除舊 CharCreateScreen，串接新精靈；2026-06-22 修世界生成卡死 bug + 補進度條）`ASkillCreatorCharacter.cpp`（2026-06-22 修滑鼠上下反向） | 對應使用者規格 `origin text setting/about w - 10 playercreate.txt` + 實作計畫 `docs/plan-w10-character-creation.md`。獨立 `UPlayerCreateWidget` 取代原本一頁式「輸入名字+確認」：`TArray<FPlayerCreateStep>` 步驟陣列機制（每步驟只認索引，不認內容，方便日後增減/調整順序），11 步全部實裝——① 名字輸入、② 創建能力點 RandRange(50,100)+設最高 100、③ 種族選擇（`FRaceRegistry` 收錄 16 體系/164 種，讀完 `Import word setting/ExportBlock-game-word/` 全部 16 份體系文件整理而成；神界種族判定為飛升後狀態、不放入創建清單；體系泡泡用 `UWrapBox`+`UFlowCardWidget`(bShowDeleteButton=false) 取代真實散落視覺，種族清單同樣重用 `UFlowCardWidget`）、④/⑤ 外貌/喜好空白流程、⑥ 基礎能力點分配（`UStatAllocatorWidget`，−/數字輸入框/+ 控制項，仿「人生重開模擬器」）、⑦ 負面效果空白流程但保留驗證（創建能力點<0 跳警告彈窗擋下一步）、⑧/⑨/⑩ 靈魂萬象/體質/心靈眾影響空白流程、⑪ 最終資訊卡（4 個可編輯項目，點擊「編輯」用 `ReturnToStepIndex` 機制跳回對應步驟調整，調整完按該步驟「確認」鈕直接跳回資訊卡，不重跑中間步驟）。右上角「快速建立」可一鍵隨機跳過 1~10 直達 11。

**2026-06-22 第二次 PIE 實測再抓到 2 個真 bug，都已修復**：
④ **種族清單排序錯亂、人類人體系漏了非黃階種族**——使用者指出「新增的種族被排在後面」、「類人體系應該有非黃階種族」。逐一重新打開全部 16 份體系總覽來源文件直接核對（不再相信計畫文件的二手摘要——摘要本身就寫錯「人、類人體系（黃階，全部有現成敘述）」，這句話就是錯的），發現：(a) 上一輪新增的 ~40 種族全部用 Edit 加在每個體系清單最後面，沒有照文件原始順序插入正確位置；(b) 人、類人體系文件其實有玄階（瓦爾基利、龍人）跟地階（不滅族、古蘭族、阿修羅）共 5 族，文件裡是完整內文敘述、不是連結，上一輪完全沒讀到。`RaceRegistry.cpp` 整份重寫，所有 `R()` 呼叫改成跟文件同階層內出現順序一致，補上這 5 族；`RacesInSystem()` 走 `TMap` 插入順序疊代（無 remove，順序穩定），UI 端也沒有額外排序，確認改了 `Init()` 呼叫順序就會反映到畫面。
⑤ **滑鼠上下反向**——`ASkillCreatorCharacter.cpp` 的 `IA_Look` 對 `EKeys::MouseY` 的 Enhanced Input 映射只加了 `UInputModifierSwizzleAxis`，沒加 `UInputModifierNegate`。引擎原始碼 `PlayerController.h:1567` doc comment 寫明 `AddPitchInput` 正值＝「look up」，但 `EKeys::MouseY` 原始滑鼠往上移動時數值是負的（螢幕座標 Y 向下為正），沒 negate 就會變成滑鼠往上、鏡頭往下——照同檔案內 `S` 鍵映射已經在用的 Swizzle+Negate 組合模式補上。

**2026-06-22 使用者實機 PIE 測試抓到 2 個真 bug，都已修復**：
① **世界創建永遠卡在「正在生成世界」畫面，進不去遊戲**——根因是 `UGameFlowWidget::StartWorldGeneration()` 用 `FTimerManager::SetTimer()` 輪詢世界生成進度，但 `UWorld::Tick()`（`Engine/Private/LevelTick.cpp:1783-1788`）裡 `GetTimerManager().Tick()` 被 `if (... && !bIsPaused)` 包住——GameFlowWidget 顯示主選單流程時遊戲本來就是暫停的（`SkillCreatorGameMode: ...game paused`），所以這個 Timer 從頭到尾沒被觸發過，世界生成永遠卡在第一句文字（連「X/Y 區塊」進度都沒更新過）。改成用 `UGameFlowWidget::NativeTick()`（UMG widget 自己的 tick，確認沒有任何暫停閘門）驅動 `PollWorldGeneration()`，移除 `WorldGenPollTimer`/`FTimerManager` 依賴。
② **缺進度條**——「請稍候」文字底下原本只有純文字，沒有視覺進度條；新增 `UProgressBar`（`SizeBox` 固定 420×18 寬度，否則在 VerticalBox 裡會被壓成細線），`PollWorldGeneration()` 同步更新百分比。
③ **基礎能力點介面置中放大 + 數字改可輸入**：`BuildStepBaseStats()` 用 `SizeBox`(寬 420)+`Overlay`(置中) 包住整個 Step 6 內容（外層 `ShowStep()` 對所有步驟統一給 `HAlign_Fill`，不能單獨改，所以在自己回傳的 Widget 內層再包一層置中容器）；`UStatAllocatorWidget` 的數字欄位從 `UTextBlock`（純顯示）改成 `UEditableTextBox`，可以直接打字輸入，`OnTextCommitted` 解析後呼叫新增的 `UPlayerCreateWidget::SetStatValue()`，clamp 到 `[0, 目前值+剩餘點數]` 合法範圍；按鈕/字體/欄位尺寸全部加大（22px 字、44×44 按鈕）。

`UPlayerCreateWidget`/`UStatAllocatorWidget`/`UGameFlowWidget` 都用 `NativeOnInitialized()` 建 WidgetTree（沿用本次 session 剛修好的慣例，沒有重蹈 NativeConstruct 覆轍）。三個 `.h` 都改了（新增 UPROPERTY/虛擬函式宣告），確認關閉 Editor 後完整 Rebuild，雙 target 皆 0 錯誤 0 警告。✅ 2026-06-25 使用者實機 PIE 確認通過：11 步流程順利完成。 |
| AR-B｜世界材質「全格子狀」根因修復：補建 DA_TileMaterialRegistry + 16 個純色材質 | `Content/Data/DA_TileMaterialRegistry.uasset`（新增）`Content/VoxelMaterials/M_Tile_*.uasset`×16（新增） | 使用者回報「世界還是有明顯的格子狀」，一開始以為是 voxel 方塊邊角的預期外觀，追下去才發現：`AVoxelWorldActor.cpp:116-121` 找材質時 `TileMaterialRegistry` 是 null（因為 `/Game/Data/DA_TileMaterialRegistry` 這個資料資產從來沒被建立過，PIE log 一直有 `Failed to find /Game/Data/DA_TileMaterialRegistry` 警告，先前沒當作優先問題處理），導致**全部 17 種 EMaterialType 都 fallback 到同一個 `M_Voxel.uasset`**——整個世界不管哪種方塊，材質都一樣。用 Python 檢查確認 `M_Voxel` 是零參數的基礎 `Material`（`unreal.MaterialEditingLibrary.get_*_parameter_names()` 三項全空），且 `GreedyMesher.cpp:151-153` 早就把 MaterialID/Variant 寫進 VertexColor 等材質端讀取，只是從來沒有任何材質資產被建立——這部分屬「建立 Material 等純美術資產」，照 CLAUDE.md 規則需要使用者決定，問過使用者後選擇「先程式化純色頂上」這個選項。寫一次性 Python script（`unreal.MaterialEditingLibrary` + `AssetTools`），讀 `MaterialRegistry.cpp` 裡既有的 `GMatColors[]` 17 色資料，逐一建立 `M_Tile_<Name>` 材質（`Constant3Vector`→`BaseColor`），再建立並填好 `DA_TileMaterialRegistry.Entries[]`（index 0 Air 留空，1-16 各自指向對應材質），跑完後用第二個腳本讀回驗證全部 17 格內容正確。**之後若要換成真貼圖材質，直接告訴 Claude 要把哪個 EMaterialType 換成哪個資產路徑即可，用同一套 Python 腳本改 `Entries[]` 對應格，不需要手動開 Editor**。⚠️ 半透明材質（Water/Lava/Fire/Steam/Leaves/MagicCrystal）目前仍是 Opaque blend mode，純色但不透明，之後若要做真正透明效果需額外處理。一次性腳本跑完後已刪除，不留在專案裡。⚠️ 仍待使用者重新進 PIE 確認方塊顏色是否正確區分。 |
| M-10｜GPU CA Phase 0~3 完成但⚠️ Phase 3 暫時停用（PIE 實測嚴重卡頓） | `Plugins/VoxelWorld/Source/VoxelWorldShaders/`（新模組）`Plugins/VoxelWorld/Shaders/Private/CaSpike.usf`/`CaMargolus.usf`（新增）`VoxelWorld/Public/CaCellPacking.h`/`CaGpuSimulator.h`/`.cpp`/`TileWorld3D.h`/`.cpp`（改簽名/新增邏輯）`AVoxelWorldActor.cpp`（`BeginPlay()` 的 `InitGpu()` 呼叫 2026-06-22 註解掉）`M10MargolusTest.h/.cpp`/`M10GpuSimulatorTest.h/.cpp`（新增）`VoxelWorld.uplugin`/`VoxelWorld.Build.cs`（新增模組相依） | 對應 `docs/plan-m10-gpu-ca.md`，逐行讀過 Godot `CaGpuSimulator.cs`（469 行）後動手。**Phase 0**（RDG 管線驗證）+ **Phase 1**（Margolus shader 邏輯 port）+ **Phase 2**（`FCaGpuSimulator` 正式實作，Upload→Simulate→Download 三次分開呼叫間用 `TRefCountPtr<FRDGPooledBuffer>` 持久化 GPU buffer）+ **Phase 3**（串接 `Tick()`）程式碼全部完成，自動化測試（4×4×4 小 zone）全部 `Result={Success}`，過程撞到 8 個坑全部記錄在計畫文件。

**2026-06-22 PIE 實機測試發現嚴重卡頓，已停用 Phase 3**：根因是 `TileWorld3D::TickGpuZone()` 每個 tick 都對整個 GPU zone（`ZoneW×ZoneH×ZoneD` = 128×256×128 = **419 萬格**）做一次完整 `GetCell()` 掃描＋打包＋GPU Upload→Simulate→Download（含同步 `FlushRenderingCommands()` GPU stall）——這個熱路徑從 Phase 0~3 開始就只用 4×4×4（64 格）小 zone 測過正確性，從未在真實尺度量過效能，**這正是原計畫把「效能驗證」獨立列成 Phase 4 的原因（先求對再求快），但這次在 `BeginPlay()` 呼叫 `InitGpu()` 讓它在 Phase 4 完成前就提前上線了**。暫時把 `AVoxelWorldActor::BeginPlay()` 的 `TileWorld.InitGpu()` 呼叫註解掉，讓 `GpuSim.IsAvailable()` 維持 false，`Tick()` 整段跳過 GPU 路徑、照舊全 CPU 模擬（跟 Phase 3 之前一樣安全，純停用不影響任何功能）。Phase 0~3 程式碼全部保留原地，之後排進開發順序時，Phase 4 至少要做：① 不要每 tick 全量重建上傳資料（只在 zone 真的移動或內容真的變了才重建）、② GPU buffer 跨 tick 持續留在 GPU、③ 下載端只讀真正有 dirty bit 的格子。下一步：Phase 4（效能優化，目前無排期）。 |
| 玩家動作鍵位重整 + 基礎動作（plan-player-actions.md 第一批+第二批） | `ASkillCreatorPlayerController.h/.cpp`（鍵位重整+F/H/Z新動作）`ASkillCreatorCharacter.h/.cpp`（Tab→Ctrl Tap trigger、Space長按大跳）`USpellCaster.h`（CancelAll） | **第一批**：Z→R（物品欄）、X→T（裝備欄）、E→V（積木編輯器）、C→G（角色面板）、移除 V 的 SpellGroupSwitch（技能組切換改為編輯器面板 UI 操作）。Ctrl（短按≤0.2s Tap trigger）→視角切換，長按不觸發（解決 Ctrl+滾輪縮放衝突）；Tab→OnSwitchLockTarget stub（S-3 待實作）；E→OnToggleLockTarget stub（S-3 待實作）；Q→OnUsePotion stub（S-6 待實作）。**第二批**：F→丟出當前物品（DroppedItemManager::SpawnDrop + Inventory::Consume）；H→取消施法（SpellCaster::CancelAll→Runner::PruneAll）；Z→疾跑（按住 2x WalkSpeedCm，放開恢復，S-5 基礎最小實作）；Space 長按大跳（0.1~0.5s 線性插值至最多 1.5x JumpZVelocity 追加衝量，短按不影響）。Build 0 錯誤 0 警告。⚠️ 待打包後實機驗證：Ctrl 短按切視角、Z 疾跑感、Space 長按大跳效果。 |
| 碎塊/碎片系統 Phase 1（docs/plan-debris-fragment.md D-1~D-5 + bug fix） | `ADebrisActor.h/.cpp`（新增）`UDroppedItemManager.h/.cpp`（SpawnDebris/FDebrisParams/ActiveDebris/TryPickupAll 擴充/Slash-Crush 路由修正）`TileWorld3D.h/.cpp`（OnExplodeComplete + Explode 聚合）`ASkillCreatorCharacter.cpp`（訂閱 OnExplodeComplete）`MaterialRegistry.h/.cpp`（Brittleness 欄位+GetBrittleness）`plan-debris-fragment.md`（Phase 1 計畫，含 Chaos Fracture Phase 2 架構）`RecipeRegistry.cpp`（100碎片→1方塊/礦石 × 9 條配方） | 爆炸→聚合→ADebrisActor（剛體物理飛行）→撿起→FragmentXxx 物品整條路徑打通。1 material unit 修正為 1331 tiles（11³，對應 DefaultShapeRadius=5）。材質加入 Brittleness 脆度係數（水晶=1.5、石=1.2、木=0.7、土=0.5 等）影響碎片公式。Slash/Crush 路由從錯誤的方塊掉落改為 SpawnDebris（單 tile 低於 threshold=13 不產碎塊，符合設計）。Phase 2（Chaos Fracture）和 Phase 3+（Voxelization/MPM/Cloth）架構已在計畫文件中規劃，物品欄層完全相容。Build 0 錯誤 0 警告。⚠️ 待打包後實機驗證爆炸碎塊飛行效果。 |
| 全專案系統性 SetBrushColor 渲染 bug 修復（20 個檔案）+ 地面格子狀調查記錄 | `Source/SkillCreatorRuntime/Public/SlateBrushHelpers.h`（新增共用 helper）`Private/UPlayerHUDWidget.cpp`/`UCraftingPanelWidget.cpp`（改用共用版本）+ 18 個其他 widget 檔案（）`Plugins/VoxelWorld/Source/VoxelWorld/Private/GreedyMesher.cpp`（`IsOpaque()` 排除 Fixture）`docs/開發血汗錄.md`（新增第 2 案） | 使用者實機回報加工面板顯示成鮮綠色方塊，查出是 Bug H-4 的「全專案 SetBrushColor() 已改掉」聲稱不實，grep 找出還有 18+ 個檔案沒修。寫共用 `SlateBrushHelpers.h`（`MakeSolidBrush`）+ 括號深度計數的 Python 腳本批次轉換全部 `SetBrushColor(args)` → `SetBrush(MakeSolidBrush(args))`，過程中發現原本每個檔案各自 `static` 定義同名 helper 會在 Unity Build 合併翻譯單元時撞名，改成共用 inline 版本根除這類問題。順手修正 `EMaterialType::Fixture` 沒被排除在地形渲染之外的小 bug。地面格子狀：深查 7 個環節全部排除但仍未找到根因，完整排查記錄寫進 `docs/開發血汗錄.md` 第 2 案。Build 0 錯誤 0 警告。 |
| 存檔系統根因修復第二輪：換世界全部殘留狀態清理（GeneratedChunks/敵人/掉落物/工作臺寶箱） | `Plugins/VoxelWorld/Source/VoxelWorld/Public/MapGenerator3D.h`/`Private/MapGenerator3D.cpp`（新增 `ResetGenerationState()`）`Public/ChunkStreamingManager.cpp`（`Init()` 呼叫它）`Source/SkillCreatorRuntime/Public/AEnemyManager.h`/`Private/AEnemyManager.cpp`（新增 `ClearAllEnemies()`）`Public/UCraftingStationSubsystem.h`/`Private/UCraftingStationSubsystem.cpp`（新增 `DestroyAllFixtures()`）`Private/ASkillCreatorGameMode.cpp`（換世界分支一併呼叫上述四個清理） | `FMapGenerator3D::InitTerrainParams()` 只重設水池佈局，沒清 `GeneratedChunks`（TSet）——換世界後走到舊世界相同座標會誤判「已生成」永遠是空氣。新增 `ResetGenerationState()` 清空 GeneratedChunks/InFlightChunks/ReadyQueue。`AEnemyManager`/`UDroppedItemManager`/`UCraftingStationSubsystem` 三者同進程持續存活（不隨 MainMap 重載而重建），補上對應清理方法並在 `SpawnWorldAndMobs()` 換世界時呼叫。Build 0 錯誤 0 警告。⚠️ 尚未實機驗證。 |
| 存檔系統真根因：AVoxelWorldActor 換世界沒重新初始化 + 角色座標跨世界誤用 + 熱鍵欄 hover 改原生事件 | `Source/SkillCreatorRuntime/Public/CharacterSaveData.h`（新增 `LastWorldId`）`Private/ASkillCreatorGameMode.cpp`（`LastWorldId` 比對+`ReinitializeForWorld`）`Plugins/VoxelWorld/Source/VoxelWorld/Public/AVoxelWorldActor.h`/`Private/AVoxelWorldActor.cpp`（新增 `ReinitializeForWorld()`+`InitializeWorldState()`）`Public/TileWorld3D.h`/`Private/TileWorld3D.cpp`（新增 `ClearAllChunks()`）`Source/SkillCreatorRuntime/Public/UPlayerHUDWidget.h`/`Private/UPlayerHUDWidget.cpp`（熱鍵欄 hover 改 `UButton` 原生事件） | `AVoxelWorldActor` 是同進程 Spawn 的單一實例，`SpawnWorldAndMobs()` 原本 `if (!VW)` 只在「第一次建立」時設 Seed/SaveDir，玩家在同一次執行內第二次創建新世界時完全沒更新——實際上一直在用第一個世界的 seed+存檔目錄，這就是「Saved/Worlds 永遠都同一個」的根因。新增 `ReinitializeForWorld()`（清空 chunk/渲染網格/放置物件/串流快取並重新初始化）+ `FTileWorld3D::ClearAllChunks()`。角色 `TilePosition` 補 `LastWorldId` 比對（對照 Godot CharacterSaveData.cs 確認 Godot 根本沒有座標欄位，是 UE5 新增設計）。熱鍵欄 hover 改用 10 個透明疊圖 `UButton`+原生 `OnHovered`/`OnUnhovered`（`FInputModeGameOnly` 下不誤觸發）。Build 0 錯誤 0 警告。⚠️ 尚未實機驗證。 |
| 修復 E 鍵開技能編輯器必崩潰（`AddDynamic` 巨集誤用） | `Source/SkillCreatorRuntime/Private/UBlockEditorWidget.cpp` | 使用者按 E 開 `UBlockEditorWidget` 直接崩潰，崩潰訊息：`Assertion failed: Result && Result[2] != '0'`、`'DotHandlers[i]' does not look like a member function`（`Delegate.h:474`）。根因：`AddDynamic` 是巨集，會把第二個參數**原樣字串化**去查 UFunction 名稱（不是執行期接受函式指標變數），程式碼裡用 `void (UBlockEditorWidget::*DotHandlers[N])() = {...}` 陣列、再迴圈 `Dot->OnClicked.AddDynamic(this, DotHandlers[i])`——巨集字串化後變成字面文字 `"DotHandlers[i]"`，引擎驗證函式名稱格式時直接 assert 崩潰。全專案搜尋同一個模式（`AddDynamic(this, \w+\[`）找到 3 處，都在這個檔案：① `BuildHeader()` 的 5 顆技能組切換 dot、② `BuildPalette()` 的 3 個主分頁、③ `RebuildSubTabColumn()` 的 11 個子分頁——全部改成迴圈內 `switch(i)` + 逐個字面 `&UBlockEditorWidget::OnXxxClicked` 呼叫，移除陣列變數轉發。純 .cpp 修改，Build 0 錯誤 0 警告。 |
| 修復 GlobalDefaultGameMode ini 類別路徑錯誤 | `Config/DefaultEngine.ini` `Config/DefaultGame.ini` | 兩個檔案的 `GlobalDefaultGameMode` 均寫成 `/Script/SkillCreatorRuntime.ASkillCreatorGameMode`——UE5 反射系統的類別路徑不含 `A`/`U` 前綴（以引擎自己的 `BaseEngine.ini` `GlobalDefaultGameMode=/Script/Engine.GameModeBase` 及專案內 `BP_SkillCreatorGameMode.uasset` 實際儲存的 parent class 參照 `/Script/SkillCreatorRuntime.SkillCreatorGameMode` 二者驗證），正確應為 `SkillCreatorGameMode`（無 `A`）。此 bug 目前被 `MainMap` World Settings 的 GameMode Override（`/Game/BP_SkillCreatorGameMode`）蓋過而未發作（已用 `UnrealEditor-Cmd.exe -game` 實機跑 3 次驗證，log 確認 `BeginPlay`→`GameFlowWidget` 鏈路目前可正常運作不 crash），但 ini 設定本身是死的錯誤設定，修正避免未來移除/換用其他地圖時失效。同時發現 NPCBrain llama-server 啟動在連續測試中不穩定（2/3 次 `exited unexpectedly`），已開獨立背景任務追蹤，與本次 GameFlowWidget 修復無關。 |
| UGameFlowWidget 完整復刻 Godot GameFlowUI 5 畫面流程 | `UGameFlowWidget.h/.cpp`（重做）`UFlowCardWidget.h/.cpp`（新增）`FlowSaveSystem.h/.cpp` `ASkillCreatorGameMode.cpp` | 對照 `C:\skill-creator\Scripts\GameFlow\GameFlowUI.cs` 全檔重做，取代先前「角色/世界各一畫面、創建內嵌同畫面、清單用文字輸入 ID」的精簡暫代版：① 補回標題畫面（GameFlowUI.cs:155-177 → `BuildTitleScreen`，"SkillCreator" 64px 標題 + 「進入遊戲」鈕）；② 角色/世界各拆回獨立選擇/創建畫面（對應 GameFlowUI.cs:181-202/257-303/307-338/403-481 五個 Panel，`ShowScreen` 切換可見性對應 GameFlowUI.cs:90-97）；③ 清單卡片改為整塊可點擊 + 獨立 🗑 刪除鈕（對應 GameFlowUI.cs:220-253/356-395 `MakeCharCard`/`MakeWorldCard`），新增 `UFlowCardWidget`（`Setup()` 自建 WidgetTree，`TFunction` 捕捉 Id 取代 dynamic delegate 限制，沿用 `UBlockCardWidget` 既有慣例）；④ 刪除確認對話框補回「此操作無法復原。」子文字（GameFlowUI.cs:126-128）；⑤ 世界創建補回 loading 遮罩 + 立即預生成出生區地形（GameFlowUI.cs:431-461 `PregenerateWorld`），新增 `FFlowSaveSystem::PregenerateSpawnArea()` 共用函式（standalone `FTileWorld3D`/`FMapGenerator3D`，此時還沒有 `AVoxelWorldActor`），`ASkillCreatorGameMode::StartGameplayWithWorld` 原本自己重複一份同樣邏輯，現改呼叫同一函式（防呆保留，正常流程已不會觸發）。各畫面背景色/標題字級/按鈕色完全對齊 Godot 原始數值；版面改用 UMG 自動排版容器（VerticalBox/HorizontalBox/ScrollBox）取代 Godot 800×600 絕對座標，避免重蹈 commit ce56c44 的「子項固定尺寸 + 外層 Fill」0×0 collapse bug。Build 0 錯誤 0 警告（UE5.7）。**2026-06-21 追蹤記錄：標題畫面曾完全不可見，已找到根因並修復**——使用者實機測試發現 `BeginPlay` log 正常顯示「showing GameFlowWidget」、`AddToViewport` 無錯誤，但畫面直接是遊戲場景，沒有任何標題畫面痕跡；用診斷 ticker 確認過 widget 幾何/Visibility/Opacity/IsInViewport 全部正常（`AbsSize=(900,650)` 滿版），排除 Anchor/Pause/InputMode/Border brush/call site/Blueprintable 等 10+ 項假設後，靠使用者手動操作 Slate Widget Reflector 在實際渲染樹裡抓到 `UGameFlowWidget` 的 `SObjectWidget` 內容是空的 `SSpacer`（不是真正的 Border/畫面內容）。回查引擎原始碼 `UserWidget.cpp:1203`：`UUserWidget::RebuildWidget()` 在 `WidgetTree->RootWidget` 為 null 時回退成空 `SNew(SSpacer)`，而這個檢查發生在 `NativeConstruct()` 被呼叫**之前**（`UWidget::TakeWidget_Private()` 呼叫順序：`RebuildWidget()` → 包裝快取 → 才呼叫 `OnWidgetRebuilt()`→`NativeConstruct()`），所以「在 `NativeConstruct()` 裡才設定 `RootWidget`」對第一次 `AddToViewport()` 永遠太晚，且 `MyWidget` 一旦快取就不會再重建。修法：把 `BuildLayout()` 從 `NativeConstruct()` 改放到 `NativeOnInitialized()`（`CreateWidget()` 當下、早於 `AddToViewport()` 就會跑，已確認 `CreateWidgetInstance(APlayerController&,...)` 會先 `SetPlayerContext()` 才 `Initialize()`，保證 `NativeOnInitialized()` 觸發）。已用 `-game` 實機截圖確認標題畫面正常顯示。**⚠️ 順便發現 6/20 commit `17dc0a0` 曾聲稱修過同一症狀，但用了 UE5.7 根本不存在的 `SetOffsetsInViewport()` API，這個「假修復」讓本 bug 潛伏超過一天沒人發現**——同一個教訓：commit 訊息/build 成功不代表畫面真的對。**⚠️ 系統性風險未解決**：專案內其他用同一套「NativeConstruct() 建 WidgetTree」慣例寫的 widget（Inventory/Equipment/Settings 等）可能中同一個 bug，已開背景任務待逐一稽核。完整排查記錄見記憶檔案 `project_gameflowwidget_invisible_bug.md` / `project_widget_lifecycle_systemic_risk.md`。 |
| HUD 熱鍵欄 hover tooltip | `Source/SkillCreatorRuntime/Private/UPlayerHUDWidget.cpp` | K-22 的 hover 偵測迴圈改為追蹤 `HoveredIdx`（-1 表示未命中）；命中且格子非空時 `ShowFloatTooltip(FItemRegistry::Get(ItemId).DisplayName, MouseAbs)`，離開時 `HideFloatTooltip()`。透過 `PC->GetPawn<ASkillCreatorCharacter>()->InventoryComp->Slots[i]` 取物品資料（無 .h 變更）。對應 Godot Main.cs:831-836 `ShowTooltip(idx) → ShowFloatTooltip(DisplayName)`。純 .cpp 修改，Live Coding 安全。 |
| K-21 F2 座標 overlay 補齊 5 個欄位 | `Source/SkillCreatorRuntime/Private/ASkillCreatorCharacter.cpp` | 新增視角（`ECameraMode` 名稱，對應 Godot `_camera3d.Mode`）、螢幕 px（`GetMousePosition`）、瞄準格（鏡頭前 5 格 `WorldToTile`，對應 `_player.MouseGridPos`）、方向（Yaw→羅盤 N/E/S/W + 角度）、彈簧臂長（取代 Godot `OrthoZoom`）。原「格:」改標「玩家格」以與「瞄準格」區分。純 .cpp 修改，Live Coding 安全。對應 Godot Main.cs:1023-1031。 |
| SpellCompiler `EBlockType::Totem` 編譯 bug 修復 | `Plugins/AbilitySystem/Source/AbilitySystem/Private/SpellCompiler.cpp` | 原 `EBlockType::Totem` case 用 `FInvokeTotemArgs` 呼叫 `ReadArgs()`，但設計時 `Params["args"]` 存的是 `FTotemBlockArgs`，`IS->GetPtr<FInvokeTotemArgs>()` 永遠回 nullptr → `SlotRef` 永遠 IsNone() → 任何技能因子積木編譯後均不產生 VM 指令。修復：改讀 `FTotemBlockArgs` 取 `TotemId`，查 `Tsm.FindRef(A.TotemId)` 得 `SlotRef`（`Tsm` 已由 `USpellCaster` 正確建立），再用 `FInvokeTotemArgs{Invoke.SlotRef}` 發出 `EOpCode::InvokeTotem`。對應 Godot `SpellCompiler.cs`（history/phase3.md）「BlockType.Totem 在 Compile 時查 totemId→slotRef 自動 emit InvokeTotem」。純 .cpp 修改，Build 0 錯誤 0 警告（UE5.7）。此 bug 首見於積木編輯器 UI 重做 Phase 4 條目，由此次確認並修復。 |
| 積木編輯器 UI 重做 Phase 8：接回 PlayerController（核心驗收標準達成） | `ASkillCreatorPlayerController.h/.cpp` `UBlockEditorWidget.h` | 移除 `ToggleBlockEditorOverlay`/`OnBlockEditorSave` 外的 `#if WITH_EDITOR`，改用 `CreateWidget<UBlockEditorWidget>`+`AddToViewport()`。**架構簡化**：舊版 Slate 流程要 `Graph->FromBlockNodes()`（讀檔案進畫面）+ 存檔時 `GetBlockNodes()`+`SetBlocks()`（畫面寫回檔案）兩段轉換；新版 `SetEditingSpell()` 直接指標指向 `Loadout.Slots[ActiveSlot]` 本體編輯，`OnBlockEditorSave` 驗證通過時資料早就是最新的，完全不需要任何轉換/寫回步驟。E 鍵第二次按下改呼叫 `BlockEditorWidget->RequestClose()`（→`TryExitEditor()`），跟「← 返回」按鈕走同一條未儲存確認流程，不開後門繞過確認；新增 `OnBlockEditorClosed()`（`OnCloseRequested` 觸發時才真正隱藏 widget/還原輸入模式）。**範圍外發現**：`Source/SkillCreatorEditor/SkillCreatorEditorModule.cpp` 還有獨立 Editor 選單 Tab 用舊 `SBlockEditorWidget`/`UBlockEdGraph`（純開發工具，非玩家會碰到，維持不動），意味著 Phase 9 不能直接刪舊類別，要先處理這個 Tab。Build 0 錯誤 0 警告（UE5.7）。 |
| 積木編輯器 UI 重做 Phase 7：儲存/驗證/未儲存確認/技能組切換 | `UBlockEditorWidget.h/.cpp` | 直接呼叫既有 `FSpellSlotSync::SyncSlotsFromBlocks`/`ValidateSpell`（5項驗證，零新邏輯），錯誤用 `ShowConfirmDialog` 單按鈕變體顯示。儲存永遠對 `RootSpell` 驗證+儲存（不是巢狀 `CurrentSpell`），巢狀容器效果是直接編輯 `ContainerEffect` 共享指標，不需要額外寫回。**`SwitchGroup` 比 Godot 簡單**：Godot `_spells[]` 本地緩衝需要明確寫回 Loadout，UE5 `CurrentSpell` 本來就直接指向 Loadout 裡的 `FSpellArray`，切組時資料已經是最新的。`bIsDirty` 策略：`RebuildList()` 一律設 true，`SetEditingSpell()`（載入既有資料）跑完後重置回 false。對應 Godot `AbilityEditorUI.cs:231-327,1276-1345`。Build 0 錯誤 0 警告（UE5.7）。 |
| 積木編輯器 UI 重做 Phase 6：容器巢狀導覽 | `UBlockCardWidget.h/.cpp` `UBlockEditorWidget.h/.cpp` | `NavStack` 用 `TArray<TPair<FSpellArray*, FString>>`（進入容器效果是切換到 `ContainerEffect` 巢狀 FSpellArray，不只是換 Blocks）。`UBlockCardWidget` 新增 `NativeOnMouseButtonDoubleClick` 雙擊偵測 + `OnDoubleClicked` callback，遞迴穿過 `BuildBlockList`/`BuildBranchPanel`。新增通用 `ShowConfirmDialog()`（疊在 `UOverlay` 最上層，Phase 7 未儲存確認共用），因 `UButton::OnClicked` 是 dynamic multicast 不支援 AddLambda，回呼存成 `PendingDialogConfirm/Cancel` member + 固定兩個 UFUNCTION。`EnterContainerEffect` 檢查 `EngraveId` 是否在 `FTotemLibrary::ContainerActionIds()`、深度上限用既有 `FSafetyGuard::MaxContainerDepth`（=3）、確認後 push `NavStack` 並切換到 `ContainerEffect.Get()`（懶建立）。`OnBackClicked` 先檢查 `NavStack` 有東西就 Pop，沒有才走關閉流程。對應 Godot `AbilityEditorUI.cs:29-34/363-404/918-928`。Build 0 錯誤 0 警告（UE5.7）。 |
| 修復 GameFlowWidget 世界選擇選單看不到/按不到 | `ASkillCreatorGameMode.cpp` `UGameFlowWidget.cpp` | 世界選單根節點改用 `USizeBox` + 顯式 `WidthOverride`/`HeightOverride`（綁定 `UWidgetLayoutLibrary::GetViewportSize`）取代依賴子項 desired size 鏈；GameMode 補 `SetAnchorsInViewport`/`SetAlignmentInViewport` |
| NPC Brain M-NPC-1~3（身分/記憶/世界感知） | `Plugins/NPCBrain/...`（NPCIdentity、UNPCIdentityGeneratorSubsystem、NPCMemoryTypes、UNPCMemoryComponent、WorldSnapshot、UNPCPerceptionComponent）詳見 [docs/plan-npc-brain.md](../plan-npc-brain.md) | FNPCIdentity JSON 存讀（身分鎖定）+ LLM 生成；UNPCMemoryComponent 短期緩衝(cap=20)+永久事件區+LLM壓縮長期摘要；FNPCPerceptionLogic（純邏輯）+UNPCPerceptionComponent 手動 TickPerception；⚠️ IWorldInterface 尚無人實作，感知系統待接通 |
| Editor 關閉完整 Rebuild，揪出並修復 3 個編譯錯誤 | `SpellCompiler.cpp` `LlamaServerProcess.cpp/.h` `UNPCBrainSubsystem.h` | SpellCompiler Totem 積木死碼刪除；LlamaServerProcess::IsRunning() FProcHandle 改用本地副本；UNPCBrainSubsystem::TickerHandle 型別修正為 FTSTicker::FDelegateHandle |
| 自動化測試補全（Bug Fix 驗證） | `SkillCreatorCoreTests.cpp` `WorldPreflightRuntimeTests.cpp` | SkillCreatorCore T01~T04（ChebyshevDistance/OreGoldRaw/ItemRegistry 一致性）；VoxelWorld T03/T04（GetDefaultDrops/FragmentItem 完整性掃描）|
| Bug 掃描 LOW×13 全修 | `ExecutionLoop.h/.cpp` `GridPos.h` `ItemId.h` `CharacterStats.h` `UCharacterStateComponent.h/.cpp` `ASkillCreatorCharacter.h/.cpp` `AEnemyManager.cpp` `USpellCaster.cpp` `UBTTask_AttackPlayer.cpp` `ASkillCreatorPlayerController.cpp` `MaterialRegistry.cpp` `ItemRegistry.cpp` | L1：ExecutionLoop 重命名常數消除與 FSafetyGuard 歧義（移除未用 MaxStepsPerCast）；L2：ExecuteSummon 改 Verbose log（W-11 刻意延遲）；L3：敵人死亡掉落改依腳下 tile MaterialRegistry 查表；L4：Tick 清理失效 EnemyProjectiles 指針；L5：刪除 BeginPlay 過期 TODO；L6：OnToggleXray 更正注解（M-10+ GPU 工作）；L7：IsAliveChar 委派給 IsAlive 消除重複邏輯；L8：OnBuildComboContext 加 Warning log 讓 InvokeSpell 失敗可見；L9：stub UPROPERTY Category 加 "Stub" 標示；L10：TakeSnapshot/RestoreFromSnapshot 補存還原 FCharacterStats；L11：GridPos 加 ChebyshevDistance，AI 攻擊範圍改用棋盤格距離；L12：Mood 回歸用 MoodRecoveryRate 常數 + 補 Mood>Default 的下降分支；L13：新增 EItemId::OreGoldRaw + ItemRegistry + MaterialRegistry 補完金礦採掘掉落 |
| Bug 掃描 MED 剩餘全修（M5/M6/M10/M14）+ M15 誤報 | `GreedyMesher.h` `AVoxelWorldActor.cpp` `SpellCompiler.cpp` `SBlockEditorWidget.h/.cpp` | M5：GreedyMesher 注解更正（PolyGroup=MaterialID）；M6：SaveAndEvict 每 128 幀執行一次（GFrameCounter & 0x7F）節流 O(N)；M10：Totem block 補齊 Tsm 查表（IdIS→FName→Tsm.Find→SlotRef，原本找到 IdIS 後直接 break）；M14：SwitchEditorGroup 加 SBox container + RebuildGraphEditor()，切換 group 時重建 SGraphEditor（SGraphEditor 不支援 SetGraph()）；M15：誤報確認，SkillCreatorUI.uplugin Type:"Editor" 已保證只在 Editor Build 編譯，UnrealEd 依賴正確 |
| 全專案 Bug 掃描 MED×10 全修 | `UDroppedItemManager.cpp` `AEnemyManager.cpp` `TileWorld3D.cpp/.h` `ChunkStreamingManager.cpp` `SpellRunner.cpp` `ExecutionLoop.cpp` `UNPCBrainSubsystem.cpp` `UBlockEdGraph.h/.cpp` | M3：掉落物 TileToWorld；M4：DynamicActiveCount FMath::Max(0,…)；M7：Explode/UpdateStatic wy-1 加 y>0 保護；M8：CheckElementalCaReactions 改 CaReactionQueue 延遲寫入；M9：DiskAttempted 殭屍條目清除；M11：TaskCounterOnReach 改 ResolveNum(A->Value)；M12：ComboDepth 先快照防 Add() 後 dangling；M13：ToBlockNodes 加 TSet Visited 循環保護；M16：NPCBrain lambda 只設 bServerReady；M17：Step() break 改 State!=Running |
| 全專案 Bug 掃描 HIGH×9 全修 + M2 熱鍵高亮 | `UInventoryWidget.cpp` `UEquipmentWidget.cpp` `AEnemyManager.cpp` `AMobSpawnController.cpp` `TileWorld3D.cpp` `SurfaceWaterPool.cpp` `SpellRunner.cpp` `ExecutionLoop.cpp` `SkillCreatorUI.Build.cs` | H1：敵人生成 tile→world 座標（乘 TileSizeCm）；H2：玩家 cm→tile 座標（WorldScale::WorldToTile）；H3：UInventoryWidget UBorder 單子項限制，加內層 UCanvasPanel + UOverlay 雙重容器；H4：UEquipmentWidget 同，Cast\<UBorderSlot\> 修正；H5：TryMove 垂直邊界保護（Height>0 && ty>=Height 恆判）；H6：SurfaceWaterPool 第二迴圈 Y 軸反向（改為碗底單格 Dirt）；H7：SpellRunner FSafetyGuard::MaxStepsPerCast=300 取代硬碼 512；H8：WaitCondition PlayerStatsQuery null fallback 改 false；H9：UnrealEd 移至 PrivateDependencyModuleNames；M2：RefreshSlot 熱鍵高亮色實際套用 |
| 素材登記表 AR-A+AR-B（TileMaterialRegistry） | `VoxelWorld/Public/TileMaterialRegistry.h/.cpp` `GreedyMesher.cpp` `AVoxelWorldActor.h/.cpp` | AR-A：UTileMaterialRegistry : UDataAsset，Entries[] 直接以 EMaterialType uint8 為 index，O(1) 查表，Validate() 跳過 Air；AR-B：GreedyMesher PolyGroup 改為 MaterialID，AVoxelWorldActor BeginPlay 設定 Count 個 slot，無 Registry 或 slot null 時 fallback 到 VoxelMaterial；⚠️ 使用者需在 Editor 手動建立 DA_TileMaterialRegistry.uasset（路徑 /Game/Data/）並填入材質 |
| M-NPC-0 NPCBrain Plugin 骨架 | `Plugins/NPCBrain/NPCBrain.uplugin` `NPCBrain.Build.cs` `NPCBrainTypes.h` `NPCBrainSettings.h` `LlamaServerProcess.h/.cpp` `LlamaInferenceClient.h/.cpp` `UNPCBrainSubsystem.h/.cpp` `Config/DefaultNPCBrain.ini` | llama-server HTTP 橋接架構：UWorldSubsystem 管理子程序生命週期，FTSTicker 每 0.5s 輪詢 /health，BindWeakLambda 防 GC crash，LlamaInferenceClient 送 OpenAI-compatible POST 並 lambda capture move-only TDelegate；待使用者下載 llama-server.exe + GGUF 模型後 build 驗證 |
| Runtime Crash 全修（Delegate + GameMode） | `ASkillCreatorGameMode.cpp` `UShapeMenuWidget.cpp` `UEquipmentWidget.h/.cpp` `USpellGroupWidget.cpp` | 修正 4 個 Play 即 crash 問題：① AddDynamic macro 字串化 array element 導致 Delegate assert；② SetWidgetToFocus 對不可聚焦 Widget 觸發 cascade；③ UEquipmentWidget.h 缺 #include "ItemId.h" |
| E 鍵積木編輯器 Tab 登錄 | `SkillCreatorEditorModule.cpp` `SkillCreatorEditor.Build.cs` | FSkillCreatorEditorModule：StartupModule 呼叫 RegisterNomadTabSpawner("BlockSpellEditor")，建立 UBlockEdGraph+UBlockEdGraphSchema 並掛入 SBlockEditorWidget；ShutdownModule 反登錄；E 鍵按下即可開啟積木技能編輯器 Tab |
| S-1 CharacterSaveData 補完 | `CharacterSaveData.h` | 新增 Level(int32) / Xp(float) / InventorySlots(TArray<FItemStack>) / ActiveHotbar(int32) / ManaCurrents(TMap<FName,float>)，對應 UInventoryComponent.Slots 型別 |
| UI-5 SpellDescriptionGenerator | `SpellDescriptionGenerator.h/.cpp` | FSpellDescriptionGenerator 純 C++ 工具：GenerateStructured（插槽/刻印/容器效果逐行結構化）/ GenerateProse（自然語言技能描述散文）；11 元素、11 EngraveColor、ContainerType 中文映射 |
| UI-2 InputSettingsWidget + UI-3 SpellListWidget | `UInputSettingsWidget.h/.cpp` `USpellListWidget.h/.cpp` | UInputSettingsWidget：GetCurrentBindings / RemapAction（修改 IMC + 重套 Sub）/ SaveBindings（GConfig→PlayerInputBindings.ini）/ LoadAndApplyBindings / ResetToDefaults；USpellListWidget：RefreshSpellList（從 SpellGroups 讀取）/ SetActiveGroup / OnSpellListRefreshed / OnSlotHovered；FKeyBindingEntry + FSpellSlotDisplayInfo USTRUCT |
| DefaultIMC 公開 + Character.h 補欄位 | `ASkillCreatorCharacter.h/.cpp` | 新增 UPROPERTY() DefaultIMC；SetupPlayerInputComponent 改存至 DefaultIMC；GetDefaultMappingContext() BlueprintPure |
| UE5.7 UInputSettingsWidget 修正 | `UInputSettingsWidget.h/.cpp` | Mappings 已於 UE5.7 設為 protected；改用 GetMappings()（const）/ UnmapKey() / MapKey()；OriginalKeys 快照 NativeConstruct 拍攝；Build 0 error |
| Widget 重設計：移除 Abstract + 程式化 UI 建構 | `UGameFlowWidget.h/.cpp` `UInputSettingsWidget.h/.cpp` `USpellListWidget.h/.cpp` | 三個 Widget 移除 UCLASS(Abstract)；BlueprintImplementableEvent→BlueprintNativeEvent；新增 BuildLayout() 於 NativeConstruct 程式化建立 WidgetTree（VerticalBox/ScrollBox/Button/EditableText）；新增 _Implementation 預設邏輯；CreateWidget\<T\>(PC,T::StaticClass()) 可直接實例化，不需要任何 .uasset |
| WorldScale 單一真相架構 + 座標轉換集中化 + MobTable 預設值 | `WorldScale.h` `ASkillCreatorCharacter.cpp` `AEnemy.cpp` `AMobSpawnController.h/.cpp` | WorldScale.h 完整重寫：TileSizeCm 一改 → CapsuleRadius/CapsuleHalfHeight/CameraArmLength/WalkSpeedCm/JumpZVelocityCm 全自動跟進；新增 TileToWorld/WorldToTile/WorldToTileF/DirToVoxel 座標轉換函數；Character.cpp 建構子/GetPosition/ApplyEnvironmentalDamage/OnMine 全換 WorldScale API；AEnemy::Respawn() 修正 30× 座標錯誤；AMobSpawnController::PopulateDefaultTable() 補 C++ 預設 MobTable（Melee×3/Ranged×2/Patrol×2/Heavy×1）|
| TerrainFeature + SurfaceWaterPool | `TerrainFeature.h` `SurfaceWaterPool.h/.cpp` | FTerrainFeature 抽象基類；FSurfaceWaterPool：Prepare 隨機碗形水池；PlaceInWorld 雕刻凹陷並填 Water |
| R-6 PlacedObjectRegistry/PlacedUnit/PlacementShape/PlacementValidator | `PlacedUnit.h` `PlacementShape.h/.cpp` `PlacementValidator.h/.cpp` `PlacedObjectRegistry.h/.cpp` | FPlacedUnit + EPlacementShape（GetOffsets）+ FPlacementValidator（三重驗證）+ FPlacedObjectRegistry（雙索引 / JSON 序列化）|
| DroppedItem + DroppedItemManager | `ADroppedItemActor.h/.cpp` `UDroppedItemManager.h/.cpp` | ADroppedItemActor（PickupRadius/Lifetime/Tick）+ UDroppedItemManager（UWorldSubsystem）SpawnDrop / TryPickupAll / Clear |
| M-5 SpellProjectile 元素命中 | `USpellCaster.cpp` | OnHitEnemy lambda 補 ApplyImmediate（元素 Aura）+ ApplyElementalImpact（命中 tile CA 反應）；Elem 由 FSpellArray.SpellElement 提供；VM SpellRunner ↔ TileWorld3D/EnemyManager 全 delegate 已在 TryCast/BeginPlay 完成接線 |
| M-5 Contact + ActionBus | `ActionBus.h/.cpp` `SpellArray.h` `ASkillCreatorCharacter.h/.cpp` `USpellCaster.h/.cpp` | FActionBus（DeltaTime 計時，pause-aware）：DamageShield/DeathGuard 攔截 TakeDirectDamage；EContainerType::Contact + FSpellArray.SpellElement；ExecuteContactHit（3D 前向掃描 MeleeRange=3、ApplyImmediate、BaseDamage、SpawnEffect→ApplyElementalImpact）；RegisterFilterFn 接通 ActionBus |
| M-5 CA 元素反應 | `MaterialRegistry.h/.cpp` `TileWorld3D.h/.cpp` | FMaterialData 加 NativeElement 欄位（17 種材質映射）；CheckElementalCaReactions（Water→Sand 0.5%、Lava+Water→Stone+Steam 3%）；ApplyElementalImpact（法術命中 API：沸騰/流沙/燃燒）|
| M-6 RMC 渲染 | `AVoxelWorldActor.h/.cpp` `GreedyMesher.h/.cpp` `VoxelWorld.Build.cs` | RealtimeMeshComponent Mega-Chunk 64³ / Greedy Meshing 6 方向 / PolyGroups |
| M-7 存讀檔 | `CharacterSaveData.h/.cpp` `FlowSaveSystem.h/.cpp` `ChunkStreamingManager.h/.cpp` | JSON 角色存檔 / Chunk binary 序列化 / 按需載入與卸載 |
| M-8 HUD | `UPlayerHUDWidget.h/.cpp` `ASkillCreatorHUD.h/.cpp` | HP/MP/Stamina/Mood 條 / 熱鍵欄 / Scroll 切換施法槽 |
| M-9 積木編輯器 | `UBlockEdGraphNode` `UBlockEdGraph` `UBlockEdGraphSchema` `SBlockEditorWidget` | Slate SGraphEditor / FBlockNode↔UEdGraphNode 雙向序列化 / Window 選單入口 |
| P1 MobSpawnController | `AMobSpawnController.h/.cpp` | Checkerboard ring spawn（Min=32 / Max=128 tile）；加權 MobTable；SoftDespawn 5%/s（Dist=192）+ HardDespawn（Dist=256）；MaxCommonActive=8；BaseInterval=8s |
| P1 Items 系統 | `ItemId.h` `ItemStack.h` `ItemDrop.h` `ItemData.h` `ItemRegistry.cpp` `EquipmentSlotType.h` `UInventoryComponent.h/.cpp` `UEquipmentComponent.h/.cpp` `ASkillCreatorCharacter.h/.cpp` | 28 個 EItemId；FItemRegistry（函數局部靜態 TArray）；UInventoryComponent（30 格，TryAdd 雙次搜尋，GetActiveToolTier）；UEquipmentComponent（Weapon/Armor/Accessory，TryEquip/Unequip）；Character constructor 注入兩組件 |
| P1 CharacterState 生存 | `UCharacterStateComponent.h/.cpp` | BodyTemperature（向 AmbientTemp 漂移，BodyTempAdaptRate=1.5°C/s）；Thirst（中暑×2 消耗，脫水 2 HP/s）；Hunger（飢餓 1 HP/s）；Oxygen（缺氧 10/s 消耗 / 30/s 回復，窒息 15 HP/s）；TickState() 回傳 PendingDamage 繞過防禦管線 |
| P1 地圖生成補全 | `MapGenerator3D.h/.cpp` | EnsureWalkableCavesInChunk（保障 PlayerH 格淨空）；PlaceOreVeinsInChunk（確定性 LCG + BFS blob：Coal/Copper/Iron/MagicCrystal）；AddDecorInChunk（鐘乳石 4% / 水坑 3%）；PostProcessRegion + FloodFill3D（連通性後處理，孤立洞穴打垂直通道）；thread-safe per-chunk |
| P2 GameFlow UI | `UGameFlowWidget.h/.cpp` `UGameSessionSubsystem.h` `FlowSaveSystem.h/.cpp` | UGameFlowWidget：RefreshWorldList / CreateWorld / RemoveWorld / EnterWorld；OnWorldListRefreshed / OnEnterGame 廣播至 Blueprint；FWorldSaveInfo USTRUCT；UGameSessionSubsystem（跨關卡持久 PendingWorld）；FlowSaveSystem 新增 ListAllWorlds / CreateNewWorld / DeleteWorld |
| P2 Snapshot 系統 | `SnapshotTypes.h` `ISnapshottable.h` `USnapshotManager.h/.cpp` `UElementalAuraComponent.h/.cpp` | FAuraEntryData / FAuraEffectData / FAuraSnapshot / FCharStateSnapshot / FEntitySnapshot（PlayerId=-1）/ FWorldSnapshot；ISnapshottable 純 C++ 介面；USnapshotManager（UGameInstanceSubsystem）TakeSnapshot + ApplyLatest stack；AuraComp 加 TakeAuraSnapshot / RestoreAuraSnapshot / CreateEffect（五種效果工廠）|
| P2 SpellGroup/SpellLoadout | `SpellLoadout.h` `SpellGroup.h` `USpellCaster.h/.cpp` `ASkillCreatorHUD.cpp` | FSpellLoadout（MaxSlots=10 / MaxPassiveSlots=20）：Slots/PassiveSpells/ActiveIndex + AddPassive/RemovePassive/ClearAll；FSpellGroup（MaxGroups=5）：Groups/ActiveGroupIndex + GetActiveLoadout/SetActiveGroup；USpellCaster 以 FSpellGroup 取代舊 HotBar/ActiveSlot；SwitchSlot/CycleSlot/TryCastSlot 重導至 GetActiveLoadout().Slots |
| P2 GPU CA stub（M-10）| `CaGpuSimulator.h/.cpp` `TileWorld3D.h/.cpp` | FCaGpuSimulator：Initialize(stub→false) / SetOrigin / Upload / Simulate / Download / Release；TileWorld3D 新增 InitGpu / UpdateGpuOrigin / InGpuZone（IsAvailable=false 時恆 false，CPU 路徑不受影響）/ SetCellFromGpu（M-10 正式實作：RDG Compute Shader + async readback）|
| P3 FormatTraceParams | `ExecutionLoop.h/.cpp` | FormatTraceParams(PC, Instr) → "PC=N | OpCode | PayloadType(key args)"；WaitArgs/JumpArgs/SetVarArgs 特化格式；Step() 在 bTraceMode=true 時逐指令 UE_LOG 輸出 |
| P3 Sky System | `SkyTypes.h` `ASkyController.h/.cpp` | FSkyConfig（TopColor/HorizonColor/CloudCoverage/CloudSpeed/CloudColor/Brightness）；ASkyController：SunLight/Atmosphere/SkyLight 三組件；PushConfig/ApplyConfig/TransitionTo（Tick LerpUsingHSV 插值）；OnSkyConfigChanged BlueprintImplementableEvent |
| P3 Camera System | `SkillCameraTypes.h` `ASkillCreatorCharacter.h/.cpp` | ECameraMode（ThirdPerson/FirstPerson/Isometric/SideScroll2D）；FCameraModeParams（ArmLength/PitchDeg/bUsePawnControlRotation/FixedYawDeg）；TMap CameraPresets；SetCameraMode（重設 SpringArm 臂長/角度，一人稱眼高 128cm）；CycleCameraMode（循環四模式）|
| issues §1 技能施放核心 | `SpellArray.h` `ExecutionContext.h` `SpellRunner.cpp` `USpellCaster.h/.cpp` | FEngraveData 加 EEngraveCategory/EEngraveTrigger/Effect；FExecutionContext 加 InvokeTotemFn；TryCast 建立 SlotLookup lambda；ResolveTotem/DispatchAction/ExecuteArea(fan/around/distant/beam)/ExecuteTechnique/ExecuteProjectileTotem/ExecuteMorph/ExecuteDisplacement(dash/dodge/teleport)/ExecuteSummon/ExecuteDomain/ApplyGlobalEngravings/ApplyModsToNearbyEnemies 全實作 |
| issues §5 GameClock | `UGameClockSubsystem.h/.cpp` `ASkillCreatorCharacter.cpp` | UGameClockSubsystem（UGameInstanceSubsystem）：TicksPerSecond=20 / TicksPerDay=28800（24 現實分鐘）；TotalTicks(int64) / DayCount / GetDayFraction() / GetDayHour()；Advance(delta) 浮點累積器轉 tick；ASkillCreatorCharacter::BeginPlay Reset + Tick Advance |
| issues §3 IWorldInterface | `IWorldInterface.h` | 純 C++ 介面合約（對應 Godot IWorldInterface.cs §16）：GetEntityAt / GetMaterialAt / GetEntitiesNear（查詢）；DestroyTile / ApplyForce(XZ) / SpawnEffect（指令）；OnEntityHit / OnTileDestroyed / OnEntityDied / OnPlayerAction TFunction 事件回呼；ICreature* 為實體抽象；位於 SkillCreatorCore（無 Gameplay 依賴）|
| issues §4 MaterialData/DestroyReason | `WorldTypes.h` `IWorldInterface.h` `MaterialRegistry.h/.cpp` | EDestroyReason(Mining/ShapeMining/Explosion/Slash/Crush)；IWorldInterface::DestroyTile 加 Reason 參數 + OnTileDestroyed 加 Reason；FMaterialData 新增 bIsMineable/Hardness(0-5)/RequiredToolTier/BlastResistance/MagicResistance/Opacity；GMatData 17 種材質全數填入遊戲機制欄位 |
| issues §6 CharacterStats元素/SpellArray驗證 | `CharacterStats.h` `SpellArray.h` | FCharacterStats 新增 ElemAffinity/ElemOutputMult/ElemResistance（TMap<ESkillElementType,float>）+ 6 個 Get/Set inline；FSpellSlot 新增 TotemType(ETotemType) UPROPERTY；FSpellArray 新增 IsPassive() / GetUsedManaTypes(bRecursive) / IsValidManaTypeCount(Limit=3) 驗證方法 |
| issues §7 SafetyGuard/AbilityPointCalc | `SafetyGuard.h` `AbilityPointCalculator.h` | FSafetyGuard：MaxExecutionsPerTick=50/MaxWhileIterations=500/MaxComboDepth=5/MaxStepsPerCast=300/MaxEntityCount=100/MaxContainerDepth=3；TryProc(ProcMask) / ResetProcMask / TryUseSpell(SceneCounts) / ResetSceneCounts / HasMp；FAbilityPointCalculator：GetMpMultiplier(ActivationType) / HyperbolicEffect / LinearEffect / CalculateMpCost / CalculateSlotCostByType / TierApCap(stub) / ExceedsLevelCap |
| issues §2 EngraveColor/TotemData | `SpellArray.h` | EEngraveColor(11色:Action/Black/White/Orange/Blue/Red/Green/Purple/Yellow/Elemental/Law)；EScalingType(Hyperbolic/Linear)；ETotemType(9 類型：Area~Custom)；FTotemData(TotemId/DisplayName/Type/BaseAbilityPointCost/RequiredPlayerLevel)；FEngraveData 新增 Color+Scaling 欄位 |
| issues §8 SpellProjectile浮點方向 | `ASpellProjectile.h/.cpp` `USpellCaster.cpp` | FIntVector TileDir → FVector TileDir（正規化單位向量）；新增 FVector FloatPos 累積子格位置；Init() 接受任意 FVector 方向並 GetSafeNormal()；AdvanceOneTile() 用 FMath::RoundToInt(FloatPos) 決定目標 tile（支援對角線/任意角度）；USpellCaster 兩處 call site 移除 FIntVector 軸向轉換，直接傳 GetActorForwardVector() |
| issues 清尾：環境傷害+setter+常數 | `AEnemyManager.h` `ASkillCreatorCharacter.h/.cpp` `UCharacterStateComponent.h` `USpellCaster.h/.cpp` | AEnemyManager 新增 FireDps=5/LavaDps=15/BoltDamage=8 靜態常數；ASkillCreatorCharacter 新增 CachedVoxelWorld + ApplyEnvironmentalDamage()（站在 Fire/Lava tile 時每幀扣血）；UCharacterStateComponent 新增 SetStamina/SetMentalEnergy/SetMood/SetHealthStatus 直接 setter；USpellCaster 將 MeleeRange=3 提升為 static constexpr 類別常數 |
| W-6 ManaSlot | `ManaSlot.h` `ASkillCreatorCharacter.h/.cpp` `USpellCaster.cpp` | 新建 FManaSlot USTRUCT（ManaTypeKey/Current/Max/RegenRate + Tick/Consume/HasEnough）；ASkillCreatorCharacter 新增 ActiveManaSlots TArray + GetManaSlot(FName Key)；BeginPlay 初始化 slot[0]("gui_dao")；Tick 改由 slot.Tick() 驅動回復，CurrentMp 同步自 slot[0].Current；USpellCaster.TryCast 從技能使用的法力類型插槽扣除，找不到 key 時降格至 slot[0] |
| W-10 等級系統 | `ASkillCreatorCharacter.h/.cpp` `AbilityPointCalculator.h` | Level(int32)/Xp(float) UPROPERTY 加入角色；GainXp() 實作升級迴圈（while Xp>=XpRequired(Level){ Xp-=req; Level++ }）；XpRequired(level)=level×100；GetTierName() 8 段境界（煉氣/築基/化形/金丹/元嬰/出竅/散仙/真仙）；FAbilityPointCalculator::TierApCap stub 改為正式 8 段分級（50/120/200/350/500/700/900/1500） |
| W-6 AbilityPointCost | `SpellArray.h` `AbilityPointCalculator.h` | FSpellSlot 新增 AbilityPointCost(int32=0)（放入技能因子時從 FTotemData.BaseAbilityPointCost 複製）；FAbilityPointCalculator::CalculateTotalCost stub 改為實作：Σ(slot.AbilityPointCost + ΣlocalEngravings.Points) + ΣglobalEngravings.Points |
| W-6 SpellSaveSystem | `SpellSaveSystem.h/.cpp` `CharacterSaveData.h` | 新建 FSpellSaveSystem：SaveGroupToString(FSpellGroup→JSON) / LoadGroupFromString(JSON→FSpellGroup)，以 FJsonObjectConverter 遞迴序列化全部 UPROPERTY 鏈（FSpellGroup→FSpellLoadout→FSpellArray→FSpellSlot→FEngraveData）；ContainerEffect(TSharedPtr 非 UPROPERTY) deferred；FCharacterSaveData 新增 SpellGroupJson UPROPERTY |
| issues 缺失補完（Batch 1） | `SpellArray.h` `MaterialRegistry.h/.cpp` `SpellRunner.h/.cpp` | HasUnboundMpBlocks() / PrimaryElement() / GetColor/GetDisplayName/GetDefaultDrops / ElapsedTime + PruneAfter |
| issues.md Batch 2 全補完 | `SnapshotTypes.h` `WorldSaveData.h/.cpp` `MaterialRegistry.h/.cpp` `FBlockNode.h` `AEnemy.h/.cpp` `AEnemyManager.h` `ASpellProjectile.h` `TileWorld3D.h/.cpp` `AVoxelWorldActor.h/.cpp` `SBlockEditorWidget.h/.cpp` `UFloatingDamageWidget.h/.cpp` `AFloatingDamageActor.h/.cpp` | FCharStatsSnapshot / LastPlayed ISO 8601 / FragmentItem / SplitAt/AppendBlocks MoveTemp / AEnemy 全屬性補完 / HeightEstimator / ShowHighlight/HideHighlight / OnChanged+SwitchEditorGroup / UFloatingDamageWidget+AFloatingDamageActor |
| 預檢 FAIL×9 + ISnapshottable + GameMode 修正 | `UDroppedItemManager.h/.cpp` `AEnemy.cpp` `ASpellProjectile.cpp` `TileWorld3D.h/.cpp` `AMobSpawnController.cpp` `SpellRunner.cpp` `ASkillCreatorCharacter.h/.cpp` `preflight-check.ps1` `DefaultEngine.ini` | FAIL 5：SpawnForReason 全 EDestroyReason；FAIL 7a/7c/7d/7e/7f/7g：地表+重力+Chunk；FAIL 13b：MaxComboDepth 引 SafetyGuard；FAIL 13d：ISnapshottable（TakeSnapshot/RestoreFromSnapshot）；DefaultEngine.ini GlobalDefaultGameMode 改指 C++ 類 |
| Godot UI 全面移植（6 widget + HUD + PlayerController） | `UPlayerHUDWidget.h/.cpp` `USettingsWidget.h/.cpp` `UShapeMenuWidget.h/.cpp` `USpellGroupWidget.h/.cpp` `UStatsWidget.h/.cpp` `UInventoryWidget.h/.cpp` `UEquipmentWidget.h/.cpp` `ASkillCreatorHUD.h/.cpp` `ASkillCreatorPlayerController.h/.cpp` | 從 Godot Main.cs 推斷視覺呈現，全部以 C++ 程式化重建：物品熱鍵欄 10 格、生存條 6 條+體溫、等級/XP/境界 HUD、動態多重法力條（18 色）、準心、死亡遮罩、境界突破通知；5 個可開關面板（B/N/V/Z/X）；ASkillCreatorHUD 統合建立+delegate；PlayerController 新增 B/N/V/Z/X/C/Q/Tab 全鍵位 |
| 積木編輯器 UI 重做 Phase 5：右側統計面板 | `UBlockEditorWidget.h/.cpp` | `CurrentBlocks` 重構為 `CurrentSpell`（`FSpellArray*`）+ 解開的裸指標，因為 AP/MP/摘要要呼叫的 `FAbilityPointCalculator`/`FSpellDescriptionGenerator` 吃整個 `FSpellArray`。`SetEditingSpell()` 取代 `SetEditingBlocks()`，`Spell->Blocks` 為 null（全新技能整構）時自動 `SetBlocks({})`。`RebuildList()` 現在每次都呼叫 `FSpellSlotSync::SyncSlotsFromBlocks()` 再 `RefreshStatsPanel()`，對應 Godot 每次 canvas 變動後同步+刷新。直接呼叫既有 `FAbilityPointCalculator`（AP數字/進度條/MP按類型分解/超上限變紅字）+ `FSpellDescriptionGenerator::GenerateStructured`（技能摘要文字），零新邏輯，對應 Godot `AbilityEditorUI.cs:934-1122`。**已知限制**：Phase 4 大多參數控制項不觸發 RebuildList（避免拖拉 SpinBox 整棵樹重建），所以改參數值不會即時反映 AP 數字，要等下次結構性變動才同步，跟 Godot 逐欄位即時刷新不同，是效能取捨。Build 0 錯誤 0 警告（UE5.7）。 |
| 積木編輯器 UI 重做 Phase 4：參數 UI（全 84 種積木） | `BlockUIDescriptor.h/.cpp`（新）`UParamControlWidgets.h/.cpp`（新）`UBlockCardWidget.cpp` `UBlockEditorWidget.cpp` | 交叉比對 Godot `ScratchCanvas.cs:474-850`（`_descs` 表）+ UE5 `Instruction.h`/`SpellCompiler.cpp`（每種積木的強型別 Args struct + Params key 是 "args" 還是 "cond"）後，建立 `FBlockUIRegistry` 84 種積木全部登記精確色+中文名+參數 UI（取代 Phase 2/3 的英文名+類別色簡化版）。新增 4 個共用控制項包裝 `UParamTextEditWidget`/`UParamSpinWidget`/`UParamDropdownWidget`/`UParamCheckboxWidget`/`UParamTinyLabelWidget`（對應 Godot `SmallEdit`/`SmallSpin`/`SmallDrop`/`CheckBox`/`TinyLbl`；UMG 控制項事件是 dynamic multicast 不支援 AddLambda，每個包裝固定綁一個 UFUNCTION 轉呼叫 `Setup()` 存的 TFunction）。`ConditionUI` 共用建構子（If/RepeatWhile/Evaluate/RisingEdge/FallingEdge/SinglePulse 六種共用）+ `FNumRef` 文字框雙重語意（`IsNumeric()` 判斷數字或變數名）對應 Godot 同名邏輯。Totem/Engraving 參數 UI（自訂名稱/MP類型/投入點數）因顏色名稱取決於積木實例子型別，留在 `UBlockCardWidget` 特殊處理。**過程中發現既有嚴重 bug**（跟此次 UI 重做無關）：`SpellCompiler.cpp` 編譯 `EBlockType::Totem` 用 `FInvokeTotemArgs` 讀 `Params["args"]`，但設計時實際存的是 `FTotemBlockArgs`，型別不符導致目前任何技能因子積木編譯後都不會產生 VM 指令；✅ 已於 2026-06-20 修復（見「SpellCompiler EBlockType::Totem 編譯 bug 修復」條目）。**已知簡化**：`EffectLabel`/`OnEffectStart`/`OnEffectEnd` 無對應 Args struct 暫不接參數UI；`DetectEntityEnter` 借 `Threshold` 當半徑用（Godot 原有獨立 radius+faction）；`InvokeTotem` 用插槽名稱文字輸入取代 Godot 的拖拉式 `TotemDropZone`。Build 0 錯誤 0 警告（UE5.7）。 |
| 積木編輯器 UI 重做 Phase 3：中央積木卡片清單 | `UBlockCardWidget.h/.cpp`（新）`UCardDragHandleWidget.h/.cpp`（新）`UBlockDropZoneWidget.h/.cpp`（新）`UBlockDragDropOp.h` `UBlockEditorWidget.h/.cpp` | 新增 `UBlockCardWidget`（卡片本體+遞迴巢狀分支渲染，對應 Godot `BuildBlockCard` ScratchCanvas.cs:100-194）、`UCardDragHandleWidget`（卡片搬移拖拉手把，8px）、`UBlockDropZoneWidget`（插入點，6px+綠色高亮，真正執行插入/搬移：對應 Godot `DropZone._DropData` ScratchCanvas.cs:955-976）。`UBlockDragDropOp` 擴充 `SourceList`/`SourceIndex`（純 C++ member，非 UPROPERTY）支援搬移既有卡片模式。巢狀分支標籤對應表（If 成立/不成立、RepeatN/While/ForEachNearby 每輪執行、RandomChoice 選項A/B、AlternateTrigger 偶數/奇數次、TaskCounterOnReach/SinglePulse 單分支）逐項對照 Godot 完成。`UBlockEditorWidget` 新增 `CurrentBlocks`（裸指標指向 `FSpellArray.Blocks`，**不建立任何視覺節點中介物件**，卡片直接讀寫 `FBlockNode` 樹本身）+ `RebuildList()`/`SetEditingBlocks()`。**已知簡化**：一般積木顯示名稱沿用 Phase 2 的英文 UENUM 名+類別色（Phase 4 補精確版）。**驗證限制**：尚無 PIE 入口可實際測拖拉/刪除（Phase 8 才接上），僅 Build+程式碼比對驗證。Build 0 錯誤 0 警告（UE5.7）。 |
| 積木編輯器 UI 重做 Phase 2：左側 Palette | `UPaletteItemWidget.h/.cpp`（新）`UBlockDragDropOp.h/.cpp`（新）`UBlockEditorWidget.h/.cpp` | 新增 `UPaletteItemWidget`（拖拉來源項目，`NativeOnMouseButtonDown`→`DetectDrag`→`NativeOnDragDetected`，取代 Godot 自家 4px 位移偵測）+ `UBlockDragDropOp`（drag payload，攜帶「如何建立」三選一資訊而非直接帶 `TUniquePtr<FBlockNode>`——`UDragDropOperation` 是 UObject，UPROPERTY 不支援 TUniquePtr，實際 `FBlockNode` 在 Drop 端才建立）。三主分頁（技能因子/積木/刻印）+ 各自子分頁（8/11/11）+ 內容清單全部對齊 Godot：`TotemTypeColor`/`EngraveClrOf` 精確色表（`AbilityEditorUI.cs:1486-1498`/`1528-1542`）、積木 16 類別分組對照表（`AbilityEditorUI.cs:704-800` BuildBlockContent 逐項抄錄）、鎖等級 🔒LV 顯示。**已知簡化**（會在 Phase 4 補齊）：積木分頁目前用「每類別一色」近似色 + 英文 UENUM 名稱，非逐型別精確色+中文名（Godot `ScratchCanvas.cs` 的 `_descs` 表有 700+ 行，完整移植留給 Phase 4 的 `FBlockUIDescriptor` 集中表）。本階段只做拖拉「來源」，Phase 3 的卡片清單才有 DropZone 可接收。Build 0 錯誤 0 警告（UE5.7）。 |
| 積木編輯器 UI 重做 Phase 1：核心 Widget 骨架 | `UBlockEditorWidget.h/.cpp`（新） | 新增 `UBlockEditorWidget`（runtime UMG）。根節點用 `USizeBox`+顯式 `WidthOverride/HeightOverride=ViewportSize`（沿用 `UGameFlowWidget::BuildLayout` 已驗證過的做法，避開 `CanvasPanel` FullRect anchor 某些情況解析不出可用空間的已知坑）。Header（50px：返回鈕/技能名稱輸入框/麵包屑/5組技能切換dot/狀態文字）+ Body 三欄（左 220px / 中 ScrollBox+VerticalBox / 右 175px）骨架對齊 Godot `AbilityEditorUI.cs:123-226` 精確尺寸，內容留空待 Phase 2/3/5 填入。⚠️ 尚無 PIE 可視入口（`ASkillCreatorPlayerController::OnOpenEditor()` 仍呼叫舊 Slate overlay，Phase 8 才接上），此階段僅 Build 驗證。Build 0 錯誤 0 警告（UE5.7）。 |
| 積木編輯器 UI 重做 Phase 0：TotemLibrary 資料表 | `Plugins/AbilitySystem/Public/Private/TotemLibrary.h/.cpp`（新）`SpellSlotSync.h` `AbilitySystemTests.cpp` `CLAUDE.md` | 派 Explore agent 完整讀過 Godot `AbilityEditorUI.cs`(~1345行)+`ScratchCanvas.cs`(~1070行) 後確認：原版積木編輯器其實是**垂直清單**（可拖拉重排卡片+縮排巢狀分支），不是節點圖+連線，UE5 現用的 `SGraphEditor` 連 UX 模型都選錯了，需整套換 runtime UMG（詳見 `plans/mutable-singing-nova.md` 10-phase 計畫，已與使用者確認用 UMG 原生 `UDragDropOperation` 做完整擬真拖拉）。本次完成 Phase 0（前置，阻塞 Palette）：新增 `FTotemLibrary`（`Plugins/AbilitySystem`，不是 CLAUDE.md 舊版寫的 `SkillCreatorUI`——`FSpellSlotSync` 需要查它，UI plugin 不能反過來被低層 plugin 依賴，已同步修正 CLAUDE.md），`AllTotems()` 29 筆 + `AllEngravings()` 117 筆（Godot `UI/TotemLibrary.cs` 檔頭註解寫「27/90種」是過時註解，已用 grep 核實實際筆數）+ `ContainerActionIds()`/`DefaultActionEngraveId()`，逐行對照 Godot `TotemLibrary.cs:15-267`。`FSpellSlotSync::SyncSlotsFromBlocks` 原本標記的已知缺口（無法查表取得 Color/Category/Trigger/ScalingType/BaseEffect）已接上（對應 Godot `AbilityEditorUI.cs:1174-1190` 的 template 複製邏輯）。新增 `AbilitySystem.Data.TotemLibraryCounts` 自動化測試核對筆數+抽樣欄位。Build 0 錯誤 0 警告（UE5.7）。Phase 1~9（核心 Widget/Palette/卡片清單/參數UI/統計面板/容器導覽/存檔驗證/PlayerController接線/清理舊架構）待續。 |
| ⭐待修速查表第二層 5 項修復（H-4/H-6/K-5/K-12/I-9） | `MaterialType.h` `TileWorld3D.cpp` `MapGenerator3D.cpp` `MaterialRegistry.h/.cpp` `GreedyMesher.cpp` `PlacedUnit.h` `PlacedObjectRegistry.h/.cpp` `AVoxelWorldActor.h` `ASkillCreatorCharacter.h/.cpp` `ASkillCreatorHUD.h/.cpp` `UDebugPaintWidget.h/.cpp`（新）`AEnemy.h` `UBTTask_MoveOnGrid.cpp` | **H-4**：`FTileCell` 補 `Variant`（4→5 bytes，存檔版號 Ver 1→2）；`MapGenerator3D::ComputeChunkData` 用座標決定性 LCG 雜湊指派（background thread 安全，非共用 RNG）；`FMaterialRegistry::GetColor` 補 Variant 參數做 ±0.03 RGB 偏移（Godot MaterialRegistry.cs:83-92）；`GreedyMesher` 把 quad 起點格 Variant 編碼進頂點色 G channel（R=MaterialID 既有慣例）。⚠️ Greedy 合併只能呈現「每個合併後 quad 一個值」，無法逐格獨立色差，且要看到視覺效果需在材質 Graph 讀 VertexColor.G 做色調微調——此為美術資產編輯，需手動。**H-6**：`FMaterialData` 補 `bIsTransparent`；修正 Water/Fire/Steam 的 `Opacity` 數值（200/150/100→140/166/89，對齊 Godot 0.55/0.65/0.35×255）+ Lava 220→255（Godot 未標記 Lava 透明，原值是錯的）。⚠️ UE5 已用「每材質一個 slot」架構，GreedyMesher 本就不會跨材質合併面，不需要像 Godot 那樣輸出兩個獨立 StreamSet；實際半透明渲染仍需在 Water/Fire/Steam 對應的 Material 資產把 Blend Mode 設為 Translucent + Two Sided（美術資產編輯）。**K-5**：`FPlacedUnit` 補 `OriginalTileCount`+`IsIntact()`；`FPlacedObjectRegistry` 補 `NotifyDestroyed()`（對應 Godot PlacedObjectRegistry.cs:51-61，損壞≥50% tiles 整體解體）；`AVoxelWorldActor` 新增 `PlacedRegistry` 成員+`GetPlacedRegistry()`；`OnPlace()` 放置成功後 `Register()` 為一個 Unit，`CanPlace` 補上 Registry 參數避免疊放；`OnMine()` 採掘前查 `FindAtTile`，`bPerfectRemove`（HUD 既有欄位，順手修正預設值 false→true 對齊 Godot Main.cs:125）開啟且命中 Unit 時整個 Unit 一起摧毀不做形狀採掘；`RebindWorldSystems` 的 `OnTileDestroyed` 全域回呼補上 `NotifyDestroyed` 呼叫（對應 Godot Main.cs:404-407 全域事件）。⚠️ 範圍邊界：未接通 `placed-registry.json` 存讀（R-6d 持久化），重開世界後完美移除記憶會清空，非本次任務範圍。**K-12**：新增 `UDebugPaintWidget`（材質按鈕 Sand/Water/Stone/Wood/Fire/Lava + 筆刷大小 1/3/5/9，對應 Godot Main.cs:549-599 UI 結構）；`ASkillCreatorHUD` 加 `ActivePaintMaterial`/`PaintBrushRadius`+`ToggleDebugPaint()`；`OnDebugPaint()`（F1）改為同步開關面板；`OnMine`/`OnPlace` 在 `bDebugPaintEnabled` 時改走塗繪分支（左鍵塗材質／右鍵清除 Air，使用 `FPlacementShape::GetOffsets(Cube, PaintBrushRadius)`）。**設計判斷**：Godot 原版面板按鈕點擊處理本身是 `GD.Print` stub（連 Godot 自己的 3D 版本都沒真正畫地形），UE5 版本經使用者確認後選擇做出真實塗繪效果，而非逐字複刻無效的 stub。**I-9**：`AEnemy` 補 `PatrolDir`；`UBTTask_MoveOnGrid::ExecuteTask` 補上 Godot `Enemy.cs:191-376` 的四種行為分支——Manhattan 距離 + AttackRange/DetectRange 門檻判定、Patrol 出生點 ±12 格 X 軸巡邏、Ranged 距離 <8 時反向後退（`TryStepXZ` 對角→X→Z 優先序，對應 `TryMoveXZ`），其餘情況維持既有 BFS 追擊不變（Melee/Heavy 兩者在 Godot 本身邏輯完全相同，差異已由 per-type stat 數值反映，不需分支）。**設計判斷**：刻意不修改 `BT_Enemy.uasset` 節點圖（Behavior Tree 圖編輯屬 CLAUDE.md 規則的手動例外），改把四種行為差異全部下推到既有 `UBTTask_MoveOnGrid` 這個 C++ task 的函式內部分支，沿用同一顆樹節點即可生效，不需要使用者開 Editor 改 BT 圖。Build 0 錯誤 0 警告（UE5.7）。 |
| ⭐待修速查表第一層 5 項修復 | `AEnemy.h` `UGameFlowWidget.cpp` `ASkillCreatorGameMode.h/.cpp` `MapGenerator3D.h` `ChunkStreamingManager.h` `AVoxelWorldActor.h` `ASkillCreatorCharacter.h/.cpp` | **I-7**：敵人復活延遲 `StartRespawn` 預設 5.f→8.f（Godot Enemy.cs:47 RespawnTime=8f）。**J-16**：`UGameFlowWidget::EnterWorld()` 補 `Meta.LastPlayed=FDateTime::Now()` + `SaveMeta`（Godot GameFlowUI.cs:379）。**J-19**：`ASkillCreatorGameMode::StartGameplayWithWorld()` 偵測 `bIsFirstEnter` → 用 `FMapGenerator3D::ComputeSpawnPoint` 算出生點 + `EnsureChunksAround`/`ApplyPendingChunks` 同步阻塞生成出生區 chunk + `SaveDirtyChunks` 落地 + 標記 `bIsFirstEnter=false`（Godot GameFlowUI.cs:451-502 PregenerateWorld + Main.cs:284-291）；為此在 `FMapGenerator3D`/`FChunkStreamingManager`/`AVoxelWorldActor` 新增 `GetMapGenerator()`/`HasPendingChunks()` 存取點；角色落點改用 `Character.TilePosition`（回流玩家）優先，否則退回世界出生點，取代固定落在關卡 PlayerStart。**順手修復既有缺口**：`SpawnWorldAndMobs` 從未設定 `VW->WorldSaveDir`（永遠用 class default "World_0001"，多世界 chunk streaming 全部讀寫同一資料夾），改用 `World.Id` 設定。**K-7**：`ASkillCreatorCharacter` 新增 `IsTileOccupiedByEntity()`（玩家/敵人佔用格不可放置，Godot Main.cs:1654 OccupiedByEntity），`OnPlace()` 逐格放置迴圈接上此檢查；玩家檢查刻意補上 Z 軸精確比對（Godot 該函式只比 X/Y，2D→3D 遷移殘留，UE5 原生 3D 沿用會誤擋鄰層格子）。**K-20 確認**：稽核誤判為缺失，實際 `ASkillCreatorGameMode.cpp` 在 Round 1（commit 10390bc）已用 `FTimerHandle PeriodicSaveHandle` 30 秒迴圈呼叫 `PerformSave()`，效果等同 Godot Main.cs:984-988 逐幀累加器，不需重複實作。Rebuild 0 錯誤 0 警告（UE_5.7）。⚠️ 待修速查表第二層（H-6/I-9/K-5/K-12/H-4 中範圍）與第三層（K-15/16/17/18/19 積木編輯器 UI）尚未處理，前次 AI 已標記建議獨立 session，詳見 `docs/audit-round3-2026-06-19.md` ⭐待修速查表。 |
| G~K 稽核後 9 項追加修復 | `TileWorld3D.cpp` `MapGenerator3D.cpp` `AEnemyManager.h/.cpp` `MaterialRegistry.cpp` `ItemRegistry.cpp` `ADroppedItemActor.h/.cpp` `UDroppedItemManager.h/.cpp` `docs/audit-round3-2026-06-19.md` | **G-3a** Lava 限速（Frame%3!=0；Godot TileWorld3D.cs:265）。**G-3b** Lava 點火機率 0.005f→0.1f（差 20 倍；Godot TileWorld3D.cs:287）。**G-9+G-10** 地形 noise freq 0.005→0.001、octaves 4→7、SurfaceY 公式改 `H*0.30+Hv*(H*0.08)`、Clamp 改 `(H*0.15,H*0.45)`（Godot MapGenerator3D.cs:70-79/247-249；原公式振幅差 4 倍）。**H-5** MaterialRegistry：Wood RequiredToolTier 1→0（徒手砍樹）；Coal Hardness 3→1.5（石頭一半）；MagicCrystalOre RequiredToolTier 3→2（Godot MaterialRegistry.cs:26-75）。**I-11** AEnemyManager：FireDps 5→25、LavaDps 15→40、BoltDamage 8→12；死亡後發放 `Char->GainXp(E->GetXpReward())`（Godot EnemyManager.cs:486/488/72/61）。**J-3** ItemRegistry：OreCoal/OreCopperRaw/OreIronRaw/OreMagicCrystal 改 MakeBlock（IsPlaceable=true，PlaceAs=礦脈材質；Godot ItemRegistry.cs:41-44）。**J-11** UDroppedItemManager 補 SpawnFragments（Mining=100碎片/unit，Explosion=Rand(20~80)/unit；Godot DroppedItemManager.cs:31-46）。**J-12** ADroppedItemActor::Tick 補重力（FallTimer 0.18f，下方 Air→Z-TileSize；CachedVoxelWorld 由 SpawnDrop 注入；Godot DroppedItem.cs:13-35）。Build 0 錯誤 0 警告（UE5.7）。 |
| 🟡中優先 10 項修復：B-2/3/4/8/11/13/17 + C-5 + F-3/8/10 | `USpellCaster.h/.cpp` `SpellRunner.h/.cpp` `IWorldInterface.h` `UCharacterStateComponent.cpp` `docs/audit-round3-2026-06-19.md` | **B-2**：TryCast MP 扣除改為 CalculateSlotCostByType 原子按類型分配（Godot SpellCaster.cs:251-275）。**B-3**：投射物改用攝影機前向+身體邊緣起點偏移 halfW+1 格（Godot SpellCaster.cs:56-70）。**B-4**：ExecuteContactHit 加 Code 參數，命中/未命中均提交 Runner.Submit(fixedOrigin=HitPos)（Godot SpellCaster.cs:134-154）。**B-8**：act_area_distant 距離公式修正：`16+BaseR*2`，爆炸半徑 `BaseR+2`（Godot SpellCaster.cs:581-583）。**B-11**：ExecuteProjectileTotem OnHitEnemy 加 TW->Explode AoE + ApplyModsToNearbyEnemies（Godot SpellProjectile.cs:114-116）。**B-13**：act_teleport 改 MaxSteps=20 倒數搜尋（Godot SpellCaster.cs:853-857）。**B-17**：ApplyModsToNearbyEnemies push/pull 改逐格碰撞檢測+位移（Godot SpellCaster.cs:695-715；Slow/Freeze/Stun 待 W-5）。**C-5**：SpellRunner FActiveEntry 新增 MpCost、Submit 存 MpCost、PruneAfter 呼叫 OnMpRefund callback 退還 MP、BeginPlay 設定 OnMpRefund 退還至 slot.Current（Godot SpellRunner.cs:111-122）。**F-3**：UCharacterStateComponent Mood 雙向回歸保留 UE5 擴充，加審查注解（Godot 標「簡化規則待完整設計」）。**F-8**：IWorldInterface.h GetEntitiesNear 注解「曼哈頓距離」→「歐幾里德距離」（Godot IWorldInterface.cs:11）。**F-10**：IWorldInterface 補 FEntityPropertyValue 結構體 + GetEntityProperty/SetEntityProperty/CreateEntity 三純虛函數（Godot IWorldInterface.cs:12-19）。Build 0 錯誤 0 警告（UE5.7）。 |
| D-13 確認 + 🟢文件訂正 5 項 | `docs/issues.md` `docs/audit-round3-2026-06-19.md` `實作進度.md` | D-13 確認：`FSpellSlot::AbilityPointCost` 只存 Totem 基礎成本，`CalculateTotalCost` 另行加總 `LocalEngravings`，無雙重計算，不需修改（`AbilityPointCalculator.h:82-98`）。5 項🟢純文件訂正：**B-20** issues.md §2統計改為~93%（列 B-2/3/4/8/11/13/17 待修）；**C-2/C-3** Submit/comboDepth + TriggerCombo 兩列由「stub」改「已實作」（MaxComboDepth=5 對齊 Godot，callback 架構轉換非缺失）；**E-17** 確認前次已移除（skip）；**A-14** §1 VM Core 架構差異補充 Tsm→SlotRef 說明；**A-22** 補充 VecCross 回傳型別升級說明。audit-round3 文件同步：🟢全部標為已完成，B-5/B-14 在🟡列補標「已於🔴修復」，D-13 確認結論寫入🟡段首。 |
| Round 3 全項稽核 A~F + 12 項🔴高優先修復 | `SpellArray.h` `AbilityPointCalculator.h` `ExecutionLoop.h/.cpp` `SpellSlotSync.h` `SpellSaveSystem.cpp` `USpellCaster.cpp` `AEnemy.h/.cpp` `AEnemyManager.cpp` `docs/issues.md` `docs/audit-round3-2026-06-19.md`（新）`docs/audit-round3-results/agent-{A~F}.json`（新） | 使用者要求「先跑 Round 3 稽核 A~F（不要一次跑 11 個 agent 燒光 token），agent 結果先寫 JSON 再填文件，避免被 compact 摘要吃掉」。6 個 agent 平行核實 106 項（VM Core/SpellCaster/SpellRunner+ActionBus+EventBus/AbilitySystem資料型別/Elemental/Character+Combat+Interfaces），每條判定附 Godot 檔案:行號佐證，結果 ✅66/⚠️25/❌15（Elemental 19項全✅，還原度最高）。彙整出 12 項🔴高優先修復並全部完成：①**D-8** `FEngraveData::CalculateEffect()` 補回 Hyperbolic/Linear 雙公式分支 + DisplayName/IsGlobal/BaseCost/RequiredPlayerLevel/Element/IsRestriction/TotalAbilityPointCost 7 欄位；②**D-4** `EContainerType` 補回 SummonMinion/SummonTurret/SummonGuardian 三獨立值（數值對齊 Godot 3/4/5），Area 移尾端；③**D-7** `EEngraveCategory`（Modifier=0/Action=1 對齊 Godot）+ `EEngraveTrigger`（補回 OnExpire）；④**C-10/D-13** `AbilityPointCalculator::CalculateTotalCost` 改用 `TotalAbilityPointCost()` 取代直接加 `E.Points`，避免漏算 BaseCost/限制型負值；⑤**D-11** `SpellSaveSystem::LoadGroupFromString` 新增 `ValidateManaTypeKeys`（對應 Godot SaveSystem.cs:282-287 未知 key 清除警告）；⑥**A-3** `ExecutionLoop.h::MaxWhileLoopIterations` 改直接引用 `FSafetyGuard::MaxWhileIterations`（原本獨立常數1000，現同步為500）；⑦**A-4** `WaitCondition` 無 `PlayerStatsQuery` 代理時 fallback 改回 `bMet=true`，對齊 Godot 同步路徑語意；⑧**C-6** `AEnemy` 新增專屬 `FActionBus` 成員，`TakeDamageAmount` 接通 `DispatchPlayerDamage` 攔截（`AEnemyManager::Tick` 補 `ActionBus.Update`），對應 Godot `Enemy.TakeDamage` 經 `ActionBus.Dispatch(EntityDamageAction)`；⑨**C-7** EventBus 改為 `USpellCaster.cpp` 檔案級全域 `GEventBusSignals`（取代 per-cast 局部 TSet）+ `GFrameCounter` 每幀清空，修復跨技能廣播物理缺口；⑩**B-7** `act_area_fan` 形狀公式重寫（`spread=(d+1)/2`、僅 Stone 擋格、整排擋住才中斷）；⑪**B-9** `act_area_beam` 補三格正交寬度 + 修正長度公式 `18+BaseR`；⑫**B-10** `ExecuteTechnique` 補 `for(rep=0;rep<M.Multi;rep++)` 迴圈 + 8 種技能因子各自公式（sword/punch/shield + 4 種舊版相容 + slash）；⑬**B-12** `ExecuteMorph` 依 `TotemId` 還原 4 支（speed/strengthen/flight/invisible）；⑭**B-15** `ExecuteDomain` 依 `TotemId` 還原 3 支（barrier 環狀護欄/terrain 純爆炸/weather 雨幕線）；⑮**B-5/B-14** `ExecuteSummon` 補視覺占位（原為純空函式）。**設計判斷**：Godot `PlayerController.Facing.Y` 因 2D→3D 遷移後恆為 0（ExecuteArea/Technique/Displacement/Summon/Domain 共用此欄位），逐字複刻會讓「扇形/光束」退化成寬度 0 的垂直退化形狀；修復改用 UE5 既有 FX/FZ（水平前進）+PX/PZ（水平垂直分量）取代，維持水平扇形/光束設計意圖，半徑/距離數值常數本身仍逐行對齊 Godot（Godot 對這幾個函式本身也未做 Grain 縮放）。**順手修復**：`SpellSlotSync.h` 缺少 `#include "AbilityPointCalculator.h"` 的既有隱性 bug（新 include 順序使其曝光編譯錯誤）。Rebuild 0 錯誤 0 警告（UE_5.7 路徑，非先前記錄的 UE_5.4）。⚠️ G~K（VoxelWorld/Rendering/EnemyAI/Items+GameFlow/UI+Input）尚未稽核；🟡中優先（13項）與🟢文件訂正（5項）尚未修復，詳見 `docs/audit-round3-2026-06-19.md` 修復待辦清單。 |
| issues.md Round 2 稽核修正 + bHoldToPlace/PlaceCooldown | `ASkillCreatorHUD.h` `ASkillCreatorCharacter.cpp` `docs/issues.md` | **bHoldToPlace=true**（Godot Main.cs:124 預設 true，UE5 之前誤設 false）。**PlaceCooldown 移至 Tick()**（對應 Godot 每幀倒數，不依賴右鍵按住）。**issues.md 4 項稽核修正**：GridPos.DistanceTo()→已實作（EuclideanDistance() 完整歐幾里德）；EnemyManager.Tick→已實作（部分）（環境傷害+死亡清理+掉落物，非「只清死亡」）；EventBus→架構不同（lambda 有實作但 per-cast，跨技能廣播缺失）；WorldScale 常數細分（Grain→已實作，WorldW/D→完全缺失設計差異，SimRadius→架構替換）。 |
| WorldScale Grain=16 可擴縮架構 + issues.md 訂正 | `WorldScale.h` `ASkillCreatorCharacter.cpp` `docs/issues.md` `SpellSlotSync.h`（新）`SBlockEditorWidget.h/.cpp` `ASkillCreatorCharacter.h` `GridPos.h` `UDroppedItemManager.cpp` `Instruction.h` | **WorldScale 修正**：GrainCurrent=16（Godot WorldScale.cs Grain=16），TileSizeCm=100/Grain=6.25cm，PlayerH=32 tile=200cm，PlayerW=16 tile=100cm。WalkSpeedCm=TileSizeCm×Grain×4≡400cm/s（4 遊戲單位/s，不隨 Grain 改變）。JumpZVelocityCm=TileSizeCm×14 配合 GravityScaleMult=TileSizeCm/30（constructor 設 GravityScale），使 3 tile 跳躍高度在任意 GrainCurrent 下一致。宣告順序修正：GrainCurrent 移至 PlayerW/PlayerH/TileSizeCm 之前（constexpr 前向引用限制）。**SpellSlotSync**：新增 SpellSlotSync.h（SyncSlotsFromBlocks/ValidateSpell/TotemToContainer/HasActivationTypeBlock，對應 Godot AbilityEditorUI.cs:1134-1345）。**SBlockEditorWidget**：技能名稱欄+儲存按鈕+HandleSaveClicked（5 項驗證）+ShowValidationErrors。**Instruction.h**：FTotemBlockArgs+FEngravingBlockArgs USTRUCT（儲存按鈕設計時參數）。**採掘/放置**：OnMine TickMining 漸進採掘（MiningProgress+=SpeedMult×dt×60）+形狀支援；OnPlace 右鍵放置（bHoldToPlace 節流）；OnMineReleased/OnPlaceReleased 重置狀態。**F1/F2/F4**：F1→OnDebugPaint，F2→OnDebugCoord（Tick 座標 overlay），F4→OnDebugSurvival（Tick 生存 overlay）。EuclideanDistance()（GridPos.h）。UDroppedItemManager ShapeMining 早退。**issues.md 訂正**：F1/F2/F4 從「完全缺失」訂正為「已實作/部分」。Rebuild 待執行（含 USTRUCT 新增→需關 Editor 完整 Rebuild）。 |
| TryCastSlot 端到端打通 + F1/F2/F4 開發者工具 + 採掘放置修正 | `FBlockNodeSaveData.h/.cpp`（新）`SpellArray.h` `SpellSaveSystem.cpp` `SBlockEditorWidget.h/.cpp` `ASkillCreatorPlayerController.h/.cpp` `ASkillCreatorCharacter.h/.cpp` `AbilitySystemTests.cpp` `GameFlowSaveTests.cpp` | 接續上次稽核交接（見前次「⚠️ 稽核後續待修」3 項）：①**Blocks JSON 持久化**——新增 `FBlockNodeSaveData`/`FBlockTreeSaveData`（AbilitySystem plugin）：FBlockNode AST 攤平成 index pool（UHT 不支援 USTRUCT 透過 TArray 自我遞迴，"'Struct' recursion via arrays is unsupported"，故分支存 index 而非巢狀值），Params 用 `UScriptStruct::ExportText/ImportText` 序列化成 "StructPath\|文字" 字串；`FSpellArray` 新增 `BlocksJson` UPROPERTY；`SpellSaveSystem.cpp` 改寫 PopulateBlocksJson/RestoreBlocksFromJson 在存讀時於 Blocks↔BlocksJson 互轉；`ASkillCreatorCharacter::FillSaveData/ApplyCharacterSaveData` 接通 `SpellCasterComp->SpellGroups` 序列化。②**「儲存技能整構」按鈕**——`SBlockEditorWidget` 新增技能名稱欄 + 儲存按鈕 + `OnSaveSpell` delegate；`ASkillCreatorPlayerController::ToggleBlockEditorOverlay` 開啟時用 `Graph->FromBlockNodes()` 載入既有 Blocks，`OnBlockEditorSave` 寫入 `SpellGroups.GetActiveLoadout().Slots[i]`（in-memory，由既有 30 秒/死亡/退出自動存檔機制負責落地，不在按鈕點擊當下寫檔）。③**F1/F2/F4 開發者工具**——對齊 Godot TogglePaint/DebugCoord/DebugSurvival，Enhanced Input 綁定 + Tick 內 `AddOnScreenDebugMessage` overlay。④**採掘/放置**——`IA_Mine` 改 `ETriggerEvent::Triggered`（連續觸發）+ 讀取 HUD ActiveShape/PlaceRadius 做形狀採掘；新增 `IA_Place`（右鍵）+ `OnPlace()`：用 `FaceNormal` 找放置中心、`FItemRegistry` 查 `PlaceAs`、`InventoryComp->Consume()` 扣道具。新增 2 個自動化測試（`AbilitySystem.Save.BlockTreeRoundTrip` AST 攤平還原往返、`SkillCreatorRuntime.GameFlow.SpellGroupBlocksRoundTrip` 全 JSON 往返）。Rebuild 0 錯誤 0 警告。⚠️ **重要限制**：`ToggleBlockEditorOverlay`/`OnOpenEditor` 整段包在 `#if WITH_EDITOR`（依賴 GraphEditor module，UnrealEd-only），積木編輯器目前只能在 Editor/PIE 開啟，**封裝出來的 Shipping build 完全無法編輯技能**（E 鍵在封裝版毫無反應）；這是既有架構限制非本次引入，修正需將 SGraphEditor 換成 runtime UMG 方案，列為後續待辦。⚠️ 本機沙箱環境的 Windows Application Control 政策封鎖剛建出的專案 DLL 載入（Win32 錯誤 4551「An Application Control policy has blocked this file」），無法在此環境執行 UnrealEditor-Cmd 自動化測試/PIE 驗證；新增測試已通過程式碼比對 UE5.7 引擎標頭確認 API 正確，但實際執行結果待使用者在自己的 Editor 環境驗證。 |
| 全專案稽核 Round 1 剩餘修補（5 批次完成） | `ASkillCreatorCharacter.cpp` `ASkillCreatorGameMode.h/.cpp` `ASkillCreatorHUD.h/.cpp` `ASkillCreatorPlayerController.h/.cpp` `AEnemy.cpp` `AEnemyManager.cpp` `UBTTask_AttackPlayer.cpp` `UGameFlowWidget.h/.cpp` `docs/issues.md` | 繼 Round 1 首輪 7 項後，本批修復剩餘 6 項中的 5 項：①**存檔接通**—GameMode 新增 30 秒定時存、死亡即存（AddDynamic 綁 OnCharacterDied，需 UFUNCTION）、EndPlay 落地存；Character 補 `FillSaveData()`（Level/Xp/Hp/Mp/TilePosition/Inventory/ManaCurrents，`FManaSlot.ManaTypeKey` 非 `Key`）；②**飄浮傷害**—Character `TakeDirectDamage()` 和 `AEnemy::TakeDamageAmount()` 補呼叫 `AFloatingDamageActor::Spawn()`；③**自動裝備 + 採礦高亮**—`Tick()` 中 `TryAdd()` 回傳 slot index 觸發 `TryEquip()`；鏡頭 Raycast 命中可採掘 tile 時呼叫 `ShowHighlight()`/`HideHighlight()`；④**HUD 面板鍵位**—HUD `BeginPlay()` 建立 `UInputSettingsWidget`/`USpellListWidget`；PlayerController 新增 I/L 鍵呼叫 `ToggleInputSettings()`/`ToggleSpellList()`；順帶補 `#include "UEquipmentComponent.h"` 修復預存 forward-declaration link 錯誤；⑤**敵人環境傷害 + 遠程攻擊**—`AEnemyManager::Tick()` 掃腳下 tile 材質（Fire/Lava）扣 `Dps * DeltaTime`；`UBTTask_AttackPlayer` 區分 Ranged/Melee——Ranged 生成 `ASpellProjectile`（`BaseDamage = BoltDamage`）並登記 `EnemyProjectiles`，Melee 維持 `TakeDirectDamage()`；⑥**刪除確認彈窗**—`UGameFlowWidget` 新增 `ConfirmOverlay`（中心錨定 UBorder+UVerticalBox+Yes/No 按鈕）+ `ShowConfirmDelete()`/`OnConfirmDeleteYes()`/`OnConfirmDeleteNo()`，刪除按鈕改呼叫彈窗；**issues.md** 訂正 5 處誤判為「缺失」的已實作功能。⚠️ 剩 1 項待調查：`USpellCaster::TryCastSlot()` 永遠傳空指令陣列（見下方）。Rebuild 0 錯誤 0 警告。 |
| 全專案稽核 Round 1：7 項嚴重 bug 修復（剩餘大項待續） | `AEnemyAIController.h/.cpp` `AEnemy.cpp` `AEnemyManager.h/.cpp` `ASkillCreatorGameMode.cpp` `ActionBus.cpp` `USpellCaster.cpp` `ASkillCreatorCharacter.h/.cpp` `TileWorld3D.cpp` | 使用者要求「issues.md 裡說已實作的部分逐項核實是否真的做對、做確實，整份專案找 bug 跟設計意圖不符的部分」。派 5 個 agent 平行逐項核對全部 706 行（不只先前的「整批項目」模式，這次每一條已實作宣稱都重新核實），找到一批反向錯誤待訂正（見下方 issues.md 待辦）+ 13 項真實問題，本輪修復 7 項最嚴重的：①敵人完全無 AI——`BT_Enemy`/`BP_EnemyAIController`/`BP_Enemy` 資產 6/15 已建好但 C++ 從未載入，`AEnemyAIController`/`AEnemy` constructor 補上 `ConstructorHelpers::FObjectFinder`/`FClassFinder` 載入；②`AEnemyManager` 從未被任何程式碼生成（除非關卡手動放置，否則敵人系統不啟動）——GameMode 補上自動生成，且刻意排在 `AMobSpawnController` 之前（順序顛倒會找不到）；③DamageShield「Halve」模式欄位存在但從未讀取；④HP代價刻印語意顛倒（對敵人造成免費傷害，應改為施法者自損 HP），順手修正 `yellow_hp`→`yellow_hp_cost` 打字錯誤；⑤`TryCast()` 從未檢查 `ExceedsLevelCap()`，玩家可無視能力點上限亂施法；⑥Z/X/C 鍵位同時綁在 Enhanced Input（debug 文字洗版）跟 legacy BindKey（真正面板開關），刪除 Character 端重複綁定；⑦CA dirty chunk 超出模擬半徑時被永久丟棄（`DirtyChunks.Empty()` 無條件清空），改為只移除真正處理過的，並補上 Godot 原有的 ±1 margin 擴張。Rebuild 0 錯誤 0 警告，既有 15 個自動化測試全數仍 Pass（無 regression）。⚠️ 還剩 6 項待續（詳見下方）|
| 角色選擇/創建流程補完 + issues.md 稽核 | `UGameFlowWidget.h/.cpp` `CharacterSaveData.h/.cpp` `FlowSaveSystem.h/.cpp` `UGameSessionSubsystem.h` `ASkillCreatorGameMode.h/.cpp` `ASkillCreatorCharacter.h/.cpp` `GameFlowSaveTests.cpp` | 使用者發現 Godot 原版流程（標題→**角色選擇/創建**→世界選擇/創建→遊戲）只搬了世界那一半；查證後發現 2026-06-16 那次實作真的只做了世界，卻把含角色在內的整條 issues.md 項目標記已實作。本次補完：①角色獨立於世界存在（對應 Godot 全域 _chars 清單，可任意搭配世界），`FCharacterSaveData` 新增 `Id` 欄位，存檔位置改為 `{ProjectSavedDir}/Characters/{Id}.json`（不再綁定 WorldDir）；②`FFlowSaveSystem` 新增 ListAllCharacters/CreateNewCharacter/LoadCharacter/SaveCharacter/DeleteCharacter；③`UGameFlowWidget` 新增角色畫面（選擇/建立/刪除，沿用既有世界畫面的單畫面內嵌建立模式以保持一致，非 Godot 的獨立創建子畫面），世界畫面加「← 返回角色選擇」；④`UGameSessionSubsystem` 新增 PendingCharacter；⑤GameMode 改為角色+世界都選定才開局，並呼叫新增的 `Char->ApplyCharacterSaveData()` 套用存檔進度（Level/Xp/Hp/Mp/Inventory/ManaCurrents）取代 BeginPlay 預設值；新增 2 個自動化測試（角色存讀往返、角色清單生命週期）全數通過。⚠️ 範圍邊界：刪除無確認彈窗（Godot 有）、角色進度目前只在選定時載入，遊玩中/退出時尚未寫回存檔（跟既有世界自動存檔是同一個未接通的缺口，非本次新增）。**順手稽核**：派 agent 查核 issues.md 是否有其他「整批標記已實作但部分子項缺失」的同類錯誤，確認全 706 行後找到 6 處（SaveSystem.cs/IWorldInterface.cs/EngraveData.cs/CameraController.cs/PlayerController.cs/FlowSaveSystem.cs），全部已拆分修正為準確狀態 |
| 修復 E 鍵開技能編輯器必崩潰（`AddDynamic` 巨集誤用） | `Source/SkillCreatorRuntime/Private/UBlockEditorWidget.cpp` | 使用者按 E 開 `UBlockEditorWidget` 直接崩潰，崩潰訊息：`Assertion failed: Result && Result[2] != '0'`、`'DotHandlers[i]' does not look like a member function`（`Delegate.h:474`）。根因：`AddDynamic` 是巨集，會把第二個參數**原樣字串化**去查 UFunction 名稱（不是執行期接受函式指標變數），程式碼裡用 `void (UBlockEditorWidget::*DotHandlers[N])() = {...}` 陣列、再迴圈 `Dot->OnClicked.AddDynamic(this, DotHandlers[i])`——巨集字串化後變成字面文字 `"DotHandlers[i]"`，引擎驗證函式名稱格式時直接 assert 崩潰。全專案搜尋同一個模式（`AddDynamic(this, \w+\[`）找到 3 處，都在這個檔案：① `BuildHeader()` 的 5 顆技能組切換 dot、② `BuildPalette()` 的 3 個主分頁、③ `RebuildSubTabColumn()` 的 11 個子分頁——全部改成迴圈內 `switch(i)` + 逐個字面 `&UBlockEditorWidget::OnXxxClicked` 呼叫，移除陣列變數轉發。純 .cpp 修改，Build 0 錯誤 0 警告。 |
| 積木編輯器再稽核：6 個使用者實機回報 bug 全修（導覽流程/拖放消失/版面/垃圾桶/欄寬/誤鎖） | `ASkillCreatorPlayerController.h/.cpp`（E 鍵流程重接）`USpellListWidget.h/.cpp`（圓球變可點擊按鈕）`UBlockEditorWidget.h/.cpp`（拖放延遲重建+版面順序）`UPaletteItemWidget.cpp`（文字換行）`UBlockTrashZoneWidget.h/.cpp`（新增） | 逐一對照 `C:\skill-creator\Scripts\UI\AbilityEditorUI.cs`/`ScratchCanvas.cs`/`SpellListUI.cs`/`Scripts\Main.cs` 原始碼後修復：① **E 鍵跳過圓球列表直接開編輯器**——`Main.cs:1772-1791 ToggleEditor()` 證實 E 鍵應先開 `_spellList`（圓球列表首頁），只有 `Main.cs:1794-1799 OnListActiveSpellClicked` 點擊圓球後才開 `_editor`；UE5 `ASkillCreatorPlayerController::OnOpenEditor()` 之前直接呼叫 `ToggleBlockEditorOverlay()` 跳過了這一層。修法：`USpellListWidget`（原本只是唯讀文字面板，沒有點擊功能）10 個槽位 `UTextBlock` 全部包進 `UButton`、新增 `OnSlotClicked` delegate；`OnOpenEditor()` 改成先開 `ASkillCreatorHUD::SpellListPanel`，綁定 `OnSlotClicked→OnSpellListSlotClicked→OpenBlockEditorForSlot`，原 `ToggleBlockEditorOverlay()` 拆成 `OpenBlockEditorForSlot(SlotIndex)`。② **拖放調色盤卡片到畫布卻憑空消失**——根因是 `UBlockDropZoneWidget::NativeOnDrop`（仍在 Slate 拖放事件分派堆疊上）呼叫 `OnChanged()`→`RebuildList()`→`CenterList->ClearChildren()` 同步銷毀正在處理該事件的子 widget 本身；Godot 對應路徑（`ScratchCanvas.cs:65` `_list.GetChildren().ToArray()` 各 `QueueFree()`）用的是延遲到當前 frame 結束才真正刪除的機制，UE5 `UPanelWidget::ClearChildren()` 沒有等價物、是立即同步銷毀。修法：`RebuildList()` 改成用 `SetTimerForNextTick` 延後一個 tick 才呼叫真正執行重建的新函式 `RebuildListImmediate()`，`bRebuildPending` 防重複排程；`SetEditingSpell()` 等非事件回呼鏈內的呼叫點改呼叫 `RebuildListImmediate()` 維持同步語意。③ **子標籤欄位置在左、應在右**——`AbilityEditorUI.cs:464-497 bodyRow`：`scroll`（內容清單）先 `AddChild`、`subWrap`（子標籤欄）後 `AddChild`，HBoxContainer 子節點順序＝視覺順序，先加的在左；UE5 `BuildPalette()` 原本反過來先加 `SubTabSize` 後加 `ContentScroll`，導致子標籤欄跑到左邊。交換兩者 `AddChildToHorizontalBox` 呼叫順序修正。④ **缺垃圾桶圖示**——`ScriptCanvas.cs:44-46,104-134 _trashZone`：畫布右下角 48×48 Panel + 🗑 icon，拖入卡片放開即刪除整串（`FinishDrag` L495-504）。新增 `UBlockTrashZoneWidget`（`NativeOnDrop` 對 `!bFromPalette && SourceList` 的卡片搬移型拖放執行 `SourceList->RemoveAt`），`BuildBody()` 把 `CenterScroll` 包進新的 `UOverlay`，垃圾桶疊在右下角（`HAlign_Right`/`VAlign_Bottom`）。⑤ **清單欄太窄塞不下描述文字**——比對 `AbilityEditorUI.cs:630/817` Btn() 文字本身不自動換行，但 Godot `CustomMinimumSize` 固定列高所以裁切不明顯；UE5 `UPaletteItemWidget` 的 `UTextBlock` 完全沒開 `AutoWrapText`，長文字直接溢出列外蓋住右側子標籤欄。`UPaletteItemWidget::NativeOnInitialized()` 加 `LabelText->SetAutoWrapText(true)`。⑥ **變幻/位移/召喚/領域分類被誤鎖**——比對 `AbilityEditorUI.cs:620-668 BuildTotemContent`：`RequiredPlayerLevel`（這些分類在 Godot `TotemLibrary.cs:41-64` 確實有設 20/30/50 等級門檻，不是無中生有的欄位）只用來在文字後面附加「LV{n}+」提示（line 629），從未檢查或設定 `btn.Disabled`／灰階顏色——技能因子（Totem）永遠可點擊拖曳，不會被等級鎖死；等級鎖只存在於 `BuildEngraveContent`（line 814 `locked = eng.RequiredPlayerLevel > PlayerLevel` + line 820 `btn.Disabled = locked`），鎖的是刻印不是技能因子。UE5 `RebuildPaletteContent()` 把刻印的鎖邏輯誤套用到技能因子分頁，移除 `bLocked` 判斷（傳 `Item->Setup(..., /*bInLocked=*/false, ...)`），保留 LV 提示文字；刻印分頁的鎖邏輯維持不變（那邊鎖法本來就跟 Godot 一致）。三個 `.h` 改了（新增 UPROPERTY/虛擬函式宣告/新增類別），確認關閉 Editor 後完整 Rebuild，雙 target 皆 0 錯誤 0 警告。⚠️ 已知範圍外限制：Godot `SpellListUI` 還有「被動技能圓球」（`PassiveSpellClicked`）跟滑鼠懸停技能描述 tooltip（`ShowTooltip`/`SpellDescriptionGenerator.GenerateProse`），UE5 `USpellListWidget` 目前只接通主動槽位點擊，被動技能編輯入口（對應 Godot `OpenPassive()`）跟圓球 hover tooltip 仍是缺口，留待下次處理。 |
| 2D 視角投影修正 + 法術熱鍵欄文字隱藏 + 熱鍵欄/採掘/敵人診斷 log | `SkillCameraTypes.h`（新增 `bOrthographic`/`OrthoWidth`）`ASkillCreatorCharacter.cpp`（套用投影模式）`UPlayerHUDWidget.cpp`（HotBarBox Collapsed）`ASkillCreatorGameMode.cpp`（新增 [DIAG] log） | 使用者回報「以前 Godot 的 2D 是真 2D，現在看起來像 2.5D」。對照 `C:\skill-creator\Scripts\World\CameraController.cs:18,164-171 ApplyProjection()` 確認：Godot **Isometric 跟 SideScroll2D 都用正交投影（Orthogonal），只有第三/第一人稱用透視投影**；UE5 4 個視角從建立以來全部都是透視投影，從沒切換過——這才是「看起來像 2.5D」的根本原因，跟相機角度怎麼調都無關，透視投影本身就會有深度線索。另外 `CameraController.cs:219-226` SideScroll2D 相機是 Pitch=0 純水平側視（UE5 原本是 -10° 帶俯角，疊加透視投影兩個因素一起讓 2D 模式看起來有 3D 縱深）。`FCameraModeParams` 新增 `bOrthographic`/`OrthoWidth`（900cm，對應 Godot `OrthoSize=30 tiles × TileSizeCm`），`SetCameraMode()` 呼叫 `Camera->SetProjectionMode()`/`SetOrthoWidth()`；`SideScroll2D` 的 `PitchDeg` 改成 0。同時修正 `SkillCameraTypes.h:12` 列舉 DisplayName 跟 debug overlay 文字裡兩處殘留的「2.5D」誤標（Godot 原始碼/文件從未出現這個說法，是先前某次 session 沒查原始碼自己編的）。另外把法術熱鍵欄文字（`HotBarBox`）依使用者要求直接 `Collapsed`，底層 `UpdateSpellHotBar()` 邏輯不動。**熱鍵欄/準心/可挖掘提示/採掘/敵人全部消失**這組問題：逐一讀過 `AEnemyManager`/`AMobSpawnController`/`StartGameplayWithWorld()`/`UPlayerHUDWidget::BuildCrosshair`/`BuildItemHotbar` 的程式碼邏輯本身都看不出明顯 bug（甚至有 2026-06-19 的歷史註解記錄過同類「忘記生成 AEnemyManager」的 bug 並修復過），純讀程式碼已經找不到更多線索，在 `ASkillCreatorGameMode::StartGameplayWithWorld()` 結尾加一行 `[DIAG]` log（印出 VW/PC/Char/HUD/HUDWidget 是否有效、EnemyManager/MobSpawnController 數量），⚠️ 待使用者下次測試後回報這行 log 內容才能確定問題出在哪一段（找到根因後這段 log 會移除）。雙 target Build 0 錯誤 0 警告。另確認「創建世界後直接進遊戲」（先前列為待查項目）是使用者誤會，創建流程本身正確（`FinishWorldGeneration()` 確實會 `ShowScreen(WorldSelectScreen)`），已排除。 |
