# Phase 3+ History

Auto-archived from C:\Users\譚揚勳\skill-creator\實作進度.md by docs/archive-done.ps1.

| 功能 | 關鍵檔案 | 摘要 |
|------|---------|------|
| 天空背景：漸層 + FBM 程序雲 | `Scripts/World/SkyConfig.cs`（新）/ `SkyController.cs`（新）/ `Main.cs` | `SkyConfig`：TopColor/HorizonColor/CloudCoverage/CloudSpeed/CloudColor/Brightness 公開欄位，遊戲機制可即時修改；`SkyController`：WorldEnvironment + Sky shader（SCREEN_UV.y 垂直漸層 + FBM 程序雲），適用所有相機模式；`Main.cs` 加 `_sky` 欄位；build 0 errors 0 warnings |
| Spawn fix：玩家出生點修正至世界中心 | `Scripts/World/MapGenerator3D.cs` | 新增 `ComputeWorldCenterSpawn()`：以 `_worldW/2` 為 spawnX，呼叫 `GenerateChunkLazy` 生成該 chunk column（全高）；`EnsureConnectivity` 改 void（連通性修復與出生點計算分離）；出生 Y = `GetHeightAt(spawnX,0) - PlayerH`，確保玩家站在地表；build 0 errors 0 warnings |
| E-3：GPU CA 分幀模擬（Z 子區間旋轉） | `Scripts/World/CaGpuSimulator.cs` / `TileWorld3D.cs` | `Simulate(rng, zFrom, zCount)` 只 dispatch 子區間（gz=子區間/2/4）；`Download(ox,oy,oz,zFrom,zCount)` 用 partial `BufferGetData(byteOffset, byteCount)` 只讀 1/4 buffer；GLSL `bz += pc.zFrom`，加上界裁切；`TileWorld3D.Tick()` 用 `(_frame%4)*subD` 旋轉四個 Z 象限；物理更新 15fps/tile，視覺仍 60fps；build 0 errors 0 warnings |
| W-2：MapGen FastNoiseLite FBm 地形 + 雙層洞穴 | `Scripts/World/MapGenerator3D.cs` | 移除 `_hp1-4` sin 疊加；新增 `_heightNoise/_caveThin/_caveWide` 三個 `FastNoiseLite` + `InitNoises(seed)`；`GetHeightAt` 改用 FBm noise（freq=0.0015，5 octave）消除地形週期感；`IsCaveAt` 改用雙場等值面（細蠕蟲：a²+b²<0.025）+ 大洞穴（_caveWide>0.58）；地表防護移至 surfaceH+4；`EnsureChunksGenerated` guard 改為 `_worldW==0`；build 0 errors 0 warnings |
| E-3：PregenerateWorld MC 式創建時預生成 | `Scripts/GameFlow/GameFlowUI.cs` / `Scripts/Main.cs` | 新增 `PregenerateWorld(WorldSaveData)`：建 TileWorld3D → `gen.Generate` → `SaveAllLoadedChunks` → 寫 SpawnXYZ+IsFirstEnter=false；GameFlowUI 確認按鈕改 async + `await ProcessFrame` 讓進度條更新；Main.cs re-entry 路徑改為只呼叫 `InitTerrainParams`（不重跑 FillAll），修正每次進入重新生成整世界的 bug |
| E-2：GPU CA 效能優化（hasPhysics early exit + zone 縮減） | `Scripts/World/CaGpuSimulator.cs` / `WorldScale.cs` / `TileWorld3D.cs` | `Upload()` 改 bool 回傳：chunk 迭代，空 chunk 直接 Clear，非空才逐 tile Pack；追蹤 Powder/Liquid 是否存在，無時跳過整個 BufferUpdate+Simulate+Download；`GpuZoneH` 1600→256（6× buffer 縮減）；wy0 改為以玩家 Y 為中心；`TryGetChunkAt` 新增至 TileWorld3D；build 0 errors 0 warnings |
| G-6：角色/世界刪除功能（🗑 + 確認對話框） | `Scripts/GameFlow/GameFlowUI.cs` | `MakeCharCard`/`MakeWorldCard` 改為 HBoxContainer（卡片 + 🗑 按鈕）；新增 `BuildConfirmDialog`（半透明遮罩 + 對話框，ZIndex=20）+ `ShowConfirm`（_pendingConfirm 模式）；刪除呼叫 `FlowSaveSystem.DeleteWorld/DeleteCharacter`；build 0 errors 0 warnings |
| G-5：WorldSaveData 擴展 + GameFlowUI 創建/進入分離 | `WorldSaveData.cs` / `FlowSaveSystem.cs` / `GameFlowUI.cs` / `Main.cs` | WorldSaveData 加 `WorldDir/SpawnXYZ/IsFirstEnter`；FlowSaveSystem 加 `MakeWorldDir`/`SaveWorld`/`DeleteWorld`/`DeleteCharacter`；GameFlowUI 創建世界時建目錄+填 WorldDir；Main.cs：`StartGameplay` IsFirstEnter 分支（首進生成+儲存出生點 / 再進直接讀）；_Process 每 300 幀 EvictFarChunks；build 0 errors 0 warnings |
| G-3/G-4：chunk 磁碟持久化 + LRU 卸載 | `Scripts/World/TileWorld3D.cs` / `MapGenerator3D.cs` | TileWorld3D 加 `SaveChunk`/`TryLoadChunk`/`SaveAllLoadedChunks`/`EvictFarChunks`（回傳卸載列表）；MapGenerator3D 加 `WorldDir`、`EnsureChunksGenerated` 磁碟優先分支、`EvictFarChunks`（同步清 `_generatedChunks`）；build 0 errors 0 warnings |
| G-2：GenerateChunkLazy 3D 噪音洞穴 | `Scripts/World/MapGenerator3D.cs` | 新增 `IsCaveAt(x,y,z,surfaceH)`：乘積型 sin*sin*cos 兩頻次，threshold=0.40，地下約 10-15% Air（蠕蟲隧道型洞穴）；`GenerateChunkLazy` 加 `if (IsCaveAt) continue`，cave tile 保持 Air 預設值；build 0 errors 0 warnings |
| G-1：GetHeightAt 確定性 per-column 地形 | `Scripts/World/MapGenerator3D.cs` | 加入 `_hp1-4/_worldSeed/W/H/D` instance fields；`GetHeightAt(x,z)` 用 sin 疊加 + 整數 hash 取代 RNG/smoothing；`GenerateHeightmap` 改為 instance method 直接呼叫 `GetHeightAt`；`GenerateChunkLazy` 移除邊界判斷，直接 `GetHeightAt`；chunk 地形在邊界無縫銜接；build 0 errors 0 warnings |
| G-0：Generate 限縮至出生區 48×H×16 | `Scripts/World/MapGenerator3D.cs` | 加入 `initW=Chunk3D.Size*3=48`；所有重操作（FillAll/HeightMap/CaCaves/…）只跑 48×16；`_heights` 縮為 48×16 array；加 `_heightsW/_heightsD` 欄位；`GenerateChunkLazy` 改為越界用固定高度 `H*0.32f`；生成時間從 ~82M→~1.2M tile ops；build 0 errors 0 warnings |
| W-1：世界尺寸擴張（WorldScale 驅動） | `Scripts/Main.cs` | 移除本地 WorldW/H/D 常數；`new TileWorld3D` 改為 `WorldScale.WorldW/H/D`（3200×1600×3200）；`InitGpu` 改為 `GpuZoneW/H/D`（128×1600×128）；依賴 E-1 預配置 byte[] 避免 104MB/frame GC 崩潰 |
| P-2：Main.cs mesh + 相機 OrthoSize | `Scripts/Main.cs` | 玩家 BoxMesh 改為 PlayerW×PlayerH×PlayerW（= 1×2×1 Godot unit）；mesh position Y 中心改為 `pos.Y*T + PlayerH*T*0.5f`；相機 TargetPosition Y 同步；`_orthoZoom` 初值與 OrthoSize block 改為 `WorldScale.OrthoSize`（= 10，tile ≈ 3px） |
| P-3：MapGenerator3D 洞穴放大 + 出生點 | `Scripts/World/MapGenerator3D.cs` | CA seed 0.45→0.55，threshold 14→18/12→15；新增 `EnsureWalkableCaves`（保證每個洞穴地板上方有 PlayerH+4=36 格淨空）；EnsureConnectivity shaft 加寬 3→5 tiles，spawnY 修正為 heights-PlayerH（頭部對齊地表）；build 0 errors 0 warnings |
| P-1：PlayerController BodyH=32 | `Scripts/World/PlayerController.cs` | 新增 `BodyH=WorldScale.PlayerH`/`BodyW=1`/`StepH=2`/`ColumnClear()`；TryMove/TryMoveDir/TryMoveDepth 改掃整列 BodyH 格碰撞 + 最多 StepH 格爬坡；ApplyPhysics/IsOnGround 查 Y+BodyH（腳底）；UpdateEnvironment 查 Y+BodyH-1 |
| E-1：GPU Upload GC 修正 | `Scripts/World/CaGpuSimulator.cs` | 新增 `_stagingBytes byte[]` 在 Initialize 預配置；Upload 改用 `Buffer.BlockCopy` 取代 `MemoryMarshal.AsBytes(...).ToArray()`，消除每幀 3.2MB GC 配置；Dispose 補 null |
| P-0：WorldScale.cs（Grain=16）| `Scripts/World/WorldScale.cs`（新）/ `TileWorldConstants.cs` / `TileWorldRenderer3D.cs` / `Main.cs` | 建立 WorldScale.cs（Grain=16，派生 TileSize/PlayerH/WorldH/OrthoSize 等）；TileWorldConstants.TileSize 改為轉發 property；修正三處 const float T/ZoomMin/ZoomMax → float/static readonly；build 0 errors 0 warnings |
| Preflight Check 7（WorldScale TileSize 迴歸防護） | `preflight-check.ps1` / `docs/plan-scale-world.md` | Check 7 掃描 Main.cs/TileWorldRenderer3D/CameraController 中裸 Godot-unit Vector3/2（WW 警告）；修正 PS 5.1 多位元組字串解析錯誤；plan-scale-world.md 補入自動迴歸保護說明節 |
| Tile 尺度縮放 S-1~S-6（TileSize=1/32） | `Scripts/World/TileWorldConstants.cs`（新）/ `TileWorldRenderer3D.cs` / `Main.cs` | 新增 `TileWorldConstants.TileSize=1f/32f`；`TileWorldRenderer3D.V3()` 所有頂點 × TileSize，chunk 位置 × S×TileSize；Main.cs：玩家/敵人 mesh 位置與尺寸 ×T、滑鼠格座標 worldPos3/T、OrthoZoom 30→30T、ZoomMin/Max ×T、相機臂長（TPS 12T/Iso 25T/Side 40T）；右鍵相機旋轉路徑移除（CameraController 改為純 Captured 模式）；投射物/掉落物無 3D mesh，S-7 N/A；build 0 error 0 warning |
| Phase 3 GPU Compute Shader CA | `Scripts/World/CaGpuSimulator.cs`（新）/ `TileWorld3D.cs` / `Main.cs` | Margolus Block CA GLSL shader 內嵌，Upload/Simulate/Download 管線；active zone 64×200×64；Powder+Liquid 走 GPU，Gas/Static+元素反應保留 CPU；InitGpu 失敗自動回退 CPU |
| 遊戲流程架構 | `Scripts/GameFlow/GameFlowUI.cs`（新）/ `CharacterSaveData.cs`（新）/ `WorldSaveData.cs`（新）/ `FlowSaveSystem.cs`（新）/ `Main.cs` | 標題畫面 → 角色選擇列表 → 創建角色 → 世界選擇列表 → 創建世界 → 進入遊戲；`Main._Ready` 改為只啟動 `GameFlowUI`，世界生成/HUD 建構移至 `StartGameplay(CharacterSaveData, WorldSaveData)`；角色+世界資料序列化至 `user://flowsave.json` |
| Phase 3 多執行緒 Mesh 重建 | `TileWorldRenderer3D.cs` | `Task.Run` worker：Greedy Meshing 純 C# 陣列建構移至 worker 執行緒（`BuildMeshDataOffThread`），主執行緒只跑 `SurfaceTool.Commit()`；`[ThreadStatic] s_maskBuf/s_mergedBuf` 消除 worker 間鎖競爭；`PrecomputeBorderAir` 在主執行緒預先查跨 chunk 鄰居（worker 無需存取 world）；`_inFlight` 字典追蹤 in-flight task，分幀收割完成 task（Pass 0），最多 `ProcessorCount-1` 個並行（上限 8）；`System.Environment.ProcessorCount`（避免與 `Godot.Environment` 衝突）；`MakeMesh` 改 SurfaceTool（避免 GodotSharp 4.6.3 `PackedVector3Array` 建構子缺失問題） |
| Phase 3 渲染優化 — 雙材質 + LOD | `TileWorldRenderer3D.cs` / `MaterialData.cs` / `MaterialRegistry.cs` / `CameraController.cs` | ①FpEyeY -1.5→-0.3：相機固定在玩家格內不踩整數邊界；②恢復 CullMode=Back（不透明材質，約等於 GPU 呼叫減半）；③雙材質 mesh：不透明 pass (Back) + 透明 pass (Disabled+AlphaBlend)，Water/Fire/Steam 走透明 pass；④MaterialData 新增 `Opacity`+`IsTransparent`，GetColor alpha=Opacity；⑤1D 靜態緩衝（`s_maskBuf`/`s_mergedBuf`）消除每幀 60 次陣列配置 GC；⑥LOD pass 1：超出 viewRadius+1 的 chunk mesh 自動 Visible=false |
| FPS 滑鼠左右修正 + 方塊透視穿透 | `CameraController.cs` / `TileWorldRenderer3D.cs` | ① yaw 更新符號錯誤（`-=`→`+=`）：游標右移 → yaw 增加 → 視角右轉（TPS/FPS 均修正）；② 材質 `CullMode=Disabled` 關閉背面剔除：FPS 攝影機貼近格子邊界時不再看穿方塊 |
| 死亡畫面 + 重生點系統 | `Main.cs` / `PlayerController.cs` | 玩家死亡時全螢幕半透明黑色遮罩 + 「你已死亡」（紅色 38pt）+ 重生按鈕；`Main.RespawnPoint`（public GridPos）預設 = 世界初始出生點，可由外部（床、重生石）修改；`PlayerController.Respawn()` 回滿 HP/MP、歸零物理速度、清除冷卻；死亡期間滑鼠釋放（捕捉模式自動解除），重生後恢復視角對應的滑鼠模式 |
| Phase 2-C 相機相對移動 + 斜向 + 3D Facing | `CameraController.cs` / `PlayerController.cs` / `Main.cs` | 暴露 `CameraController.Yaw`；新增 `TryMoveDir(dx, dz)`（斜向支援、最多一格爬坡、Facing 改 3D XZ 方向）；Main.cs WASD 改為以 Camera.Yaw 計算 fwd/right 向量後閾值取整，W=前、S=後、A=左橫移、D=右橫移，SideScroll2D 下 W/S 不觸發 Z |
| Phase 2-C 地形跨坡移動 | `PlayerController.cs` | `TryMove`（A/D）和 `TryMoveDepth`（W/S）新增一格爬升邏輯：若目標 Z/X 被一格高差擋住，且頭頂（Y-1）有空間，自動步上去；修正玩家 Z 只能走 1~3 的根因（高度圖造成的一格升坡讓玩家卡死）|
| Phase 2-C FPS 自身 mesh 隱藏 | `Main.cs` | `_playerMesh.Visible = (mode != FirstPerson)`：第一人稱相機在玩家頭部往外看，切換到 FPS 模式時自動隱藏 player box mesh，避免看到自己；其他視角恢復顯示 |
| Phase 2-C 3D 視覺修正 | `Main.cs` / `CameraController.cs` | ① player/enemy mesh Z 由寫死 0.5 改為 `pos.Z+0.5`；② camera target Z 在非 SideScroll2D 模式跟隨 player.Z；③ SideScroll2D 攝影機改為 -Z 側朝 +Z 看 + WorldUp=(0,-1,0)，修正世界上下顛倒（跳躍視覺錯誤根因）；④ FirstPerson Rotation 加 Z=180° 補正，修正 A/D 顛倒 |
| 真 3D 世界 600×200×600 | `MapGenerator3D.cs` / `TileWorld3D.cs` / `TileWorldRenderer3D.cs` / `Main.cs` | WorldD=WorldW=600。MapGenerator3D 改 instance class：啟動只生成 Z=0 一個 chunk strip（速度同 D=4）；`EnsureChunksGenerated` 每幀懶加載最多 4 個新 chunk。Tick/RebuildDirtyMeshes 加 centerCZ/viewCZ 支援 3D 裁剪。SideScroll2D 模式維持只渲染 Z=0，其他視角渲染玩家 Z 附近。 |
| Phase 2-C WASD 重映射 | `InputBindings.cs` / `PlayerController.cs` / `Main.cs` | W=`move_forward`(+Z)、S=`move_backward`(-Z)、Space=`jump`；物理碰撞從 `TypeAt(x,y)` 全改為 `GetTile(x,y,Position.Z)` 支援 3D 深度移動；加 `TryMoveDepth`；SideScroll2D 模式下 W/S 不觸發 Z 移動。 |
| Phase 2-C TPS 控制校正 | `CameraController.cs` | `_yaw=0°` 時攝影機右向量 = 世界 -X，導致 A 向右走 / D 無效。改 `_yaw=180°`（相機在 -Z 後方，右 = +X）；`HideCursorOnLook=true` 恢復 Minecraft 式 Captured；俯仰改為 -89°~89° 統一 TPS/FPS。 |
| Phase 2-C 灰屏修正 | `MapGenerator3D.cs` | `midZ=0` 使 EnsureConnectivity 迴圈 `z=-1`→`heights[x,-1]` IndexOutOfRangeException。修正：加 `zFrom=Max(0,midZ-D/4)` / `zTo=Min(D,midZ+D/4)` clamping。 |
| 3D 化 Phase 2-C 修正 | `MapGenerator3D.cs` / `CameraController.cs` / `Main.cs` | **A/D 根因**：玩家出生 Z=midZ=2，但 TypeAt/ApplyPhysics shim 查 Z=0，碰撞永遠見到石頭。修正：EnsureConnectivity 改 midZ=0，玩家生在 2D 物理平面。**旋轉**：改為右鍵拖曳（HideCursorOnLook=false 預設），游標始終可見；HideCursorOnLook=true 留給未來設定面板控制 Captured 模式。**準心**：BuildCrosshair() 畫中央 + 形（白色帶黑色陰影，MouseFilter=Ignore）。**放置限制**：右鍵放置只在 SideScroll2D 模式生效，3D 視角右鍵不衝突。 |
| 3D 化 Phase 2-C — 實體視覺 + 滑鼠旋轉 | `Main.cs` / `CameraController.cs` | 玩家（藍色 BoxMesh 0.65×0.9×0.65）、四種敵人各色 BoxMesh（Melee 紅/Ranged 橙/Patrol 藍紫/Heavy 暗紅 1.55×1.8）；`_entitiesRoot` 統一開關（編輯器開時隱藏）；`SyncEnemyMeshes()` per-frame 動態建立/更新；`CameraController` 加 `_pitch` + `ApplyMouseCapture()`：TPS 仰角 5~85°，FPS 俯仰 -80~80° |
| 3D 化效能修正 | `TileWorld3D.cs` / `TileWorldRenderer3D.cs` / `Main.cs` | 三層優化：① `RebuildDirtyMeshes(maxPerFrame:30)` 分幀建 mesh；② `WorldD 32→4`，CA 格數從 3.84M 降至 48 萬；③ `Tick/RebuildDirtyMeshes` 加玩家中心視距裁剪（CA 半徑 6 chunk / Mesh 半徑 5 chunk），最壞情況 169 chunk vs 原始 494 chunk |
| 3D 化 Phase 2-B — Main.cs 全切 | `Main.cs` / `CameraController.cs` | 所有 `_world.World.*` → `_world3d.*`；移除 Camera2D 鏡頭定位，改以 `_camera3d.TargetPosition` 跟隨玩家；世界 Tick 改為手動（`_simStepsPerFrame` 步/幀 + `RebuildDirtyMeshes()`，編輯器開啟時 `_simPaused`）；滑鼠格座標改用 `ProjectScreenToWorld()`；傷害數字改用 `WorldToScreen()`（`WorldPx` 由像素改存世界單位）；`SetOrthoSize()` 加入 CameraController 支援滾輪縮放；SpellCaster/SpellRunner/SpellProjectile/Enemy/PlayerController/SnapshotManager 等所有 `TileWorld` 型別參數已全替換為 `TileWorld3D` |
| 3D 化 Phase 2-A — 渲染骨架 | `TileWorldRenderer3D.cs`（新）/ `CameraController.cs`（新）/ `Chunk3D.cs` | `TileWorldRenderer3D`：ArrayMesh + Greedy Meshing per chunk，手性推導（X/Z 正向、Y 負向），跨 Chunk 鄰居查詢，`RebuildDirtyMeshes()` 由 Tick 後呼叫；`CameraController`：4 視角（第三人稱/第一人稱/俯視45°/2D側捲），Tab 循環切換，世界 Y+=向下補正（LookAt up=(0,-1,0)），SideScroll2D 鎖 +Z 偏移正交；`Chunk3D` 修正：`MarkDirty` 加 `MeshNeedsRebuild=true`、`ClearDirty` 不清 `MeshNeedsRebuild`、新增 `MeshNode` 屬性；三個設計決策確認：鏡頭四視角 + 2D視角=Camera3D Orthographic + SideScroll2D下鎖Z=0 |
| 重置視角按鈕 | `ScriptCanvas.cs` | 畫布左下角新增「⌖ 重置視角」按鈕（82×24px），點擊呼叫 `ResetView()`（_canvasPan→Zero、_canvasZoom→1.0）|
| Ctrl+Left 平移 | `ScriptCanvas.cs` | 按住 Ctrl 時，左鍵拖曳亦可平移畫布（`_panWithLeft = true`）；放開 Ctrl 或左鍵任意一個即結束平移（MouseMotion 輪詢 `Input.IsKeyPressed(Key.Ctrl)`，Left release 優先還原）|
| 畫布縮放/平移 | `ScriptCanvas.cs` | 滾輪縮放（0.2×–3×，滑鼠為圓心）；中鍵拖曳平移；`ToCanvasLocal()` 統一座標轉換；`ResetView()` 公開方法；空白技能首次拖入技能因子時主腳本出現在落點而非左上角 |
| 畫布垃圾桶刪除區 | `ScriptCanvas.cs` | 畫布右下角新增 🗑 垃圾桶（48×48px），拖曳中懸停時高亮；放開積木鏈在垃圾桶上即刪除整串（含主腳本清空 `_blocks`）；palette 積木拖到垃圾桶則靜默捨棄 |
| palette 拖曳重構：浮動積木含動作刻印 | `ScriptCanvas.cs` / `AbilityEditorUI.cs` | `ScriptCanvas` `PaletteBlockDropped` 回呼改帶落點 `Vector2`，加 `SpawnPaletteScript` 公開方法；`AbilityEditorUI` 訂閱後：技能因子 drop 在落點建立浮動積木（含動作刻印，_actInserted 預標記防重複）；空白技能無主腳本則直接加入 `_spell.Blocks` 建立主腳本 |
| 雙擊 Engraving 積木修正 | `BlockScript.cs` | 雙擊 GuiInput 掛載條件從 `Totem` 擴為 `Totem\|Engraving`，修正動作刻印雙擊無反應問題 |
| 動作刻印 1:1 拆分 + 雙擊觸發點修正 | `TotemLibrary.cs` / `AbilityEditorUI.cs` | 18 個共用 act_ 刻印拆為各技能因子專屬（投射物×2、被動×2、形態×5、召喚×6、領域×3）；`ContainerActionIds` 更新為 8 個容器型刻印；`BlockDoubleClicked` 由技能因子積木改為動作刻印積木觸發，往前掃描所屬技能因子取顯示名稱 |
| 術語統一：法陣→技能整構、圖騰→技能因子 | 全 UI + C# 注釋（約 20 檔）| 所有 UI 字串及 C# 注釋中「法陣」改為「技能整構」、「圖騰」改為「技能因子」；類別名稱全為英文不需改；`plan-w6-mp-multitype.md` 同步更新 |
| 刻印 Tab colorOrder off-by-one 修正 | `AbilityEditorUI.cs` | `BuildEngraveContent` 的 `colorOrder` 補上 `EngraveColor.Action` 為第一項，修正各子標籤顯示錯誤刻印類別的問題 |
| 3D 化 Phase 1 — MapGenerator3D | `MapGenerator3D.cs`（新）| XZ 高度圖（雙軸正弦疊加 + 雙軸平滑）；3D CA 洞穴（45% 隨機填充、3×3×3 Moore 鄰域、threshold 14/27）；6-鄰接 FloodFill 連通性保證（打豎井）；底部岩床；6-鄰接 BFS 礦脈 blob；石鐘乳 + 水坑裝飾；洞穴地板敵人 Spawn；完全不動原有 MapGenerator.cs |
| 3D 化 Phase 1 — GridPos 升級 + 完整 CA | `GridPos.cs` / `Chunk3D.cs` / `TileWorld3D.cs` | `GridPos` 改為 `(X, Y, Z)` + 2-arg 向後相容 overload（47 處 `new GridPos(x,y)` 零修改）；`Chunk3D` 加 `Updated[]` 防同幀雙移；`TileWorld3D` 完整移植：全 CA 物理（Powder/Liquid/Gas/Static + 元素反應）、球形 Explode、3D DDA Raycast、球形 SnapshotRegion、IWorldInterface 全實作；Tick 以 Dirty Chunk 驅動（底→頂排序）|
| 3D 化 Phase 0 — 基礎資料結構 | `GridPos3D.cs` / `Chunk3D.cs` / `TileWorld3D.cs`（新）| `GridPos3D` readonly record struct（+/-、DistanceTo、MoveXYZ、ToWorldPos、6 鄰接偏移）；`Chunk3D`（16³、Dirty AABB）；`TileWorld3D`（chunk 字典、GetTile/SetTile、Sand 重力 Tick、InBounds、FloorDiv/PosMod 支援負座標） |
| SpellCaster Design B — ExecuteSlot 接入 Action 刻印 | `SpellCaster.cs` | `ExecuteSlot` 改為讀取 `EngraveCategory.Action` + `EngraveTrigger.OnCast` 刻印驅動行為；新增 `DispatchAction` switch（16 個 `act_*` Id）；`DefaultActionEngraveId` fallback 保底；舊存檔以 TotemType switch 向後相容；`ExecuteTechnique` 新增 `technique_sword/punch/shield` 案例 |
| 自定義圖騰 + 容器進入條件 | `TotemType.cs` / `TotemLibrary.cs` / `ScratchCanvas.cs` / `AbilityEditorUI.cs` | `TotemType.Custom`；每個圖騰子分頁底部「＋ 自定義圖騰」按鈕（可點可拖）；積木上 `SmallEdit(customName)` 直接打字命名；`SyncSlotsFromBlocks` 動態建立合成 `TotemData`；雙擊進入容器效果條件改為「掃描該圖騰後方是否有 `ContainerActionIds` 刻印」，與圖騰類型完全解耦 |
| Design B — 圖騰=宣告 / 刻印=動作 | `EngraveColor.cs` / `EngraveData.cs` / `TotemLibrary.cs` / `AbilityEditorUI.cs` / `EditorSettings.cs`（新）| `EngraveCategory { Modifier, Action }` + `EngraveTrigger { OnCast/Hit/Tick/Expire }`；16 個 `act_` Action 刻印（各圖騰預設行為）；`DefaultActionEngraveId` 對照表（29 個圖騰→刻印 Id）；`AutoInsertBaseEngravings()`（`_actInserted` 標記防重複插入）；⚙ 設定 Popup（`EditorSettings.AutoInsertBaseEngraving` 開關）；刻印面板加「動作」子標籤（青色）|
| Container 概念去除 | `AbilityEditorUI.cs` / `SpellDescriptionGenerator.cs` | 雙擊 Summon/Projectile 圖騰積木→進入容器效果（不再依賴 `_spell.Container`）；`EnterContainerEffect` 改接受 totemDisplayName；法陣摘要移除「直接施放」標籤（只有 non-DirectCast 才顯示容器類型） |
| InvokeTotem 插槽拖放 | `ScratchCanvas.cs` / `ScriptCanvas.cs` | `TotemDropZone`（sealed partial Panel）取代 SlotPicker 下拉；靜態 `ActiveZones` 列表；拖 Totem script / 調色盤 Totem 積木到區域即綁定；橙框拖入提示 + 黃框高亮；`ScriptCanvas._Input` 新增 `IsTotemDragging` / `UpdateTotemZoneHighlight` / `ClearTotemZoneHighlights` |
| O-1/O-2 全域刻印 | `SpellCaster.cs` / `TotemLibrary.cs` | `ApplyGlobalEngravings` helper：`green_death_replace`→ ActionBus one-shot PlayerDeathAction 攔截；`green_invincible`→ duration-based PlayerDamageAction 攔截（`ulong` expiry，BaseEffect=1s）；移除 TODO-STUB 標記 |
| 左側面板子標籤 | `AbilityEditorUI.cs` | 圖騰/積木/刻印三 tab 各加右側豎向子標籤欄（36px）；圖騰 8 類 / 積木 11 類 / 刻印 10 類；點擊子標籤過濾顯示內容，切主 tab 重置子 tab |
| SpellListUI 細修 | `SpellListUI.cs` | 浮動加號圓球縮小至 130px（半尺寸）置右下角；橫向 scrollbar 視覺隱藏（HScrollBar.Modulate=透明）保留滾輪互動 |
| Direction A Runtime 修正 | `SpellCompiler.cs` / `SpellCaster.cs` / `SpellRunner.cs` | BlockType.Totem 在 Compile 時查 totemId→slotRef 自動 emit InvokeTotem；BlockType.Engraving 靜默跳過（純宣告）；修正 BlockName 在 GenerateProse 和拖曳預覽中仍用舊 overload 的 bug |
| Contact 容器 | `SpellCaster.cs` | 掃描前方 3 格命中敵人，20 點接觸傷害 + 技能效果 |
| InvokeSpell 連段 | `SpellCaster.cs` | TriggerCombo 遞迴施放，最深 5 層 |
| 存讀檔系統 | `SaveSystem.cs` | `user://loadout.json`，JsonElement 保留型別 |
| 敵人重生 | `Enemy.cs` / `EnemyManager.cs` | 死亡後 8 秒重生回出生點 |
| 命中位置確認 | `SpellCaster.cs` / `SpellProjectile.cs` | `atHitPoint` flag，slash 偏移從 4→0 |
| 扁平位元組碼 VM | `VM/SpellCompiler.cs` + `ExecutionLoop.cs` | Wait 在分支/循環任意深度結構正確 |
| 文件拆分 | `PLAN.md` / `docs/` | PLAN.md→3 子檔，進度→歷史+現況 |
| 多樣化敵人 AI | `Enemy.cs` / `EnemyManager.cs` / `EnemyProjectile.cs` | Ranged（橙）/ Patrol（藍紫）/ Heavy（暗紅 2×2），各自行為差異化 |
| Scratch 積木編輯器 | `ScratchCanvas.cs` | 色塊卡片、巢狀容器、拖拉重排（頂層），SyncFrom/Changed 解耦介面 |
| SpellRunner | `SpellRunner.cs` | 持久化 ExecutionContext，Wait 真實跨幀計時；PlayerBody 走 Runner，Projectile/Contact 仍同步 |
| 程序生成地圖 | `MapGenerator.cs` | BSP 切割→雕刻房間→L 型走廊→裝飾；玩家/敵人生成點由 SpawnData 傳回，取代寫死座標 |
| Compare + RepeatWhile | `OpCode.cs` / `SpellCompiler.cs` / `ExecutionLoop.cs` / `ScratchCanvas.cs` | 新增 WhileCheck / StoreCompare OpCode；RepeatWhile 編譯為條件跳轉迴圈（最多 500 次迭代 Fizzle 保護）；Compare 積木將比較結果存入具名浮點變數；If/RepeatWhile 條件 UI 支援 compare 子參數與 varBool 分支 |
| ForEachNearby + QueryNearest | `ExecutionContext.cs` / `ExecutionLoop.cs` / `SpellCaster.cs` / `SpellRunner.cs` / `ScratchCanvas.cs` | 新增 ForEachStart / ForEachStep / QueryNearest / GetEntityProp / StoreEntityProp OpCode；SpellCaster/Runner 注入 EntityQuery delegate（按距離排序），ForEach body 自動覆蓋 player.Position 至目標實體；SetEntityProp 透過 PendingEntityDamageIdx 扣血 |
| List 積木 | `OpCode.cs` / `ExecutionContext.cs` / `ExecutionLoop.cs` / `ScratchCanvas.cs` | 新增 ListCreate / ListAppend / ListPop / ListGet OpCode；InstanceLists + GlobalLists 儲存；ListPop/Get 結果寫入具名變數；順帶修復 ResolveNum 無法解析字串數值的 bug（影響 SetVar / Compare 的 UI 輸入） |
| Broadcast / OnReceive | `EventBus.cs`（新）/ `ExecutionContext.cs` / `ExecutionLoop.cs` / `SpellCaster.cs` / `SpellRunner.cs` / `Main.cs` | 新增 EventBus（本幀訊號 HashSet）；WaitingSignal 執行狀態；Broadcast opcode 寫入 EventBus；OnReceive opcode 訊號存在則通過，否則暫停至 Step 頂部偵測；Main._Process 每幀起始 ClearFrame()；同步路徑直接跳過 |
| Scratch 編輯器拖拉改良 | `ScratchCanvas.cs` / `AbilityEditorUI.cs` | 新增 BlockDrag 靜態狀態類別；拆除 same-canvas 限制，drop zone 支援跨容器、跨層級放入；palette 按鈕改為滑鼠按下啟動拖拉，可拖入 canvas 任意位置（含巢狀容器），點擊仍加頂層；移除 BuildBlockRow 死碼（~270 行）|
| B-1~B-5 Bug 修正 | `ScratchCanvas.cs` / `SpellCompiler.cs` / `SpellCaster.cs` / `SpellRunner.cs` / `SafetyGuard.cs` / `EventBus.cs` / `Main.cs` | B-1: If 積木新增 ELSE 分支容器；B-2: EmitBlock 加 default GD.PushWarning；B-3: 消費 PendingEntityDamage 後同步更新 CurrentIterEntity 與 _e.hp；B-4: Main._Ready() 清除 GlobalVars/GlobalLists/EventBus；B-5: MaxComboDepth 統一移至 SafetyGuard |
| D-1~D-6 設計/技術債修正 | `SafetyGuard.cs` / `SpellCaster.cs` / `SpellRunner.cs` / `ExecutionContext.cs` / `ScratchCanvas.cs` | D-1: 提取 ConsumeInvokeTotem/ConsumeEntityDamage 共用 helper；D-2: ExecutionContext.EffectOriginOverride 取代 player.Position 暫改；D-3: MaxStepsPerCast 移至 SafetyGuard；D-4: QueryEnemies 距離只算一次；D-5: BroadcastAndWait 標注「等同廣播」（納入 D-6）；D-6: BlockDescriptor registry 集中管理色彩/名稱/預設積木/UI，AddParams 改為單行查表 |
| L-1~L-4 低優先修正 | `ExecutionContext.cs` / `ExecutionLoop.cs` / `Enemy.cs` / `SpellCaster.cs` / `SpellRunner.cs` / `ScratchCanvas.cs` / `AbilityEditorUI.cs` | L-1: WhileIterCounters Dictionary<int,int> 按 PC 獨立計數；L-2: Enemy.Id 穩定識別碼，EntityInfo/PendingDamage 改用 Id，ConsumeEntityDamage 用 Find 查找；L-3: SetEntityProp 支援 x/y 座標設定（PendingEntityMove + ConsumeEntityMove helper），UI 加 X/Y 選項；L-4: 刪除 AbilityEditorUI.BuildBlockRow 死碼 ~270 行（及關聯的 BuildSlotPicker / 重複 MakeDefaultBlock） |
| 積木拖拉修復 | `ScratchCanvas.cs` | 以 Godot 原生 `_GetDragData` / `_CanDropData` / `_DropData` 虛擬方法（在 Viewport 層處理）取代 `ForceDrag` + `GuiInput` 組合；新增 `DragHandle : Panel` 與 `DropZone : Control` 巢狀類別，根本解決 ScrollContainer 攔截事件導致拖拉失效的問題 |
| 地圖改版（Heightmap + CA） | `MapGenerator.cs` | 完整替換 BSP：Heightmap（3層正弦+噪點）塑形地表、CA（45%初始密度，5輪平滑）挖掘有機洞穴、FloodFill 連通修補、掃描式裝飾（石鐘乳/水坑/木台）、洞穴地板敵人生成點 |
| VM Phase 3 Group 1（控制流） | `BlockType/OpCode/ExecutionLoop/SpellCompiler/ScratchCanvas` | Die（終止法陣）、Evaluate（無 else 條件容器）、Sleep（WaitingFrames 幀計時） |
| VM Phase 3 Group 2（執行追蹤） | 同上 | LoopcastIndex（本陣觸發次數）、SuccessCount（讀 HitTotems.Count）；共用 ReadExecStat OpCode |
| VM Phase 3 Group 3（List 補完） | 同上 | ListDequeue / ListSet / ListLength / ListContains / ListRemoveAt / ListClear；補全 BlockType.cs 已有但 OpCode 缺少的 6 個列表操作 |
| VM Phase 3 Group 4（Vector 運算） | 同上 | 11 個 2D 向量積木（青綠色 CVec）：建立/分量/加減/縮放/反向/單位化/長度/點積/叉積/實體位置；向量以 `name.x`/`name.y` 存入既有 InstanceVars，無需新型別 |
| VM Phase 3 Group 5（Raycast） | `TileWorld.cs` / `ExecutionContext.cs` / `SpellCaster.cs` / `SpellRunner.cs` / VM 四檔 | DDA 射線投射：TileWorld 加 `Raycast()` 方法；RaycastQuery delegate 注入 ctx；結果以 `ray.x/y/hit/mat` 寫入 InstanceVars |
| VM Phase 3 Group 5（FocalPoint） | `PlayerController.cs` / `Main.cs` / `ExecutionContext.cs` / `SpellCaster.cs` / `SpellRunner.cs` / VM 四檔 | 焦點位置：PlayerController 加 `MouseGridPos`，Main._Process 每幀更新；FocalPointQuery delegate 注入 ctx；積木輸出 `focal.x/y` |
| 積木顯示名稱修正 | `ScratchCanvas.cs` | 執行次數→本陣觸發次數；取反→反向；正規化→正規化（轉為單位向量）；點積→點積（內積）；叉積→叉積（外積） |
| 採掘建築系統 Phase A | `Materials/MaterialData.cs` / `MaterialRegistry.cs` | IsMineable / Hardness / RequiredToolTier / BlastResistance / MagicResistance（預留）；各材質填入數值 |
| 採掘建築系統 Phase B | `World/Items/`（新目錄）/ `PlayerController.cs` | ItemId / ItemDrop / ItemStack / ItemData / ItemRegistry / Inventory（30 格，5 熱鍵）；MaterialData 加 DefaultDrops；玩家加 Inventory + MiningRange |
| 採掘建築系統 Phase C | `World/Items/DroppedItem.cs`（新）/ `DroppedItemManager.cs`（新）/ `Main.cs` | 掉落物重力、60 秒壽命、玩家 2 格自動拾取；OnTileDestroyed 觸發生成；TileWorldRenderer 渲染亮金色像素 |
| 採掘建築系統 Phase D | `PlayerController.cs` / `TileWorldRenderer.cs` / `Main.cs` | TickMining 狀態機（進度 × 工具速度倍率）；採掘 overlay 0→130 漸暗；PaintingEnabled 屬性；F1 切換畫筆模式 |
| 採掘建築系統 Phase E | `Main.cs` | 右鍵放置（0.12s 冷卻，距離/Air/重疊驗證）；熱鍵欄 HUD 5 格（StyleBoxFlat 原地更新）；Ctrl+滾輪縮放鏡頭 |
| 採掘建築系統 Phase F | `Main.cs` | 玩家初始 BasicPick + BasicAxe；F1 畫筆/遊玩模式切換 + 頂部指示標籤 |
| 更大的世界 | `TileWorldRenderer.cs` / `Main.cs` | 世界 200×150 → 600×200 格（4 倍面積）；視窗固定 800×600；Camera2D 跟隨玩家，Limit 限於世界邊界；_renderBuf 動態配置 |
| 鏡頭縮放 | `Main.cs` | 預設 10x；ZoomMin=1（全覽）/ ZoomMax=10（近景）；Ctrl+滾輪調整（×1.2 / ÷1.2，約 12 步）；不影響熱鍵欄滾輪功能 |
| 熱鍵欄 UI 改版 | `Main.cs` | 文字 Label 改為色塊縮圖（36×28px，方塊取材質色、工具用固定色）+ 右下角數量；PanelContainer 懸停提示框（自動寬度）顯示物品名稱；左鍵點擊選取槽位；_mouseOverHotbar 防衛避免點格觸發採掘 |
| Wait 路徑修正 | `SpellProjectile.cs` / `SpellCaster.cs` / `SpellRunner.cs` / `ExecutionContext.cs` | Projectile/Contact 命中時改走 SpellRunner.Submit（跨幀，Wait 真實計時）；`ExecutionContext.FixedOrigin` 持久化命中點，取代暫改 player.Position 手法；同步路徑（runner == null）保留向後相容 |
| 遊戲內時間 GameClock | `World/GameClock.cs` / `Main.cs` | 靜態時鐘；20 ticks/現實秒，28,800 ticks/遊戲日（24 現實分鐘）；TotalTicks / DayCount / DayFraction；Main._Ready Reset + _Process Advance |
| §9-B 戰鬥狀態系統 | `World/CombatState.cs` / `SpellCaster.cs` / `SpellProjectile.cs` / `PlayerController.cs` / `Main.cs` | InCombat + BattleId（每場遞增）；脫戰 5 秒超時；CastCount / DamageDealt / KillCount；TookDamageThisFrame 本幀事件 |
| §7 全局戰鬥統計積木 | `VM/OpCode.cs` / `SpellCompiler.cs` / `ExecutionLoop.cs` / `ScratchCanvas.cs` | ReadBattleStat OpCode；GetBattleStat 積木讀取 castCount / damageDealt / killCount → 具名變數 |
| §7 被動反應觸發積木 | `VM/OpCode.cs` / `ExecutionContext.cs` / `ExecutionLoop.cs` / `SpellRunner.cs` / `SpellCaster.cs` / `ScratchCanvas.cs` | WaitCondition OpCode + WaitingCondition 狀態；DetectHpThreshold（HP%低於N）/ DetectMpThreshold（MP%低於N）/ DetectHitReceived（受傷偵測）；Runner 跨幀等待，Caster 同步路徑直接通過 |
| Category A 積木補完（第一批） | `VM/OpCode.cs` / `ExecutionContext.cs` / `ExecutionLoop.cs` / `SpellCompiler.cs` / `ScratchCanvas.cs` / `Main.cs` | SetVarBool/GetVarBool（布林→float 0/1）、QueryNear→QueryNearCount（半徑內敵人數量）、RandomChoice（ThenBranch/ElseBranch 各50%，RandomJump OpCode）、TaskCounter 六組（全域計數器 + OnReach 一次性觸發，Main._Ready 重置）|
| Category A 積木補完（第二批） | 同上 + `SpellRunner.cs` / `SpellCaster.cs` | RisingEdge/FallingEdge（WaitingRisingEdge/FallingEdge 狀態，EdgeState dict 追蹤上幀值）、SinglePulse（EdgeSinglePulse OpCode，PulseArmed 一次性觸發 ThenBranch）、DetectEntityEnter（WaitCondition condKey="entityInRange" 每幀用 EntityQuery 檢查）|
| 角色端系統 | `PlayerController.cs` / `PlayerEquipment.cs`（新）/ `Enemy.cs` / `EnemyManager.cs` / `ItemData.cs` / `ItemId.cs` / `ItemRegistry.cs` / `Main.cs` | XP/等級（殺敵累積 XP，每級 level×100 XP）、AP 上限依等級強制限制、裝備槽（W/A/飾 3 欄，Q 鍵裝備，退回背包）、武器 AtkMult × contact 傷害、護甲 DefFlat 減傷、飾品 MpBonus 加 MaxMp、HUD（LV/XP 進度條 + 裝備欄文字）|
| W-1 境界等級對應 | `PlayerController.cs` / `AbilityPointCalculator.cs` / `Main.cs` | 星盟通用戰力等級（學徒→超凡→征戰→主將→群星→耀日→巔峰→特異）、AP 上限對應境界重寫、境界名稱 HUD（帶徽章色）、境界突破通知（3 秒淡出）|
| W-2 屬性刻印補完 | `ElementType.cs`（新）/ `EngraveData.cs` / `TotemLibrary.cs` / `AbilityEditorUI.cs` | ElementType enum（金木水火土冰風光暗雷毒）、EngraveData.Element 欄位、法則刻印境界門檻（群星 LV50/耀日 LV65/巔峰 LV80/特異 LV100）、刻印庫低境界鎖定（灰字 + 🔒標示）|
| Phase 4 行動攔截鉤子（第三層） | `GameAction.cs`（新）/ `ActionBus.cs`（新）/ `Enemy.cs` / `PlayerController.cs` / `VM/OpCode.cs` / `VM/BlockType.cs` / `VM/ExecutionLoop.cs` / `VM/SpellCompiler.cs` / `ScratchCanvas.cs` / `Main.cs` | GameAction record 型別（EntityDamage / PlayerDamage / PlayerDeath）；ActionBus 靜態管線（Register / Dispatch / ClearAll / UnregisterByTag）；Enemy.TakeDamage + PlayerController.TakeDamage 路由至 ActionBus；新增 RegisterFilter OpCode + DamageShield / DeathGuard BlockType；積木 UI（攔截下一次受傷：取消/減半/封頂；攔截下一次死亡：存活 1HP）；Main._Ready 加 ActionBus.ClearAll() |
| W-3a 元素系統核心框架 | `Elemental/IElementalTarget.cs`（新）/ `Elemental/ElementalStatusEffect.cs`（新）/ `Elemental/ElementalReactionTable.cs`（新）/ `Elemental/ElementalAuraComponent.cs`（新）/ `MaterialData.cs` / `MaterialRegistry.cs` / `Enemy.cs` / `PlayerController.cs` | 統一屬性接觸架構：IElementalTarget 介面；ElementalStatusEffect 抽象基底 + 5 具體效果（鏽化/蔓生/流沙/感電佔位/結凍）；ElementalReactionTable 22 條反應定義；ElementalAuraComponent（多重 Aura + 1 秒環境接觸 CD + A 方案疊層 + MaxStacks 上限）；MaterialData 加 NativeElement；MaterialRegistry 填入 10 種材質元素屬性；Enemy/PlayerController 實作 IElementalTarget，Aura.Process 接入移動/傷害管線 |
| W-3b 元素 CA 材質效果 | `TileWorld.cs` | CheckElementalCaReactions：水+土→Dirt 轉 Sand（流沙侵蝕，0.5% 機率）；岩漿+水→Lava 轉 Stone + Water 轉 Steam（沸騰固化，3% 機率）；UpdateLiquid 末尾觸發；ApplyElementalImpact 公開 API 供 W-3c 使用 |
| W-3c 技能元素 Aura 接入 | `SpellArray.cs` / `SpellProjectile.cs` | SpellArray.PrimaryElement（掃全域刻印+各槽局部刻印取第一個非 None 元素）；SpellProjectile 命中敵人→ ApplyImmediate(elem)，命中材質格→ ApplyElementalImpact(elem)；元素技能正式作用於世界 |
| W-5a 完整數值系統框架 | `CharacterStats.cs`（新）/ `PlayerController.cs` / `SpellCaster.cs` / `SpellRunner.cs` / `Main.cs` | CharacterStats 涵蓋全部數值欄位：生命/防禦/減傷/攻擊/爆率/爆傷/MP系列/移速/攻速/閃避/社會類/元素親和力(Dictionary)/法則能量預留/經驗加成/天賦+基礎能力點；MaxHp 從 const 改為 instance property（Stats.MaxHpBase）；MpRegen / DamageReduction / BaseDefense / MoveSpeedMult 接入 PlayerController 管線；PlayerController.MaxHp 靜態參照全數改為 player.MaxHp |
| W-5b 角色狀態系統 | `CharacterState.cs`（新）/ `PlayerController.cs` | CharacterState：Stamina（體力值）/ MentalEnergy（精力值）/ Mood（心情值）/ HealthCondition enum / SocialStatus flags；戰鬥中消耗、脫戰回復；PlayerController.Tick 每幀觸發 State.Tick |
| W-5b 生存四值 | `CharacterState.cs` / `ElementalAuraComponent.cs` / `PlayerController.cs` / `Main.cs` | 體溫（°C，向環境漂移）/ 口渴（耗盡扣血 2/s）/ 飢餓（耗盡扣血 1/s）/ 氧氣（水下 10 秒耗盡，窒息 15/s）；生存傷害直接扣血繞過防禦；ElementalAuraComponent 新增 AuraTemperatureShift（Fire +15 / Ice -20 / Water -8 °C）；PlayerController.UpdateEnvironment 每幀設 IsOxygenDeprived + AmbientTemperature |
| S-1~S-4b 快照純資料層 | `Snapshot/ISnapshottable.cs` / `AuraSnapshot.cs` / `CharStateSnapshot.cs` / `CharStatsSnapshot.cs` / `EntitySnapshot.cs` | ISnapshottable 介面；AuraSnapshot（Aura+Effect 列表，含 AccumulatedState）；CharStateSnapshot（7 生存值，附 From factory）；CharStatsSnapshot（全數值欄位 + 元素三字典，附 From/ApplyTo）；EntitySnapshot（含 WasAlive + 玩家用 CharState/CharStats nullable） |
| S-5~S-7 快照實作層 | `ElementalStatusEffect.cs` / `ElementalAuraComponent.cs` / `CharacterState.cs` / `CharStatsSnapshot.cs` / `PlayerController.cs` / `Enemy.cs` | ElementalStatusEffect 加 GetAccumulatedState/RestoreAccumulatedState 虛方法（QuicksandSlowEffect 覆寫）；AuraComponent TakeSnapshot/RestoreFromSnapshot（Activator.CreateInstance 還原效果實例）；CharacterState TakeSnapshot/RestoreFromSnapshot；PlayerController/Enemy 實作 ISnapshottable；Enemy 加 ForceRevive（D2 強制復活） |
| S-8/S-9/S-13 調度層 | `Snapshot/SnapshotManager.cs`（新）/ `Snapshot/TileWorldSnapshot.cs`（新）/ `TileWorld.cs` / `SpellRunner.cs` / `Main.cs` | TileWorldSnapshot record（y*Width+x 鍵）；TileWorld.SnapshotRegion（圓形區域）/ RestoreRegion；SpellRunner.ActiveSpell 加 SubmittedAt + MpCost；PruneAfter(anchorTimestamp) 退還 MP；SnapshotManager 靜態棧式（WorldSnapshot = entities + tiles + 時間戳）；TakeSnapshot/ApplyLatest/Clear；Main._Ready 加 Clear() |
| S-10/S-11/S-12 VM 層 | `VM/OpCode.cs` / `VM/BlockType.cs` / `VM/ExecutionContext.cs` / `VM/ExecutionLoop.cs` / `VM/SpellCompiler.cs` / `ScratchCanvas.cs` / `SpellRunner.cs` | AnchorSnapshot + RollbackSnapshot OpCode；Anchor + Rollback BlockType；ExecutionContext 加 AnchorAction/RollbackAction delegates；ExecutionLoop emit（radius 參數 → AnchorAction?.Invoke）；SpellCompiler 路由；ScratchCanvas 法則刻印紫色 BlockDescriptor（Anchor 有 radius SpinBox，Rollback 無參數）；SpellRunner.Submit 注入 delegates |
| Bug 修正（實測回饋） | `SpellCaster.cs` / `SpellProjectile.cs` / `Main.cs` | 投射物方向改為滑鼠游標方向（mouse - player 向量正規化，8 方向，游標重疊時 fallback Facing）；SpellProjectile._dir 支援 Y 軸；熱鍵欄 Tooltip 修復（OffsetRight 未更新 bug → ShowTooltip 加 GetMinimumSize 後顯式 Size 設定）；熱鍵欄數量標籤改為永遠顯示 ×N |
| 生存數值 HUD | `Main.cs` | 左側 HUD 新增 6 條小橫棒（體力/精力/心情/口渴/飢餓/氧氣）+ 體溫文字行；危險閾值時數值標籤轉紅；Level/XP/裝備欄向上移 105px 讓出空間 |
| 角色數值面板 | `Main.cs` | 「角色數值 [C]」按鈕加入右側工具欄；C 鍵切換顯示/隱藏；面板呈現生命/法力/戰鬥/防禦/機動/裝備/天賦所有 CharacterStats 欄位 |
| 缺失積木 UI 接入（13 種） | `ScratchCanvas.cs` | SequentialGate/AlternateTrigger/Discard/EndOfChain/GetComboCount/DetectProjectile/DetectAttack/DetectStatusChange/SetActivationInstant/Declare/Sustained/EffectLabel/OnEffectStart/OnEffectEnd 加入 descriptor；VM 實作待補，目前執行顯示警告 |
| 投射物座標系修正 | `Main.cs` | 以 `Camera.GetScreenCenterPosition()` 明確公式取代 `_world.GetLocalMousePosition()`；Camera2D zoom=10 導致 GetLocalMousePosition 回傳螢幕座標（Y 最大 600/4=150 tile）而非 canvas 座標，玩家在洞穴深處時 MouseGridPos.Y 永遠 < player.Y → dy=-1 → 投射物永遠往上飛；新公式直接用 viewport 滑鼠位置 + 攝影機中心 + zoom 換算，不依賴 Godot 內部座標轉換 |
| 偵錯：F2 座標驗證（3D 更新） | `Main.cs` | 新增「視角」一行（CameraMode 名稱）；玩家座標補顯 Z；panel 尺寸擴大至 240×125；其餘不變 |
| 偵錯：F2 座標驗證 | `Main.cs` | F2 切換半透明 overlay，顯示螢幕/畫布/格座標、玩家格、方向向量、縮放倍率；左上角固定位置；`_debugCoordPanel` + `_debugCoordLabel` 每幀更新 |
| 偵錯：F3 VM 追蹤 | `ExecutionContext.cs` / `ExecutionLoop.cs` / `Main.cs` | `ExecutionContext.TraceMode` 靜態旗標；F3 切換，_Ready 重置為 false；`ExecutionLoop.Step` 每指令前印 `[VM PC=NNN] OpCode params`，狀態改變時額外印 `→ NewState`；`FormatTraceParams` helper 格式化參數（`__` 前綴顯示為 `→N`） |
| 偵錯：F4 生存速率 | `Main.cs` | F4 切換 overlay；即時顯示 8 項生存數值 + 各自的 /s 速率 + 口渴/飢餓/氧氣耗盡倒計時；戰鬥/休息回復速率區分；危險旗標（脫水/飢餓/窒息/低溫/中暑）亮紅 |
| 偵錯：F5/F6 快照完整性 | `Main.cs` | F5：在玩家位置半徑 5 呼叫 `SnapshotManager.TakeSnapshot`，記錄「錨點前」玩家 HP + 存活敵人 HP 陣列 + 腳下材質；F6：`ApplyLatest` 回滾，逐項比對 HP/材質，印 ✓ / ✗MISMATCH |
| 熱鍵欄 Tooltip 修復（二次） | `Main.cs` | 以 `ResetSize()` 取代 `GetMinimumSize()+強制 Size`；PanelContainer 在文字設定後自動重算最小尺寸，不再因 layout 延遲導致寬度為 0 |
| 積木庫補完 | `AbilityEditorUI.cs` | `blockLibCats` 從 8 分類 26 個擴充至 16 分類 82 個（含向量 13 / List 10 / 統計 / Phase 4 攔截與快照等全部缺漏） |
| 鍵位系統 InputBindings | `Scripts/InputBindings.cs` | 全域行為常數（MoveLeft/Jump/CastSpell/OpenInventory 等 25 個）；預設鍵位字典；Godot InputMap 動態注入；玩家自訂存檔（`user://bindings.json`）；Main.cs 全部硬編碼 Key.X 換成 `Input.IsActionPressed` / `k.IsAction` |
| 靜態預檢腳本 | `preflight-check.ps1` | 6 項檢查（BlockType 完整性 / ItemRegistry / MaterialRegistry / 危險 API / InputBindings / 技術債），PS 5.1 相容，0 錯誤即 exit 0 |
| 物品欄面板 | `Main.cs` | I 鍵開關；30 格 5×6 網格（含熱鍵欄分區）；懸停顯示物品名稱；左鍵點擊可裝備物品；active 熱鍵格以金框高亮；與世界互動互斥（開啟時禁止採掘/放置） |
| 裝備欄面板 | `Main.cs` | P 鍵開關；3 欄（武器/防具/飾品）各自顯示圖標色 + 名稱；懸停顯示「點擊卸下」提示；左鍵點擊卸下裝備並退回背包；兩面板可同時開啟 |
| VM BlockType 補完（第二批） | `VM/OpCode.cs` / `VM/ExecutionContext.cs` / `VM/ExecutionLoop.cs` / `VM/SpellCompiler.cs` / `SpellRunner.cs` / `ScratchCanvas.cs` | Discard/EffectLabel（純 UI no-op）、GetVar（複用 SetVar 讀取變數）、GetComboCount（ReadExecStat comboCount）、AlternateTrigger（AlternateJump OpCode，per-PC 計數器，偶→ThenBranch/奇→ElseBranch）、SetActivationInstant/Declare/Sustained（SetActivationMode OpCode，SpellRunner 注入 delegate 修改 ActivationType）；ScratchCanvas 加 AlternateTrigger 容器分支 |
| preflight SpellCompiler 精確化 | `preflight-check.ps1` | 新增 $spExclude（10 個已知待解阻塞項目：TotemDone/Hit/Fizzle 為 EvalCondition 參數、SequentialGate 需 ExtraBranches、6 個需事件基礎設施）；1c 從 Warn 改為 Pass，明確區分「已實作」vs「有意推遲」 |
| 懸浮 Tooltip（游標跟隨） | `Main.cs` | 移除靜態 `_invInfoLabel`/`_eqInfoLabel`；新增 `_floatTooltip` PanelContainer（建構於 BuildFloatTooltip，每幀 UpdateFloatTooltipPos 更新位置）；物品欄/裝備欄 MouseEntered 統一呼叫 ShowFloatTooltip |
| 投射物方向修正 | `Main.cs` | `_Process` 中改以 `(_player.Position + 0.5) * TilePixels` 算出 camCenter，取代 `GetScreenCenterPosition()`（單幀落後問題）；F2 驗證方向向量正常 |
| 按鈕 FocusMode 修正 | `Main.cs` | `MakeBtn` 加 `FocusMode = None`；空白鍵施法時不再意外觸發聚焦按鈕（法陣編輯器自動開啟 bug 修正） |
| 物品欄拖曳 | `Main.cs` / `Inventory.cs` | Inventory 加 `SwapSlots`；`_Input` 處理左鍵按下/放開（按下記 `_dragSrcSlot`，在同格放開→裝備，跨格放開→交換）；拖曳圖示（`_dragFloatIcon`，30×30 色塊）跟隨游標；`SetInputAsHandled()` 防止事件透穿；關閉物品欄自動取消拖曳 |
| 自動裝備（初始 + 拾取） | `Main.cs` / `DroppedItemManager.cs` | `_Ready` 在 TryAdd 初始道具後掃描背包自動穿戴空裝備欄；`DroppedItemManager.Update` 拾取裝備後若對應欄空則自動穿戴 |
| T-1 積木名稱白話化 | `ScratchCanvas.cs` | 21 個 BlockDescriptor Name 改為白話中文（邊緣觸發→剛成立/剛結束、序列閘門→按順序輪流、回朔→回溯 typo 修正等） |
| T-2 開發者工具面板 | `Main.cs` | 材質選擇+筆刷抽出為 `_paintToolPanel`（F1 切換顯示）；右上角新增「🛠 開發者工具」按鈕展開 `_devToolsPanel`（F1~F6 快捷說明清單） |
| T-3 背包 Z 鍵 + 熱鍵欄按鈕 | `Main.cs` / `InputBindings.cs` | `OpenInventory` 改 Z 鍵；熱鍵欄最右側新增「▣」背包按鈕呼叫 `ToggleInventoryPanel` |
| T-4 裝備欄靠右預設開啟 | `Main.cs` / `InputBindings.cs` | `OpenEquipment` 改 X 鍵；`_equipPanel` anchor 改右側（AnchorLeft/Right=1f）；`_Ready` 後預設開啟；標題改「[X 關閉]」 |
| T-5 10 槽技能 + UIOP 組合鍵 | `SpellLoadout.cs` / `InputBindings.cs` / `Main.cs` / `AbilityEditorUI.cs` | MaxSlots 5→10；移除 CastSpell/SpellSlot1-5；InputBindings 改 Key[]；_Process rising-edge 偵測 U/I/O/P 10 種組合；HUD 槽位寬 74px 字型 10；RefreshSlotLabels 顯示 [U]/[U+I] 等提示 |
| W-4 礦石礦脈系統 | `MaterialType.cs` / `MaterialRegistry.cs` / `ItemId.cs` / `ItemRegistry.cs` / `MapGenerator.cs` / `Main.cs` | 新增 CoalOre/CopperOre/IronOre/MagicCrystalOre（共 4 種）；MaterialRegistry 動態陣列大小；BFS 礦脈生成（PlaceOreVeins/PlaceOreBlob，深度分層，煤 120 脈/銅 80/鐵 50/魔晶 25）；礦石 Item 標記 IsPlaceable 供熱鍵欄顯色；挖掘後 DefaultDrops 自動掉落礦石物品；初始背包加 ToolIronPick（Tier2，速度 3.5x）|
| 等加速度物理重力 + CA 障礙物 | `TileWorld.cs` / `TileWorldRenderer.cs` / `PlayerController.cs` / `Enemy.cs` | 玩家/敵人重力從計時器改為連續物理（_vy + _fractY，g=30 tiles/s²，MaxFall=20，JumpSpeed=20）；TileWorld 加 _occupied[] + SetOccupied/ClearOccupied API；TileWorldRenderer Tick 前登記玩家+敵人佔用格；CA 粒子無法穿越實體所在格 |
| 固定傷害積木修正 + 浮動傷害數字 | `SpellCaster.cs` / `SpellProjectile.cs` / `SpellRunner.cs` / `CombatState.cs` / `EnemyManager.cs` / `PlayerController.cs` / `Main.cs` / `ScratchCanvas.cs` | 修正 StoreEntityProp 在投射物/接觸命中路徑下 CurrentIterEntity 未設定導致固定傷害被跳過的 bug；移除 SpellProjectile 硬編碼 25f 傷害；新增 `CombatState.OnHit` 事件 + `SpawnDmgNum` 物件池（24 個 Label），傷害發生時在命中格顯示浮動數字（敵人受傷黃字，玩家受傷紅字，1.2s 上升淡出）；積木顯示名稱校正（共 13 項） |
| 投射物精確直線飛行 | `SpellCaster.cs` / `SpellProjectile.cs` | SpellProjectile 改為浮點位置（_posX/Y）+ 正規化方向向量（_dirX/Y），每步推進 1 個正規化單位；SpellCaster 移除 Math.Sign 量化，以玩家→游標原始 float delta 傳入 → 投射物沿任意角度精確直線飛行，不再鎖 8 方向 |
| 熱鍵欄 10 格 + 數字鍵 + Tooltip | `Inventory.cs` / `Main.cs` | HotbarSize 5→10（10 熱鍵 + 20 主欄）；熱鍵欄 HUD 自動擴展為 10 格，各格左上角顯示 1~9/0 鍵位提示；`_Input` 新增數字鍵 1~9/0 直接切換對應槽位；Tooltip 改用 ShowFloatTooltip（游標跟隨，與物品欄一致）；物品欄面板佈局改為 2 列熱鍵 + 4 列背包 |
| 積木庫全面繁中白話化 | `AbilityEditorUI.cs` | `BlockTypeName` 改為委託 `ScratchCanvas.BlockName`，消除重複定義與英文 fallback；所有積木（Evaluate/Sleep/Die/SequentialGate/AlternateTrigger/DetectHitReceived/DetectProjectile/DetectAttack/DetectStatusChange/QueryNearest/TaskCounterGet 及向量/列表/攔截/快照類）在積木庫一律顯示繁體白話名稱；分類標籤「邊緣觸發」→「觸發條件」、「效果標記」→「效果標示」；修正「回朔」錯字 |
| 接觸容器移除 | `AbilityEditorUI.cs` | 從容器選擇器移除「接觸」選項（ContainerType.Contact enum 值保留供相容，但不再出現於 UI）|
| 施放方式概念重整 | `ContainerType.cs` / `SpellArray.cs` / `SpellCaster.cs` / `AbilityEditorUI.cs` | 概念釐清：容器=裝載效果的實體（投射物/召喚物），本體不是容器；`PlayerBody`→`DirectCast`；新增 `SummonMinion/SummonTurret/SummonGuardian` 三種容器類型（TODO-STUB）；UI 標籤「容器：」→「施放方式：」，新增精靈/砲台/護衛按鈕（深紫色，與直接施放的綠色區隔）；SpellCaster 補入召喚物容器 stub（ExecuteSummonContainer） |
| 圖騰/刻印庫補完 + TODO 標記 | `TotemLibrary.cs` | 補入計畫中缺漏的 5 個刻印：開關式/條件觸發/軌跡回播（藍）、效果劫奪/目標替代（紅）；54→59 種；全部未實作項目標記 `// TODO-STUB` / `// TODO-IMPL`（尚未實作 / 佔位邏輯），可 grep 快速定位；變幻/召喚/領域圖騰補入 RequiredPlayerLevel 門檻 |
| 設計文件對照補完（圖騰+刻印）| `TotemLibrary.cs` | 與「創造角色能力的遊戲 技能」文件比對，補入缺漏項目：新圖騰 5 種（附體形態/召喚武器/建築/載具/傳送門）；新刻印 30 種（白 3 / 橙 6 / 藍 1 / 紅 9 / 綠 7 / 紫 2 / 黃 2）；全部標 `// TODO-STUB`；圖騰 22→27，刻印 59→89 |
| 圖騰架構重整 | `TotemType.cs` / `TotemLibrary.cs` / `SpellCaster.cs` / `AbilityEditorUI.cs` / `BlockAutoGenerator.cs` | 觸發圖騰→**範圍圖騰**（Area）：改為文件定義的形狀（扇形/周身/遠距圓形/射線衝擊）；武技圖騰對齊文件：劍技/拳擊/盾防（移除弓箭），與玩家武器掛鉤（TODO-STUB）；新增**投射物圖騰**（Projectile）：能量投射/實物投射；紫色刻印加入第一人稱射擊（TODO-STUB）；移除舊 EvaluateTrigger 條件邏輯，圖騰全部改為純執行型；BlockAutoGenerator 移除 trigger 分類概念 |
| UI 重構 Stage 1（資料層） | `SpellArray.cs` / `SpellLoadout.cs` / `SaveSystem.cs` / `SafetyGuard.cs` / `AbilityEditorUI.cs` | `SpellArray` 加 `IsPassive` + `ContainerEffect`（遞迴）；`SpellLoadout` 加 `_passiveSpells[20]`（AddPassive/RemovePassive/GetPassive）；`SaveSystem` DTO 擴充（PassiveSpells + ContainerEffect + IsPassive）+ 遞迴序列化深度保護（MaxContainerDepth=2）；`SafetyGuard` 加 `MaxContainerDepth`；AbilityEditorUI Load/Save 接入新簽名 |
| UI 重構 Stage 2（圓球列表） | `Scripts/UI/SpellListUI.cs`（新）/ `AbilityEditorUI.cs` / `Main.cs` | 新建 `SpellListUI`：橫向可滑動圓球列表，主動紅框/被動橘框，超過上限黑色，Hover Tooltip（靜態文字）；`+` 按鈕（列表尾 + 浮動右下角）；`AbilityEditorUI` 加 `BackPressed` 信號 + `← 返回` 按鈕 + `OpenSlot(i)` 公開方法 + `_headerSlotBtns[]` 視覺同步；`Main.cs` E 鍵現在先開 SpellListUI，點圓球才進 AbilityEditorUI |
| UI 重構 Stage 3-A（自由畫布架構） | `Scripts/UI/BlockScript.cs`（新）/ `Scripts/UI/ScriptCanvas.cs`（新）/ `ScratchCanvas.cs` / `AbilityEditorUI.cs` | 新建 `BlockScript`（絕對定位積木鏈，Header 整條拖移、色條斷開、分支容器、`SplitAt`/`AppendBlocks`）；新建 `ScriptCanvas`（自由畫布，多腳本絕對定位，`_Input` 拖曳 + 40px 磁吸連結，`SyncFrom`/`Changed` API 與舊 ScratchCanvas 相容）；`ScratchCanvas` 重構（`BlockDescriptor.BuildUI` 改傳 `Func<opts>?`、`SlotPicker` 改 `internal static`、全輔助方法改 `internal static`、加 `TryGetDescriptor`）；`AbilityEditorUI` 將 `_canvas` 型別改為 `ScriptCanvas` |
| UI 重構 Stage 4（容器效果編輯頁） | `AbilityEditorUI.cs` / `SafetyGuard.cs` | **深度上限改 3**：SafetyGuard.MaxContainerDepth 2→3（SaveSystem 遞迴序列化自動跟進）；**容器導覽棧**：`_navStack` 讓 `_spell` property 在所有深度自動指向正確 SpellArray；**BuildHeader 重構**：`_depth0Only`（名稱/主被動/發動/槽位，depth>0 隱藏）+ `_breadcrumbLabel`（路徑，depth>0 顯示）+ 施放方式按鈕所有深度可見 + `_enterContainerBtn`（選了非 DirectCast 且未達深度上限才顯示，最深層加提示）；**EnterContainerEffect**：ConfirmationDialog 確認 → push navStack → RefreshAll；**← 上一層**：depth>0 時 pop navStack，depth=0 才 BackPressed；SelectEditorSlot/OpenSlot 切換槽位時 Clear navStack |
| UI 重構 Stage 5（法陣文案生成） | `Scripts/UI/SpellDescriptionGenerator.cs`（新）/ `AbilityEditorUI.cs` / `SpellListUI.cs` | `GenerateStructured`：編輯器右側即時摘要（施放方式/圖騰/刻印/積木）；`GenerateProse`：圓球列表 Tooltip 散文段落 |
| 被動圖騰重構 | `TotemType.cs` / `TotemLibrary.cs` / `SpellArray.cs` / `SaveSystem.cs` / `AbilityEditorUI.cs` / `SpellLoadout.cs` / `Main.cs` | 新增 `TotemType.Passive` + `passive_continuous`/`passive_switch` 圖騰；`IsPassive` 改為計算屬性（掃槽位）；`blue_passive`/`blue_switch_mode` 刻印移至圖騰層；SaveSystem 向後相容遷移（舊存檔 IsPassive:true 自動補 passive_continuous 圖騰）；`AbilityEditorUI` SetSpellPassive helper 取代直接設定 flag |
| Direction A UI 重構 | `BlockType.cs` / `ScratchCanvas.cs` / `BlockScript.cs` / `SpellListUI.cs` / `AbilityEditorUI.cs` / `SaveSystem.cs` / `SpellDescriptionGenerator.cs` | **Header 簡化**：只保留 ← 返回 + 名稱輸入 + 麵包屑 + 容器效果按鈕；**插槽區移除**：BuildCenter 刪除插槽排列+捲動區，canvas 填滿；**圓球升級**：CircleSize 80→130，藍框（主動）/ 青框（被動）高質感；**BlockType.Totem+Engraving**：圖騰/刻印以積木形式進入 canvas（青藍/淡紫色）；**SyncSlotsFromBlocks**：canvas 積木掃描後重建 Slots/GlobalEngravings/Container；**SaveSystem 遷移**：舊存檔無 Totem 積木時自動從 Slots 生成 |
| UI 重構 Stage 3-C（左側面板 + 儲存驗證） | `AbilityEditorUI.cs` | **新版左側面板**：頂部 3 個母分類 Tab（圖騰/積木/刻印），切換重建可捲動子內容（`RebuildLeftContent`/`BuildTotemContent`/`BuildBlockContent`/`BuildEngraveContent`）；**主被動 toggle**：Header 加 `[主動][被動]` ButtonGroup，`RefreshAll` 同步狀態；**儲存驗證 AcceptDialog**：`SaveSpell` 改為收集所有錯誤後一次彈出（名稱空白 / AP 超上限），儲存成功訊息加 主/被動 標注 |
| UI 重構 Stage 3-B（完整 Scratch 互動） | `ScriptCanvas.cs` / `BlockScript.cs` / `ScratchCanvas.cs` / `ExecutionLoop.cs` / `AbilityEditorUI.cs` | **調色盤拖放**：`AbilityEditorUI._Input` 偵測 4px 門檻啟動 `BlockDrag.BeginNew`；`ScriptCanvas._Input` 拾取 drop 並生成新腳本；**C-shape 容器視覺**：`BlockScript.MakeBranch` 以 `body Panel`（左邊框）+ `closeRow`（armEnd+shelf）組成 C 形底邊；**Reporter 積木槽**：RepeatN/Wait/Sleep/SetEntityProp 的 NUMBER 參數改 `SmallEdit`（支援輸入數值或變數名稱），`ExecutionLoop` 四處改呼叫 `ResolveNum`；**SnapPoint 視覺高亮**：拖曳期間 `_snapHL`（綠色橫條 4px）顯示在最近目標底端（60px 範圍內）；**游標預覽**：進入 ScriptCanvas 時顯示 `_palPreview` Label 跟隨游標 |
| R-1~R-3+：Raycast 面法線 + face-aligned 放置 | `TileWorld3D.cs` / `CameraController.cs` / `PlayerController.cs` / `Main.cs` / `PlacementValidator.cs`（新）/ `ItemRegistry.cs` | **R-1**：3D Raycast 加回傳 `GridPos FaceNormal`（DDA lastAxis 追蹤）；2D wrapper 向後相容；**R-2**：`CameraController.GetCenterRay()`；`PlayerController.MouseFaceNormal`；Main.cs 非 SideScroll2D 模式改用 Raycast 更新 MouseGridPos+MouseFaceNormal；**R-3**：放置改 `placeTarget = MouseGridPos + MouseFaceNormal`，呼叫 `SetTile(x,y,z)`；**R-3+**：新增 `PlacementValidator.CanPlace`（Air 检查集中於此）；方塊/礦石 MaxStack 99→99999；build 0 errors 0 warnings |
| Bug Fix：Z 世界狹長 + 天空不顯示 | `Scripts/World/TileWorldRenderer3D.cs` / `SkyController.cs` | **Z狹長**：Renderer Pass 2 的 2D 模式 skip 行原本清除 dirty flag（`MeshNeedsRebuild=false`），導致切換到 FP 模式時 Z≠0 chunks 永遠不重建；改為只 `continue` 保留 dirty flag，FP 模式進入後正常重建；**天空不顯示**：spatial shader QuadMesh 深度排序不可靠；改用 `WorldEnvironment + Sky shader_type sky + SCREEN_UV.y`，Godot 內建背景系統保證在所有相機模式、所有幾何體後方渲染；build 0 errors 0 warnings |
| R-4 採掘補完：形狀採掘 + 面板游標 | `DestroyReason.cs` / `DroppedItemManager.cs` / `PlayerController.cs` / `Main.cs` | 新增 `ShapeMining` reason（per-tile 靜默）；`TickMining` 改回傳 `MaterialType?`；採掘破壞後套 `_activeShape` × N 格 + spawn 1 個掉落物；N 鍵面板開啟時 MouseMode→Visible，關閉時 `ApplyMouseCapture()` 還原；build 0 errors 0 warnings |
| Bug Fix：方向光遮蔽（凹洞深度感） | `TileWorldRenderer3D.cs` | Greedy mesh 頂點色乘方向係數：+Y=100%（地面）/ ±X=80% / ±Z=70% / −Y=45%（底面最暗）；不依賴 lighting/shader，Unshaded 模式即有立體感；挖出的坑洞側壁明顯較暗，凹洞層次清晰可辨；build 0 errors 0 warnings |
| Bug Fix：3D 地板 +Y 面透明（走在空氣上不透明） | `TileWorldRenderer3D.cs` | `_matOpaque` 加 `CullMode=Disabled`：Greedy mesh 只在 Air 鄰居方向生成面，雙面渲染無 artifact，且修正 +Y/−Y 繞序 CW→ Godot 剔除導致地板/天花板透明的問題；同步修正採掘失效（MiningRange 5→192 tile）、掉落物無法拾取（以腳底為基準 + 3D 距離）、玩家踩空（GetTilePhysics）、一方向視角散落格（Chebyshev shell 生成順序）；build 0 errors 0 warnings |
| T-5：10 個技能欄位 + U/I/O/P 組合鍵施放 | `Main.cs` / `SpellLoadout.cs` | `MaxSlots` 5→10；熱鍵欄 10 格 + 組合鍵提示標籤；U/I/O/P rising-edge 多鍵優先偵測（最多鍵優先，避免子集誤觸發）；舊 Space/1–5 切換施放機制移除 |
| 採掘修復 + 地形大幅調整 | `Main.cs` / `MapGenerator3D.cs` | **採掘 2D**：MouseGridPos Z 從硬碼 0 改為 `_player.Position.Z`（世界中心化後 Z=0 距玩家 1600 格，DistanceTo 永遠 >> 5）；**採掘 3D**：Raycast 距離 10→50（相機在玩家後 12 tile，舊值永遠打不到地形）；**地形**：height noise 頻率 0.0015→0.001、octave 5→7（增加地表細節）、振幅 0.13→0.20（山高 ±320 tile = ±10 玩家高）、clamp 放寬至 [10%,57%]；**洞穴**：_caveWide 閾值 0.58→0.75（洞穴密度 21%→12.5%）；**出生預生成**：ComputeWorldCenterSpawn 改為先生成 3×3 chunk 範圍，防止出生後四周全空；build 0 errors 0 warnings |
| 世界中心化 + 出生點修正 | `WorldScale.cs` / `MapGenerator3D.cs` / `TileWorldRenderer3D.cs` / `Main.cs`（7 處） | 新增 `WorldScale.OriginX/Z`（= 100f）；世界 X/Z 原點移至 Godot (0,0,0)；玩家出生 spawnZ 從 0 改為 `_worldD/2`；chunk 渲染、玩家/敵人 mesh、相機跟隨、2D/3D 滑鼠格座標轉換、傷害數字全部套用偏移；build 0 errors 0 warnings |
| 歸檔輔助腳本 | `docs/archive-done.ps1`（新）/ `CLAUDE.md` | 新增 `archive-done.ps1`：最新完成表格超 8 筆時保留最新 5 筆，舊紀錄追加到 `docs/history/phase3.md`；CLAUDE.md 補充「最新完成表格修剪」規則說明 |
| R-5：碎片系統（DestroyReason + Fragment Items） | `DestroyReason.cs`（新）/ `ItemId.cs` / `ItemRegistry.cs` / `MaterialData.cs` / `MaterialRegistry.cs` / `IWorldInterface.cs` / `TileWorld3D.cs` / `TileWorld.cs` / `MockWorld.cs` / `DroppedItemManager.cs` / `PlayerController.cs` / `Main.cs` | `DestroyReason` enum（Mining/Explosion/Slash/Crush）；`OnTileDestroyed` 加第三參數 `DestroyReason`；`DestroyTile(pos, reason=Mining)` 介面統一更新；9 種 Fragment ItemId + ItemRegistry 登記；`MaterialData.FragmentItem` 欄位；MaterialRegistry 全部可採掘材質填 FragmentItem；`DroppedItemManager.Spawn(pos,mat,reason)` Mining→DefaultDrops / Explosion→return early；`SpawnFragments(center,mat,tileCount,reason)` 批次碎片生成 helper；build 0 errors 0 warnings |
| R-4：多形狀放置（PlacementShape） | `PlacementShape.cs`（新）/ `InputBindings.cs` / `Main.cs` | `PlacementShape` enum（Single/Cube/Sphere/Cylinder/Cone/TriPyramid）+ `ShapeVoxels.GetOffsets`（radius 自動取 `PlayerH/6`）；N 鍵切換形狀選單（螢幕中央 2×3 按鈕面板）；左下角形狀指示器（HP 上方）；放置邏輯改批次：foreach offsets → CanPlace → SetTile，成功任一格消耗 1 物品；build 0 errors 0 warnings |
| R-6b：採掘分流 + 完美移除 toggle | `Main.cs` | `TickMining` 前先 `TryGetUnit(target)` 抓 pendingUnit；挖中放置物件且「完美移除」開啟時，`RemoveUnit` 移除整個 Unit 的剩餘 tiles，DestroyTile 靜默銷毀，spawn 1 物品返還；關閉或非放置格走原形狀採掘路線；設定面板新增「完美移除放置物件」CheckBox（預設開啟）；build 0 errors 0 warnings |
| R-6a/d：放置物件 Registry 核心 + 持久化 | `PlacedUnit.cs`（新）/ `PlacedObjectRegistry.cs`（新）/ `Main.cs` | `PlacedUnit`（Id/Mat/Tiles/Original/OriginalCount/Damage/IsIntact）+ `PlacedObjectRegistry`（Register/TryGetUnit/NotifyDestroyed/RemoveUnit/Disintegrate）；Damage≥0.5 自動解體；放置後 Register；`OnTileDestroyed` 呼叫 NotifyDestroyed；Load 於 StartGameplay、Save 於 EvictFarChunks 每 300 幀；placed-registry.json 序列化 Original+Tiles 兩份 tiles 陣列；build 0 errors 0 warnings |
| 設定面板 + 長按連放 toggle | `InputBindings.cs` / `Main.cs` | B 鍵或右下 ⚙ 按鈕開關設定面板；面板內「長按連放」CheckBox 控制放置行為：開啟=長按持續放（冷卻 0.12s），關閉=單擊觸發一次（rising-edge）；`_rightWasPressed` 追蹤上幀狀態；build 0 errors 0 warnings |
| Bug Fix：掉落物 3D 渲染缺失 | `Scripts/World/Items/DroppedItem.cs` / `Scripts/Main.cs` | `DroppedItem.Update` 從 2D `TypeAt(x,y)` 改 3D `GetTile(x,y,z)`；`Main.cs` 新增 `_itemMeshes` 字典 + `SyncDroppedItemMeshes()`（4-tile 金黃 BoxMesh，每幀與 `_droppedItems.Items` 同步）；掉落物現在以金黃小方塊出現並跟隨重力落地；build 0 errors 0 warnings |
| Bug Fix：Z 世界狹長 + 天空不顯示 | `Scripts/World/TileWorldRenderer3D.cs` / `SkyController.cs` | **Z狹長**：Renderer Pass 2 的 2D 模式 skip 行原本清除 dirty flag（`MeshNeedsRebuild=false`），導致切換到 FP 模式時 Z≠0 chunks 永遠不重建；改為只 `continue` 保留 dirty flag，FP 模式進入後正常重建；**天空不顯示**：spatial shader QuadMesh 深度排序不可靠；改用 `WorldEnvironment + Sky shader_type sky + SCREEN_UV.y`，Godot 內建背景系統保證在所有相機模式、所有幾何體後方渲染；build 0 errors 0 warnings |
| R-4 採掘補完：形狀採掘 + 面板游標 | `DestroyReason.cs` / `DroppedItemManager.cs` / `PlayerController.cs` / `Main.cs` | 新增 `ShapeMining` reason（per-tile 靜默）；`TickMining` 改回傳 `MaterialType?`；採掘破壞後套 `_activeShape` × N 格 + spawn 1 個掉落物；N 鍵面板開啟時 MouseMode→Visible，關閉時 `ApplyMouseCapture()` 還原；build 0 errors 0 warnings |
| Bug Fix：方向光遮蔽（凹洞深度感） | `TileWorldRenderer3D.cs` | Greedy mesh 頂點色乘方向係數：+Y=100%（地面）/ ±X=80% / ±Z=70% / −Y=45%（底面最暗）；不依賴 lighting/shader，Unshaded 模式即有立體感；挖出的坑洞側壁明顯較暗，凹洞層次清晰可辨；build 0 errors 0 warnings |
| Bug Fix：3D 地板 +Y 面透明（走在空氣上不透明） | `TileWorldRenderer3D.cs` | `_matOpaque` 加 `CullMode=Disabled`：Greedy mesh 只在 Air 鄰居方向生成面，雙面渲染無 artifact，且修正 +Y/−Y 繞序 CW→ Godot 剔除導致地板/天花板透明的問題；同步修正採掘失效（MiningRange 5→192 tile）、掉落物無法拾取（以腳底為基準 + 3D 距離）、玩家踩空（GetTilePhysics）、一方向視角散落格（Chebyshev shell 生成順序）；build 0 errors 0 warnings |
| X-ray 模式 | `InputBindings.cs` / `TileWorldRenderer3D.cs` / `Main.cs` | Tab 鍵切換；`TileWorldRenderer3D.XrayZRadius` 控制 Z 可見半徑（-1=關閉，0=玩家 Z ±1 chunk）；LOD pass 改為雙向（hide/show），X-ray 關閉後自動恢復；build 0 errors 0 warnings |
| CA 特效 3D 化 | `SpellCaster.cs` | 所有 `world.Explode/TypeAt/Set/InBoundsPublic` 2D shim 呼叫全數改為 3D API（補 `p.Z`）；`GridPos` 建構子補 Z；ExecuteTechnique / ExecuteArea / ExecuteMorph / ExecuteDisplacement / ExecuteSummon 全部修正；build 0 errors 0 warnings |
| 投射物 3D 飛行 | `SpellProjectile.cs` / `SpellCaster.cs` | 加 `_dirZ`/`_posZ`；3D 方向歸一化；`next` 包含 Z；世界碰撞 / 元素命中改用 3D API；施放起點補 Z offset；方向向量從 `mouseDelta.Z` 取得；build 0 errors 0 warnings |
| 存檔效能優化 + 玩家資料持久化 | `Chunk3D.cs` / `TileWorld3D.cs` / `CharacterSaveData.cs` / `Main.cs` | `Chunk3D.NeedsSave`（在 `MarkDirty` 中設置，`SaveChunk` 後清除）；`SaveDirtyChunks()` 只寫異動 chunks；退出鉤子改 `SaveAll(fullChunkSave:true)` 保留安全網；`CharacterSaveData` 新增 `Mp/InventorySlots/ActiveHotbar`；`StartGameplay` 非首次進入時還原 Hp/Mp/Level/Xp/Inventory；`SaveAll()` 同步更新玩家位置（SpawnX/Y/Z）並呼叫 `SaveCharacter`；build 0 errors 0 warnings |
| act_area 四形狀 + orange/red/yellow 刻印接通 | `SpellCaster.cs` / `SpellRunner.cs` / `ElementalAuraComponent.cs` | **area shapes**：`ExecuteArea` 拆分為 fan（扇形，隨距離展開）/ around（周身爆炸）/ distant（遠距定點）/ beam（射線縱火/水）4 種真實形狀；**ReadMods 擴充**：讀入 `orange_push/pull/slow/freeze` 、`red_stun`、`yellow_hp_cost`；**ExecuteSlot**：施放前扣 HpCost，施放後呼叫 `ApplyModsToNearbyEnemies`（在效果半徑內掃描敵人，套用擊退/牽引/凍結/暈眩/緩速）；**`enemies` 傳遞鏈**：`ConsumeInvokeTotem`→`ResolveTotem`→`ExecuteSlot` 全加 `EnemyManager?` 參數；`ElementalAuraComponent.ApplyFreeze/ApplySlow` 新增 public facade；Preflight Check 7 移除 Main.cs 誤報（53 WW → OK）；build 0 errors 0 warnings |
| 虛空破圖修正 + 生成距離改採掘單位 | `Chunk3D.cs` / `TileWorld3D.cs` / `MobSpawnController.cs` / `Main.cs` | **虛空破圖**：挖掘邊界 tile 時 `SetTile`/`WriteCell`/`SetCellFromGpu` 呼叫 `MarkNeighborsMeshDirty`，相鄰 chunk 立即 `FlagMeshRebuild()`，根除邊界面不更新問題；**生成距離**：`MobSpawnController` 常數改為 `N × WorldScale.Grain`（32/80/96/128 採掘單位），`EnsureChunkAt` delegate 注入 `MapGenerator3D`，`TryFindSpawnPos` 在採樣前按需生成目標 chunk column；build 0 errors 0 warnings |
| 地表水池擴散 + 放置碰撞 | `MapGenerator3D.cs` / `Main.cs` | **水池**：`AddSurfaceWater` 放寬盆地判斷（≥2 鄰居較淺），再 BFS 擴散 Manhattan≤2 格（同深度或更深），形成小型水池；`GenerateChunkLazy` 換用 `IsSurfaceWaterTile` instance method（同邏輯）；**放置碰撞**：舊 `p != _player.Position` 改為 `OccupiedByEntity`，完整覆蓋玩家 PlayerW×PlayerH 矩形 + 存活敵人座標；build 0 errors 0 warnings |
| 動態怪物生成系統 | `SpawnCategory.cs` / `MobSpawnController.cs` / `EnemyManager.cs` / `Enemy.cs` / `Main.cs` / `MapGenerator3D.cs` | 新增 4 類生成分類（Common/Area/Specific/Boss）；`MobSpawnController` 仿 Minecraft 生成環（MinDist=32 / MaxDist=80 tile，上限 8 隻）；`SpawnRateMultiplier` 供技能道具調整；死亡 Common 怪從列表移除（生成器補充），非動態怪維持原地復活；孤兒 mesh 自動清理；`MapGenerator3D.BuildSpawns` 精簡為僅回傳玩家出生點；build 0 errors 0 warnings |
| 敵人物理 3D 化 + 敵人 3D AI | `Enemy.cs` / `EnemyProjectile.cs` | `TryMoveX` → `TryMoveXZ(world, dx, dz)`：優先對角移動，fallback X → Z；`ApplyGravity` 補 Z（`new GridPos(x, y+1, z)` + `world.GetTile`）；巡邏路徑補 Z；新增 `FacingZ` 屬性；`Update` 改為 3D 曼哈頓距離（含 Z），計算 `dz`，更新 `FacingZ`，所有 UpdateX 方法加 `dz` 參數；`EnemyProjectile` 修正 `new GridPos(x, y, z)` 含 start.Z，移動 `next` 補 Z，`InBoundsPublic`→`InBounds`，`TypeAt`→`GetTile`；build 0 errors 0 warnings |
| 自動存檔機制 | `Main.cs` | `SaveAll()`（chunks + placed registry + world metadata）；三觸發點：退出視窗（`_Notification` WMCloseRequest）、每 30 秒（`_autoSaveTimer`）、玩家死亡（`_deathScreen` 首次顯示時）；build 0 errors 0 warnings |
| R-6c 殘餘格處理 | `Main.cs` | 完美移除模式遇到 PlacedUnit 時，不再因 Unit 剛達 Damage≥0.5 解體而 fall-through 到形狀採掘路線；解體 → 僅返還 1 物品，剩餘 tiles 靜默變世界原生；Damage<0.5 完整時仍完美移除全部剩餘 tiles；build 0 errors 0 warnings |
| T-5：10 個技能欄位 + U/I/O/P 組合鍵施放 | `Main.cs` / `SpellLoadout.cs` | `MaxSlots` 5→10；熱鍵欄 10 格 + 組合鍵提示標籤；U/I/O/P rising-edge 多鍵優先偵測（最多鍵優先，避免子集誤觸發）；舊 Space/1–5 切換施放機制移除 |
| Bug Fix 5 項：周身衝擊 / 投射物視覺 / 敵人重生 / 刻印 pts 即時刷新 | `SpellCaster.cs` / `TileWorldRenderer3D.cs` / `MapGenerator3D.cs` / `Main.cs` / `ScriptCanvas.cs` / `BlockScript.cs` / `AbilityEditorUI.cs` | **1 周身衝擊**：`ExecuteArea` origin 改用 body-centre Y（`+PlayerH/2`）+ baseR × Grain（16）讓爆炸正確涵蓋地形；**2 投射物視覺**：`TileWorldRenderer3D.SetProjectiles` 逐幀同步 SpellProjectile 位置到黃色球體 MeshInstance3D；**3 敵人重生**：`MapGenerator3D.RebuildSpawns` 在載入舊世界後以洞窟 FloodFill 重建出生點（替代原本 `EnemySpawns=[]`）；**4 刻印 pts 即時更新**：`BlockScript` 在 `BuildUI` 後掃 SpinBox/OptionButton 子節點，ValueChanged 觸發 `_onParamChanged`；`ScriptCanvas.ParamChanged` 事件傳回 `AbilityEditorUI`，呼叫 `SyncSlotsFromBlocks` + `RefreshCost`；build 0 errors 0 warnings |
| Bug Fix 補漏：敵人重生 chunk 預載 + 地表水池 | `MapGenerator3D.cs` | **敵人**：`RebuildSpawns` 原本在空世界呼叫 FloodFill（chunks 未載入，全視作 Air），floors 清單永遠空；修正為在 FloodFill 前同步預載初始區域所有 chunks（3×allY×1 chunk 矩陣）；**地表水**：`AddSurfaceWater` static method（local-max Y = 地形盆地，置換 Dirt→Water）加入 `Generate` 流程；`GenerateChunkLazy` 同步加 basin detection，懶加載遠端 chunks 也享有地表水；需建立新世界才可見水（舊存檔地表 chunks 已固化在磁碟）；build 0 errors 0 warnings |
| 地表水 Grain-scale Flood Fill + 生成環簡化 | `MapGenerator3D.cs` / `MobSpawnController.cs` | **地表水**：刪舊 tile-scale 盆地判斷（全地圖淹水問題），改為 Grain cell 降採樣（取 min-h）→ 找局部最深種子 → BFS 閉合盆地（MinArea=10 Grain cells，MaxDepth=4 遊戲單位）→ 結果存 `_waterGrainSet`，懶加載 chunk 只做 `.Contains()` 查詢；**生成環**：移除 Y 軸「掃洞穴地板」邏輯，改為直接在 player.Y 採樣，敵人靠重力自然落地；按需 chunk 生成改為單 chunk 呼叫；build 0 errors 0 warnings |
| BodyH 可變化 + 遊戲單位語意標注 | `WorldScale.cs` / `PlayerController.cs` / `Main.cs` / `SpellCaster.cs` / `DroppedItemManager.cs` | `BodyH` 從 `const int` 改為 `public int { get; set; }`（預設仍 WorldScale.PlayerH），`MiningRange` 改 instance property；所有 gameplay 引用（渲染位置、採掘距離、拾取範圍、碰撞判定）改用 `_player.BodyH`；世界生成保留 `WorldScale.PlayerH`；`WorldScale.Grain` 加「遊戲單位」定義註解；`StepH = WorldScale.Grain / 8`；build 0 errors 0 warnings |
| 虛空破圖修正 + 生成距離改採掘單位 | `Chunk3D.cs` / `TileWorld3D.cs` / `MobSpawnController.cs` / `Main.cs` | **虛空破圖**：`SetTile`/`WriteCell`/`SetCellFromGpu` 在 `MarkDirty` 後呼叫 `MarkNeighborsMeshDirty`，相鄰 chunk 立即 `FlagMeshRebuild()`，徹底修復挖掘 chunk 邊界 tile 後鄰 chunk 不更新的問題；**生成距離**：`MobSpawnController` 常數改以 `WorldScale.Grain` 為單位（32/80/96/128 採掘單位 = 512/1280/1536/2048 tile），`EnsureChunkSync` delegate 注入 `MapGenerator3D`，`TryFindSpawnPos` 按需同步生成單一 chunk；build 0 errors 0 warnings |
| 非同步 chunk 生成 + 效能優化 | `MapGenerator3D.cs` / `EnemyManager.cs` / `MobSpawnController.cs` / `Main.cs` | **Direction B**：`GenerateChunkLazy` 的噪音計算移到背景 Task（`ComputeChunkFlat`），主執行緒每幀呼叫 `ApplyPendingChunks` 寫入 world；noise pool（主執行緒建立，ConcurrentBag 借還）確保 FastNoiseLite 不跨執行緒共享；pool 耗盡時 sync fallback；`EnsureChunkSync` 供 spawner 同步使用；**★☆☆**：`EnemyManager.DynamicActiveCount` 計數器取代每幀 LINQ Count；生成半徑 radius=12、MeshRadiusChunks=13；build 0 errors 0 warnings |
| W-6A 純資料層 | `Scripts/AbilitySystem/Data/ManaType.cs` / `ManaTypeRegistry.cs` / `ManaSlot.cs` / `SpellGroup.cs` | **ManaType**：record(Id/Key/DisplayName/RootGroup/IsComposite/SortOrder)；**ManaTypeRegistry**：18 種基礎 MP 靜態登記（修煉/支配/世界各 6），`Register` 供 W-13 追加複合類型；**ManaSlot**：Current/Max/RegenRate 獨立，`Tick(delta)` 回復 MP；**SpellGroup**：`SpellLoadout[5]` + ActiveGroupIndex + V 鍵切換接口；build 0 errors 0 warnings |
| W-6 實作計畫定稿 | `docs/plan-w6-implementation.md` | 決策 A–E 全數確認：技能因子（SpellSlot）等級 MP 綁定、走火入魔暫緩、ManaSlot 獨立 Max/Regen、儲能瓶暫緩、HUD 縱向排列；新增 SpellGroup（5 技能組）+ V 鍵切換設計；W-7→未來方向、W-8/9/10→待辦（W-10 為 W-6 後下一優先項）；PLAN.md 索引同步更新 |
| 地形特徵系統 + 地表水池地形 | `Scripts/World/Terrain/TerrainFeature.cs` / `SurfaceWaterPool.cs` / `MapGenerator3D.cs` | **TerrainFeature 架構**：抽象基底類別，`Initialize`（純計算佈局）/ `PlaceInWorld`（寫 tile，只在初始生成）/ `GetSurfaceOverride`（懶加載查詢）；新增地形只需繼承、加進 `_terrainFeatures` 清單；**SurfaceWaterPool**：rng 決定 4–8 個池子位置，radius=3–9 遊戲單位，depth=3–6 tile，挖圓形凹洞後底部填水；懶加載 chunk 透過 `GetSurfaceOverride` 取得 effectiveH + Water 覆寫；**移除 Flood Fill**：舊 `AddSurfaceWater`（Grain-scale BFS）/ `Adj4` / `_waterGrainSet` 全部移至 `Scripts/BackupCode.cs`；build 0 errors 0 warnings |
| ★★★ CA 效能優化：dirty HashSet + AABB 子區域 + SurfaceTool→ArrayMesh | `TileWorld3D.cs` / `TileWorldRenderer3D.cs` | **dirty HashSet**：`_dirtyChunks` HashSet 追蹤每幀有 `SetTile` 的 chunk，Tick 只掃這些 chunk（舊版全字典掃描）；**AABB 子區域**：每 chunk 只跑 `DirtyMin/Max ±1` 的子範圍（舊版跑全部 4096 tiles）；**SurfaceTool→ArrayMesh**：`MakeMesh` 改用 `AddSurfaceFromArrays` + C# 陣列隱式轉換，比 SurfaceTool 逐頂點快 2–5×；同批加入 `FlushNeighborDirty`、`SaveDirtyChunks`；build 0 errors 0 warnings |
| W-6D SpellGroup + V 鍵切換 | `Scripts/InputBindings.cs` / `Scripts/UI/AbilityEditorUI.cs` / `Scripts/Main.cs` | **AbilityEditorUI**：`Loadout` 改為委派 `SpellGroup.ActiveLoadout`；Header 加 5 個技能組圓點（點擊 → `SwitchEditorGroup`）；退出時若有 MP 種類超限顯示非阻斷警告；**Main**：`BuildGroupPanel` 建 V 鍵開關面板（5 個組選鈕，選完自動關閉）；V 鍵 `ToggleGroupPanel` 啟用游標；`SwitchActiveGroup` → 呼叫 `_editor.SwitchEditorGroup` + 更新 `_spellList.Loadout` + `UpdateActiveManaSlots`；**InputBindings**：加 `SpellGroupSwitch="spell_group_switch"` 預設 Key.V；build 0 errors 0 warnings |
| W-6C PlayerController + SpellCaster | `Scripts/World/PlayerController.cs` / `Scripts/AbilitySystem/SpellCaster.cs` | **PlayerController**：`Mp` 改為委派至 `ActiveManaSlots[0].Current`；加 `List<ManaSlot> ActiveManaSlots`、`GetManaSlot(key)`；constructor 初始化 gui_dao 槽；Tick 每幀同步 slot[0].Max/RegenRate 後呼叫 `slot.Tick(delta)`；Respawn 填滿所有槽；**SpellCaster**：新增 `TryConsumeMp(player, spell)` helper（有 ManaTypeKey → 均分扣對應槽，無 key → 回退舊邏輯扣 slot[0]）；TryCast + TriggerCombo 改呼叫 helper；舊存檔/舊技能（全 null key）行為不變；build 0 errors 0 warnings |
| W-6B SpellSlot + SpellArray 驗證 | `Scripts/AbilitySystem/Data/SpellSlot.cs` / `SpellArray.cs` | `SpellSlot.ManaTypeKey: string?`（null = 未指定）+ `HasAnyMpBlocks`（保守判斷：有圖騰即 true）；`SpellArray.MaxManaTypes = 3`（常數）/ `GetUsedManaTypes()` 遞迴收集 HashSet / `IsValidManaTypeCount()` / `HasUnboundMpBlocks()`（含容器效果遞迴）；純資料層，W-6D/E 再接入編輯器；build 0 errors 0 warnings |
| W-6A 純資料層 | `Scripts/AbilitySystem/Data/ManaType.cs` / `ManaTypeRegistry.cs` / `ManaSlot.cs` / `SpellGroup.cs` | `ManaType` record；`ManaTypeRegistry` 18 種基礎 MP（修煉/支配/世界各 6），`Register` 供 W-13 追加；`ManaSlot` Current/Max/RegenRate 獨立 + `Tick(delta)`；`SpellGroup` `SpellLoadout[5]` + ActiveGroupIndex；build 0 errors 0 warnings |
| 水池保底：出生點附近一定可見 | `Scripts/World/Terrain/SurfaceWaterPool.cs` | **根因**：池子隨機分佈在 3200×3200 世界，玩家出生在中心，機率分析顯示只有 ~15% 機會在出生點附近看到水池；**修法**：`Initialize` 第 0 號池固定在世界中心 ±64 tile 範圍內（radius 4–6 G = 64–96 tile），確保池邊緣在 160 tile 內，遠小於 mesh render radius 208 tile；其餘 N-1 個池維持隨機分佈；build 0 errors 0 warnings |
| W-6F 技能資料綁定角色存檔 | `CharacterSaveData.cs` / `SpellLoadout.cs` / `SaveSystem.cs` / `AbilityEditorUI.cs` / `Main.cs` | **根因**：`SaveSystem` 寫死 `user://loadout.json`（全域），新建/刪除角色不影響此檔案；**修法**：`CharacterSaveData` 加 `SpellGroupJson: string`；`SpellLoadout` 加 `ClearAll()`；`SaveSystem` 加 `SaveGroupToString(SpellGroup)` / `LoadGroupFromString(json, target, ...)` 字串序列化 API，涵蓋全 5 組；`AbilityEditorUI._Ready()` 不再讀檔，改由 Main.cs 呼叫 `InitSpells(json)` 注入；存檔改為 `EmitSignal SpellDataSaved` → `OnEditorSpellDataSaved` 寫入 `_charData.SpellGroupJson` 後呼叫 `FlowSaveSystem.SaveCharacter`；遊戲存檔路徑也同步 `GetSpellGroupJson()`；新角色 json="" → 全空白；build 0 errors 0 warnings |
| W-6D fix：技能組圓點移至 SpellListUI | `Scripts/UI/SpellListUI.cs` / `Scripts/Main.cs` | **SpellListUI**：加 `GroupSwitchRequested(int)` signal + `SpellGroup?` 屬性 + header 5 個技能組圓點（發 signal）+ `RefreshGroupDots()`；**Main.cs**：`_spellList.SpellGroup = _editor.SpellGroup`；訂閱 `GroupSwitchRequested → SwitchActiveGroup`；`SwitchActiveGroup` 移除內部 `ToggleGroupPanel`，改加 `RefreshGroupDots + Refresh`；V 鍵面板按鈕自行呼叫 `ToggleGroupPanel` 關閉；build 0 errors 0 warnings |
| W-6E HUD 動態 MP 條 | `Scripts/Main.cs` | 移除單一 `_mpLabel`，改用 `VBoxContainer _manaHudContainer`（AnchorBottom=1、GrowDirection.Begin，底邊 30px，向上堆疊）；`RebuildManaHud` 依 `ActiveManaSlots` 動態建 1–3 條橫式進度條（名稱縮寫 + 彩色填充條 + 數值標籤）；`RefreshManaHud` 每幀更新填充寬度與數值（ChangeDet 偵測 key 組成異動時自動 rebuild）；`UpdateActiveManaSlots` 結束後自動呼叫 rebuild；18 種基礎 MP 各有對應顏色；HP label 上移至 -92 以避免重疊；build 0 errors 0 warnings |
| 水池可見性 + 世界全水 + NPE 三修 | `SurfaceWaterPool.cs` / `MapGenerator3D.cs` / `Main.cs` / `TerrainFeature.cs` / `TileWorld3D.cs` | **水池可見性**：`TerrainFeature.Prepare(getHeight)` 虛方法計算 WaterSurface；`InitTerrainParams` 也呼叫 Prepare，修正存檔路徑；**世界全水**：① pool center 從 spawn±64 改為 spawn 外 radius+64 tiles（四向隨機），玩家不再出生在池內；② `Initialize+Prepare` 移到 `ComputeWorldCenterSpawn` 之前，spawn chunks 也能正確套用水池特徵；**NPE line 916**：`_Process` 開頭加 `if (_player == null) return;`，gameplay 啟動前不跑 HUD 更新；**玩家卡空中**：`GetTilePhysics` 注入 `HeightEstimator`，未加載 chunk 用地形高度估算實心/虛空；build 0 errors 0 warnings |
| 水池碗型 + 地面消失 bug 修正 | `SurfaceWaterPool.cs` / `Main.cs` | **水池**：深度 10–50 tile、拋物面碗型（中心最深）、平靜水面（WaterFill=0.7）；水下整欄填水、碗緣填 Dirt；`GetSurfaceOverride` 同邏輯；**地面**：`viewRadius: 5` 只重建玩家 5 chunks 內的 mesh，改為 `MeshRadiusChunks`（13），距離 6–12 的 chunk 現在也會重建 mesh；build 0 errors 0 warnings |
| CA 效能優化：元素反應限速 + 時間預算 | `TileWorld3D.cs` | **A**：GPU zone 內水格的 `CheckElementalCaReactions` 改為每 8 幀才呼叫一次（侵蝕速度 ÷8）；**B**：`foreach toProcess` 改為 `for` + Stopwatch，超過 4ms 就把剩餘 chunk 留到下幀，mesh rebuild 每幀有足夠主線程時間；build 0 errors 0 warnings |
| ★★☆ 效能優化：鄰居 dirty 去重 + 動態 mesh 預算 | `TileWorld3D.cs` / `Main.cs` | **FlushNeighborDirty**：`MarkNeighborsMeshDirty` 從立即呼叫 `FlagMeshRebuild()` 改為加入 `_pendingNeighborDirty HashSet`，每幀 `FlushNeighborDirty()` 統一觸發（去除同幀重複 flag）；**動態 maxPerFrame**：`RebuildDirtyMeshes` 的每幀 mesh 重建上限從固定 30 改為 `Clamp(30*(1/60)/dt, 5, 80)`，高幀率多建、低幀率少建，維持 16ms 時間預算；build 0 errors 0 warnings |
| 水算法 + 敵人生成三重修正 | `MapGenerator3D.cs` / `MobSpawnController.cs` / `Enemy.cs` | **水算法**：`AddSurfaceWater` BFS cutoff 方向錯誤（`h-BasinMaxDepth` 導致永不觸發，所有格都加入盆地 = 全地表淹水 + CA 卡頓）改為 `h+BasinMaxDepth`，只擴展到離最低點高度差 ≤ BasinMaxDepth 的格；**敵人三重 bug**：①生成距離 `32*Grain=512 tile` 超出 render radius（208 tile），敵人永遠不可見，改為直接 tile 單位（32-128 tile）；②`TryFindSpawnPos` 只生成 spawn chunk（Y=ty），未生成地表 chunk（Y=surfaceH），敵人用 `GetTile` 墜落（回傳 Air）→ 8 格位滿，加 `EnsureChunkAt` 生成 `surfaceH/S` chunk；③`ApplyGravity` 越界時無限落入虛空，加 `Position.Y+1 >= world.Height` → ForceDespawn；build 0 errors 0 warnings |
| 採掘 hover 高亮 | `TileWorldRenderer3D.cs` / `Main.cs` | `UpdateHighlightMesh` 在準心指向可挖掘格時重建半透明 overlay MeshInstance3D（白色 0.28α，僅曝光面，epsilon 偏移防 Z-fighting）；`ComputeHighlightTiles` 考量工具等級、採掘範圍、_perfectRemove PlacedUnit（整個 Unit 高亮）、形狀採掘；ChangDet 避免每幀重建；`DepthDrawMode.Disabled` + `RenderPriority=1`；build 0 errors 0 warnings |
| chunk 加載速度 + 水 + 敵人生成 Bug Fix | `Main.cs` / `MapGenerator3D.cs` / `MobSpawnController.cs` | **1 加載速度**：`ApplyPendingChunks(maxPerFrame: 4→16)`，背景 task 佇列 drain 速度提升 4x，近處 chunk 不再因積壓而落後遠處；**2 無水**：`AddSurfaceWater` 舊版只建初始 48×16 tile strip 的 `_waterGrainSet`；改為對全世界 200×200 grain cell 以 `GetHeightAt` 取樣跑 BFS，懶加載 chunk 查 set 有值 → 正確生水；`InitTerrainParams` 也呼叫（存檔世界重新進入）；**3 無敵人**：`TryFindSpawnPos` 舊版 `ty = player.Y`，跨地形生成點的地表 Y 不同 → 24 次嘗試全是實體，從不生成；改為 `GetTerrainY` delegate（注入 `_mapGen.GetHeightAt`）計算正確地表 Y；build 0 errors 0 warnings |
| 水 seed 條件修正 | `MapGenerator3D.cs` | `AddSurfaceWater` 種子條件由 `>= h`（找 local max = 高地）改為 `<= h`（找 local min = 視覺谷底）；`grainH` 大 = tile Y 大 = Godot Y 大 = 視覺凸起，water 從 local min 往上灌 `h+BasinMaxDepth`，不再覆蓋凸起地形；搭配前次 BFS cutoff = h+BasinMaxDepth；build 0 errors 0 warnings |
| ★★★ 7 HIGH bug 修正 + preflight-check-v2.ps1 | `SurfaceWaterPool.cs` / `EnemyManager.cs` / `Enemy.cs` / `SaveSystem.cs` / `CharacterSaveData.cs` / `Main.cs` | **G-2** WaterSurface≤0 守衛（原 <0 miss 了 =0 邊界，全世界淹水）；**P-1** Heavy 巡邏補雙格碰撞檢查（原僅查腳部，頭會穿牆）；**P-2** `EnemyManager.TypeAt(x,y)` 改 `GetTile(x,y,z)`（原 2D call，Z≠0 敵人永遠免疫火/岩漿）；**S-1** `DtoSlot` 補 `ManaTypeKey` 序列化（原缺失，W-6B MP 類型指定存讀檔後全失）；**S-2** `CharacterSaveData` 加 `ManaCurrents` dict，`SaveAll` 存所有 MP 槽、還原段逐 key 還原（原僅存/還 slot 0）；**S-3** 還原守衛改 `!IsFirstEnter`（原誤用 `InventorySlots.Length>0`，空背包玩家讀檔收到起始道具）；**死亡存檔**：`IsAlive ? Hp : MaxHp`，還原側 `Math.Max(Hp,1f)`（原存 Hp=0 → 讀檔後立刻再死）；build 0 errors 0 warnings；preflight-check-v2.ps1 新增 7 項自動檢查 |
| MED 10 項 bug 修正 | `MapGenerator3D.cs` / `TileWorld3D.cs` / `PlayerController.cs` / `Enemy.cs` / `EnemyManager.cs` / `MobSpawnController.cs` / `SaveSystem.cs` | **MED-2** `EnsureConnectivity` X 越界 clamp；**MED-4** `GetTilePhysics` 注釋錯誤修正；**MED-5+1** chunk 序列化改 4 bytes/cell（Type+Variant+TimerLo+Hi），老存檔向後相容；**MED-6** `GenerateChunkLazy`/`ComputeChunkFlat` 水池地下補水列（isWaterPool 旗標 + 懶加載路徑雙路修正）；**MED-7** `TryMoveDepth` 步階 Y-1 越界守衛；**MED-9** `TryMoveXZ` 補 X/Z 兩軸 1-tile 台階爬升（step-up）；**MED-10** `ApplyGravity` Heavy 補 2 格寬地板掃描；**MED-11** `ApplyExplosionDamage` 改 3D 球體距離（補 `dz` 分量）；**MED-12** `TryFindSpawnPos` 補地板實體確認（`ty+1` 不得是 Air）；**MED-14** `BlockFromDto` `TryParse` 失敗補 `GD.PushWarning`；build 0 errors 0 warnings |
| LOW 8 項 bug 修正 | `TileWorld3D.cs` / `Enemy.cs` / `SpellProjectile.cs` / `PlayerController.cs` / `SaveSystem.cs` / `CharacterSaveData.cs` / `Main.cs` | **LOW-1** Fire `fb` 變數修正（`z+(fb?0:0)` → `z+(fb?-1:1)`，Z 斜向擴散生效）；**LOW-2** `TryLoadChunk` 加 `NeedsSave=false`（防止 MarkDirty 在載入期觸發多餘重存）；**LOW-3** `ApplyGravity` 於 while 迴圈後補 `!IsAlive‖Y+1≥Height` 早退（防世界底邊 OOB）；**LOW-4** SpellProjectile `HitsEnemy` 靜態方法擴充 Heavy 2×2 碰撞盒；**LOW-5** `ApplyPhysics` 改 `Math.Min`（只限落速，JumpSpeed 不被 MaxFallSpeed 截斷）；**LOW-6** `SaveSystem.CleanupLegacy()` 刪舊 `loadout.json`，`InitSpells` 後自動呼叫；**LOW-8** 刪除 `CharacterSaveData.Ap` 死碼欄位（AP 由 level tier 派生，不需單獨存檔）；**LOW-9** 速度按鈕共用一個 `speedGroup` ButtonGroup（原各自 `new` 導致多選）；build 0 errors 0 warnings |
| ICreature 介面 + OccupiedByEntity 敵人封鎖 + 投射物起點 | `ICreature.cs` / `PlayerController.cs` / `Enemy.cs` / `Main.cs` / `SpellCaster.cs` | **1-ICreature**：新增介面（Id/Position/Hp/MaxHp/IsAlive/Aura），PlayerController 加 `int Id => -1`，Enemy/Player 均實作；**2-OccupiedByEntity**：敵人碰撞盒改與 `HitsEnemy` 一致（Heavy=2×2，其他=1×1，精確 Z 對齊），修正可在怪物身上放置物件的 bug；**3-投射物起點**：從玩家視覺 mesh 中心出發 `(playerX, playerY+halfH)` + 各軸偏移 halfBody+1 格，確保球體不在 opaque player box 內被遮擋；build 0 errors 0 warnings |
| 敵人生成 ty 公式修正 | `Scripts/World/MobSpawnController.cs` | MED-12 地板檢查暴露舊 bug：`ty = surfaceH - WorldScale.PlayerH`（-32 tile）讓 `ty+1` 永遠是 Air → 地板檢查全失敗 → 無敵人；改為 `ty = surfaceH - 1`（腳底緊貼地表），地板檢查正確通過；build 0 errors 0 warnings |
| AbilityEditorUI 三項改進 | `Scripts/UI/AbilityEditorUI.cs` | **1-摘要捲動**：`_descLabel` 改包在 `ScrollContainer`（`ExpandFill`，水平滾動停用），按鈕固定底部不再被壓縮；**2-未儲存提示**：加 `_isDirty` 旗標，名稱/積木/ParamChanged/刻印/ManaKey 異動即設 true，`TryExitEditor` 改呼叫 `DoExitEditor`，dirty=true 時先彈 `ConfirmationDialog`（儲存並離開 / 捨棄變更）；**3-MP 注入選單**：右側面板加 `_manaKeyRows` VBoxContainer，`RefreshManaKeys()` 按 Slot 動態建 OptionButton（18 種 + 未指定），`SyncSlotsFromBlocks` 保留既有 `ManaTypeKey`；`SaveSpell` 改回傳 `bool`；build 0 errors 0 warnings |
| preflight-check-v2.ps1 擴充 | `preflight-check-v2.ps1` | 新增章節 15（Spell W-6）/ 16（Enemy）/ 17（Terrain）共 21 項檢查；UTF-8 BOM 加入；PASS 29 / WARN 9 / FAIL 0 |
| ActivationType None + 傷害數字 3D + 水流 cross-chunk | `AbilityActivationType.cs` / `SpellArray.cs` / `SpellDescriptionGenerator.cs` / `Main.cs` / `TileWorld3D.cs` | **1-ActivationType None**：新增 `None=3` 值，`SpellArray` 預設改 `None`，描述生成器只在非 None 時顯示「即時型/宣告型」標籤；**2-傷害數字 3D**：`ActiveDmgNum.WorldPx (Vector2)` 改 `WorldPos3 (Vector3)`，`SpawnDmgNum` 正確計算 Z offset (`pos.Z * dT - OriginZ`)，投影時直接傳 3D 世界座標；**3-水流 cross-chunk**：`SetTile` 設為 Air 時呼叫 `MarkNeighborsCaDirty`，喚醒相鄰 chunk CA，修正挖空 chunk 邊界格後水不流動的問題；build 0 errors 0 warnings |
| 拖放技能因子修正 + Container 優先順序 | `Scripts/UI/AbilityEditorUI.cs` | `PaletteBlockDropped` else 分支（已有積木時）改追加至 `_spell.Blocks` 尾端並呼叫 `SyncCanvas`（原本建浮動腳本，導致第二個 Totem 不被計入 MP 消耗）；`SyncSlotsFromBlocks` Container 推斷改「投射>召喚>其他」優先順序，避免投射型 Totem 被前面的範圍型 Totem 覆蓋為 DirectCast；build 0 errors 0 warnings |
| UE5 遷移實作計畫 | `docs/plan-ue5-migration.md` `PLAN.md` | 撰寫 Godot→UE5 遷移計畫（M-0~M-10）；技術決策：RMC 渲染 / Compute Shader CA / Slate SGraphEditor / GameFeature Plugin；架構對照表；分階段依序 |
| 儲存驗證：主被動 + 發動類型 + MP 未綁定 | `Scripts/UI/AbilityEditorUI.cs` `Scripts/UI/SpellDescriptionGenerator.cs` | SaveSpell 加驗證：無技能因子阻擋/主動無 SetActivation* 阻擋/HasUnboundMpBlocks 阻擋；GenerateProse 移除「宣告型」錯誤 fallback |
| W-6E HUD MP 條啟動多欄修正 | `Scripts/Main.cs` | BuildManaHud 改呼叫 UpdateActiveManaSlots()（讀取技能組中已用 MP 類型再重建條）；OnEditorSpellDataSaved 也加呼叫，確保存技能後 HUD 立即更新 |
| 還原 PaletteBlockDropped 未授權機制 | `Scripts/UI/AbilityEditorUI.cs` | 畫布已有積木時拖入 Totem，`else` 分支改回 `SpawnPaletteScript(blocks, localPos)`；移除先前自動 append 主腳本 + SyncCanvas 的全對齊行為，讓積木停在拖放位置 |
| 敵人 AI 感知距離早退 | `Scripts/World/Enemy.cs` | `Update()` 最前端加水平距離早退（> DespawnSoftDist=192 tile 跳過 AI）；重力繼續執行防止遠端浮空 |
| act_fire_projectile 真實投射物 | `Scripts/AbilitySystem/SpellCaster.cs` `Scripts/Main.cs` | 移除 TODO-STUB；建立繼承來源刻印的迷你 SpellArray（命中執行 act_area_around）；SpellCaster 靜態 pending 佇列 + Main.cs 兩處 flush |
| 敵人攻擊 Y 座標修正 | `Scripts/World/Enemy.cs` | `Update()` 距離計算改用 `playerFeetY = player.Position.Y + BodyH - 1`（腳底格）對齊 `enemy.Position.Y`；原來用 HEAD tile 導致同一地面 Y 差 ≈31 格，Melee AttackRange=2 永遠不觸發 |
| ManaTypeKey OptionButton 重整修正 | `Scripts/UI/ScratchCanvas.cs` | `AddParams` 在 Totem 積木 `BuildUI` 後，對 row 內 OptionButton 補掛 `OnChanged()`；原本 ItemSelected 只更新 params 而不觸發 Changed，導致右側 MP 分解列表不刷新 |
| M-0 環境準備 | `.gitignore` `CLAUDE.md` `Source/` `Plugins/` | 新 repo 初始化、計畫文件複製、模組資料夾結構建立 |
| M-0 Build 設定 | `*.Build.cs` × 6、`*.uplugin` × 3 | 所有 Module 與 Plugin 設定檔 |
| M-1 核心型別 | `WorldScale.h` `GridPos.h` `MaterialType.h` | 世界常數 / FGridPos+hash / ETileCategory+FTileCell POD |
| M-1 積木系統 | `BlockType.h` `OpCode.h` `FBlockNode.h` | EBlockType(80) / EOpCode 全集 / TUniquePtr 樹（WithCopy=false）|
| M-1 MP 系統 | `ManaType.h` `ManaTypeRegistry.h/.cpp` | 18 種基礎 MP + 靜態登記表 |
| M-1 技能整構 | `SpellArray.h` | FSpellSlot / FSpellArray |
| Module 入口點 | `*Module.cpp` × 6 | 全 Module IMPLEMENT_MODULE，build 必要 |
| M-2 VM 型別預寫 | `Instruction.h` | FInstruction + 全部 Args structs（25 種） |
| M-3 世界模擬 | `MapGenerator3D.h/.cpp` `Chunk3D.h/.cpp` `TileWorld3D.h/.cpp` | CPU CA 世界：格狀地圖生成 / 3D Chunk / 全局 TileWorld 管理 |
| M-4 角色與 AI | `ASkillCreatorCharacter.h/.cpp` `AEnemy.h/.cpp` `AEnemyManager.h/.cpp` | 角色移動+屬性 / 敵人 AI 行為樹 / 敵人生成管理 |
| M-4 VM 執行層 | `USpellCaster.h/.cpp` `ExecutionContext.h` `SpellRunner` | SpellCompiler / SpellRunner（手動 Tick 累加器）/ ExecutionLoop |
| M-5 元素反應表 | `ElementType.h` `ElementalStatusEffect.h` `ElementalReactionTable.h/.cpp` | ESkillElementType（注意：EElementType 被 SlateCore 佔用）/ 5 種效果 / 22 條反應 |
| M-5 Aura 元件 | `UElementalAuraComponent.h/.cpp` | Apply/ApplyImmediate（帶/無冷卻）/ Process（每幀 tick）/ RecalcAggregates |
