# SkillCreator 實作進度

*最後更新：2026-06-23（**物品熱鍵欄外框不可見 Bug H-3 修復 + 法術熱鍵欄文字強制隱藏**：① `BuildItemHotbar()` 原呼叫 `SetBrushColor(0.10,0.10,0.15)` 設 `BrushColor`；每幀 `UpdateItemHotbar()` 又呼叫 `SetBrush({TintColor=(0.10,0.10,0.15)})`；Slate 渲染最終色 = `BrushColor × TintColor ≈ 0.01`（近黑）→ 移除 `BuildItemHotbar()` 裡的 `SetBrushColor` 呼叫，`BrushColor` 維持預設 White，最終色 = `White × TintColor = TintColor` ✓。② `HotBarBox`（法術槽位「[1] — ◄ [2] — [3]...」三行文字）設 `Collapsed` 隱藏，施法 U/I/O/P 偵測不受影響。⚠️ 純 `.cpp`，需 Rebuild。**此前：B 系列設計決策落地：敵人 stats 管線 + 力量公式**：① `AEnemy` 新增 `FCharacterStats Stats` + `TakePhysicalDamage`/`TakeEnergyDamage`（物理2步/能量4步，與玩家端完全一致；`BeginPlay` 依 Type 設 Stats 預設，Heavy 物防8/暴率2%，Ranged 暴率10%，Melee Power8）。② `ApplyCharacterSaveData` 補全 W-10 基礎點→Stats 映射（`BasePoint_* → ConstitutionPoints…LuckPoints`）+ 力量衍生計算（暫定 `Strength = StrengthPoints * 20`，無種族係數）。③ 0.5s 計時器維持不變。④ WantedLevel int32 確認。⑤ 健康值型別不急。⚠️ `AEnemy.h` 加了 `FCharacterStats`（UPROPERTY），需**關閉 Editor + 完整 Rebuild**（可與 B-1/B-2/B-3 同一次 Rebuild）。**此前：B-1~B-4 基本數值系統 + 浮空方塊 bug 修復**：① `GetHeightAt()` noise 參數（freq=0.005/4 octave/公式 0.4+v×0.30）與 `ComputeChunkData()` 實際地形（freq=0.001/7 octave/公式 0.30+v×0.08）完全不符（G-9/G-10 更新地形未同步），導致生成高度預測與實際地形落差大、大多數 Spawn 驗證失敗、偶爾通過的敵人在地形 mesh 尚未渲染處浮空後被 despawn；修正 `GetHeightAt()` 對齊 `ComputeChunkData()`。② B-1：`CharacterStats.h` 拆分防禦欄位（`BaseDefense`/`DamageReduction`→`PhysicalDefense`+`PhysicalDamageReduction`+`EnergyDefense`+`EnergyDamageReduction`+特定MP的`MpSpecificDefense/MpSpecificDamageReduction` TMap）、補齊攻擊欄位（`Strength`/`EnergyAtk`）、補齊抗暴/超暴欄位（`AntiCritDmgReduction`+`AntiSuperCritRate`+`AntiSuperCritDmgReduction`）、社會/法則/能量三項 stub（`Affection`/`BondPoints`/`WantedLevel`/`LawAffinity`×3/`MpAffinity`×3）、能力點 stub（`SkillAbilityPoints`/`ElementAbilityPoints`/`LawAbilityPoints`），共新增 ~30 欄位。③ B-2：`CharacterSaveData.h` 加 `CharacterAge`/`AppearanceAge`。④ B-3：`ASkillCreatorCharacter` 新增 `TakePhysicalDamage()`（物理 2 步公式＋暴擊/閃避判定）和 `TakeEnergyDamage()`（能量 4 步公式＋暴擊/閃避判定），`TakeDamage()` 暫改用 `PhysicalDefense`+`PhysicalDamageReduction`（B-1g）。⑤ B-4：移除 Tick 裡的 per-frame HP/MP regen，改用 0.5s `FTimerHandle RegenTimerHandle` 計時器（`TickRegen()`，rate 為 /秒，每跳 ×0.5f，對應 Godot CharacterStats.cs HP/秒/ManaSlot.cs 每秒回復量）。⑥ `UStatsWidget.cpp` 更新顯示字串（物防/能防/物減/能減）。⚠️ B-1/B-2/B-3 涉及 .h UPROPERTY 變更，需**關閉 Editor + 完整 Rebuild** 才能套用；B-5 HUD 生存條待 B 系列 Rebuild 後再做。**此前：SpellList E 鍵/點擊卡死 3 個 bug 修復**：① E 鍵按下無反應——`OnOpenEditor()`/`OpenBlockEditorForSlot()` 都用 `SetInputMode(FInputModeUIOnly())`，封鎖了所有 `InputComponent->BindKey` 事件（包含 E 鍵自身的 `OnOpenEditor` callback）；改成 `FInputModeGameAndUI()`（遊戲輸入+UMG 滑鼠事件同時有效）。② 「+」圓球點擊無反應——`OnAddSpellRequested` 未綁定；在 `OnOpenEditor()` 首次執行時 `BindLambda` 綁定：掃第一個空 active slot，關閉 SpellList，呼叫 `OpenBlockEditorForSlot`。③ DrawHUD 每幀呼叫 `SpellListPanel->RefreshSpellList()`（`ClearChildren()`+重建所有圓球 widget）移除——只在開啟面板和切換組別時呼叫。Build 0 錯誤 0 警告。此前累計：**採掘/熱鍵欄 3 個確定 bug 修復**：① 採掘完全無反應——根因是 Raycast 距離只有 10 tiles（62.5cm），遠小於 MiningRangeTiles（192 tiles = 12m），玩家按左鍵時射線根本打不到任何方塊，第一行 `!Hit.bHit` 就 return；同步修 Tick 高亮 raycast（也是 10 tiles，對應 Godot Main.cs:1012 使用 50 game-units 射線）。兩處都改成 `static_cast<float>(WorldScale::MiningRangeTiles)`。② 熱鍵欄 icon/數量/鍵位標籤位置設定全部失效——`UBorder` 是 ContentWidget（單子節點），`HotbarBorder->AddChild(Icon/Cnt/Key)` 後 `Slot` 型別是 `UBorderSlot` 不是 `UCanvasPanelSlot`，Cast 失敗、`SetOffsets` 從未執行；新增中間層 `UCanvasPanel`（`HotbarBorder` 唯一子節點，`UBorderSlot` 設 Fill）、Icon/Count/Key 改為加到 `SlotCanvas`，Cast `UCanvasPanelSlot` 正確生效。③ 熱鍵欄 active slot 缺金黃邊框——`SetBrushColor()` 只改背景色，對應 Godot `RefreshHotbar() BorderColor=(0.95,0.80,0.20)+BorderWidth=2` 完全缺失；改用 `SetBrush(FSlateBrush)` 帶 `ESlateBrushDrawType::RoundedBox` + `OutlineSettings`，active slot 套金黃邊框。純 .cpp 修改，Build 0 錯誤 0 警告。⚠️ 待 PIE 確認採掘高亮出現+左鍵採掘可破壞方塊、熱鍵欄 icon 位置/active 金邊正常。敵人 ApplyBodyColor 材質參數（E-1）和生成忙等阻塞（E-2）已知，非本輪優先。**USpellListWidget 完整對齊 Godot SpellListUI.cs**：前版是憑印象寫的簡化版（標題錯、固定 10 格 [空]、組別另起一行用 ○● 符號、缺提示文字、缺底部欄、方形按鈕）；本次逐行對照 Godot SpellListUI.cs 重寫——①標題改「技能創建空間」②新增 `USpellCircleWidget`（TFunction 模式，同 UFlowCardWidget）實作 260×260 全圓角按鈕（RoundedBox brush，CornerRadius=130，主動藍框/被動青框/超限灰框）③圓球列表動態建立：只渲染有技能的槽位，被動技能另渲染青框圓球，列尾加「+」新增圓球（對應 Godot RebuildCircles）④組別按鈕「1」~「5」移回標題列同一行，SetBackgroundColor 切換亮藍/灰（對應 Godot RefreshGroupDots）⑤右側補提示文字「點擊圓球編輯技能　E 關閉」⑥底部欄：「主動上限 10　被動上限 20」+暫時訊息 Label（ShowMsg，3 秒後消失）⑦Tooltip 改為 CanvasPanel 子 UBorder，NativeTick 追蹤滑鼠位置（對應 Godot _Process Tooltip 定位）。新增 `OnAddSpellRequested` delegate。Build 雙 target 0 錯誤 0 警告。⚠️ 待使用者 PIE 確認圓球外觀、Tooltip 跟隨滑鼠、底部訊息列。此前累計：**熱鍵欄/準心/敵人不出現 4 個真 bug 已修復**：① 熱鍵欄+準心完全不建立——`MainMap.umap` World Settings 的 `DefaultGameMode` 被存成 `BP_SkillCreatorGameMode_C`，覆寫了 `HUDClass`→`BP_SkillCreatorHUD_C`→`HUDWidgetClass`→舊版 `WBP_PlayerHUD_C`（只有 HpBar 等 4 個舊綁定元件），`UPlayerHUDWidget::NativeOnInitialized()` 偵測到 `HpBar` 非 null 就直接 `return`，從未呼叫 `BuildCrosshair()`/`BuildItemHotbar()`；World Settings 覆寫存在 `.umap` 二進位裡沒有安全文字修法，改在 `ASkillCreatorPlayerController` 覆寫 `SpawnDefaultHUD()` 強制忽略 GameMode CDO 的 HUDClass。② 敵人從未真正生成——`AMobSpawnController` 原本用 `TW->HeightEstimator()` 掃描「實際已載入的 tile」找地表，但生成點 32~128 tile 外幾乎必定未載入（永遠掃到 Air），改用 `FMapGenerator3D::GetHeightAt()` 純雜訊函數（對應 Godot `MobSpawnController.cs:124 GetTerrainY`），並排程+忙等同步生成該 chunk。③ 敵人完全無視覺——`AEnemy`（純 APawn）C++ 端從未建立任何 mesh 元件，`BP_Enemy` 子類也只覆寫了 AIControllerClass，對應 Godot 視覺其實是 `Main.cs:1716 CreateEnemyMesh()` 外部建立的純色 BoxMesh；新增 `AEnemy::MeshComp`（引擎 Cube）+`ApplyBodyColor()`（依 Type 上色）。雙 target Build 0 錯誤 0 警告。⚠️ 左鍵採掘無反應待使用者重新驗證——`OnMine()` 邏輯逐行比對 Godot 後結構正確，懷疑先前「沒反應」其實是被①的視覺缺失誤判，建議先確認準心/高亮恢復後再重測。此前累計：渲染管線 Lumen/RayTracing/VSM 跟 Intel 內顯不符造成嚴重卡頓已修復、2D 視角投影修正、法術熱鍵欄文字隱藏、積木編輯器 6 項 bug 全修、W-10 世界生成卡死、種族清單排序+漏 5 族、滑鼠上下反向、AR-B 材質全格子狀、E 鍵崩潰、M-10 GPU CA Phase 3 嚴重卡頓已暫停用。其餘歷史摘要見前次更新）*

*前次更新：2026-06-21（✅ GameFlowWidget 標題畫面不可見的 bug 已找到根因並修復並經使用者實機確認——`BuildLayout()` 原本掛在 `NativeConstruct()`，但 UMG 內部 `RebuildWidget()` 會先讀取 `WidgetTree->RootWidget`（此時還是 null，回退成空的 `SNew(SSpacer)`）再呼叫 `NativeConstruct()`，導致第一次 `AddToViewport()` 永遠拿到空畫面且不會再重建；改成在 `NativeOnInitialized()`（`CreateWidget()` 當下、`AddToViewport()` 之前就會跑）裡呼叫 `BuildLayout()`解決。系統性風險：專案裡其他依照 CLAUDE.md「在 NativeConstruct() 呼叫 BuildLayout()」慣例寫的 widget（Inventory/Equipment/Settings/ShapeMenu/SpellGroup/Stats/InputSettings/SpellList/DebugPaint/BlockEditor）已逐一稽核改用 `NativeOnInitialized()`）*

---

# ▼▼▼ UE5 遷移階段（2026-06-14 起） ▼▼▼

> 引擎：Unreal Engine 5.4+ / C++
> 遷移計畫詳見：[docs/plan-ue5-migration.md](docs/plan-ue5-migration.md)
> NPC Brain 計畫：[docs/plan-npc-brain.md](docs/plan-npc-brain.md)（含使用者手動步驟）

## UE5 目前狀態

M-0~M-9 ✅ · GameFlowWidget 完整復刻 Godot 5 畫面流程 ✅（含「標題畫面不可見」UMG 生命週期 bug 修復，已實機確認；專案內 16 個同病灶 widget 已全部稽核改用 `NativeOnInitialized()`）· **熱鍵欄/準心/敵人不出現 4 項真 bug 已修復（HUD class 被舊 BP 覆寫、敵人生成高度查未載入 chunk、敵人無視覺）✅，⚠️ 左鍵採掘待重新驗證** · **W-10 角色創建系統 11 步精靈架構完成（Build 通過，⚠️ 待實機 PIE 視覺驗證）** · Bug 掃描 HIGH×9/MED×14/LOW×13 全修 ✅ · 自動化測試覆蓋 ✅ · NPC Brain M-NPC-0~3 ✅ · GameFlowWidget ✅ · 全專案稽核 Round 1（14 項已修）✅ · 鍵位全面校正 ✅ · TryCastSlot 端到端打通 ✅ · F1/F2/F4 開發者工具 ✅ · 採掘形狀支援+右鍵放置 ✅ · WorldScale Grain=16 可擴縮架構 ✅ · issues.md Round 2 稽核 ✅ · Round 3 稽核 A~K 全覆蓋 ✅ · 🔴高優先 12 項修復 ✅ · 🟡中優先 B-2/3/4/8/11/13/17/C-5/F-3/8/10 ✅ · **G~K 稽核後 9 項追加修復：G-3a/b/9/10+H-5+I-11+J-3/11/12 ✅** · **⭐速查表第一層+第二層全 10 項 ✅** · **積木編輯器 UI 重做（SGraphEditor→runtime UMG）Phase 0~8 完成（9/10）**——Shipping build E 鍵可用 ✅，僅 Phase 9 清理舊架構待使用者同意（`plans/mutable-singing-nova.md`）· **GPU CA（M-10）Phase 0 spike 驗證通過 ✅**（RDG compute shader 首次跑通，Phase 1~4 待續，見 `docs/plan-m10-gpu-ca.md`）

## UE5 最新完成

> 歷史紀錄：[docs/history/ue5-milestones.md](docs/history/ue5-milestones.md)（M-5~M-9 里程碑、WorldScale、Widget 重設計、UI-2/3/5、S-1…等）

| 功能 | 關鍵檔案 | 摘要 |
|------|---------|------|
| Shift 游標模式切換 | `Source/SkillCreatorRuntime/Public/ASkillCreatorPlayerController.h` `Source/SkillCreatorRuntime/Private/ASkillCreatorPlayerController.cpp` | 遊戲中按 **LeftShift** 一鍵喚出系統游標，同時用 `SetIgnoreLookInput(true)` 凍結鏡頭旋轉（Look 的 `AddControllerYawInput/PitchInput` 受 `bIgnoreLookInput` 阻斷）；再按一次關閉游標並恢復鏡頭。新增 `bCursorMode` 成員記錄狀態，面板（積木編輯器/技能列表）關閉後 `SetShowMouseCursor(bCursorMode)` 恢復正確狀態（而非 hardcoded false）。⚠️ `.h` 加了非 UPROPERTY 成員，需 Rebuild。 |
| HUD 全面渲染修復 (Bug H-4) + 採掘高亮 + 格子地面根因 | `Source/SkillCreatorRuntime/Private/UPlayerHUDWidget.cpp` `Plugins/VoxelWorld/Source/VoxelWorld/Private/AVoxelWorldActor.cpp` | **Bug H-4 UBorder 渲染**：全專案所有 `SetBrushColor()` 呼叫改為 `SetBrush(MakeSolid({RoundedBox,TintColor=color}))`。根因：`UBorder::SetBrushColor` 設 `BrushColor`；Slate 最終色 = `BrushColor × Background.TintColor`；但 UE5.7 的 `T_DefaultDiffuse_D` ConstructorHelpers 失效，`Background.ResourceObject=null` + `DrawAs=Image` → 什麼都不渲染。`RoundedBox` 是純程式繪製，不需貼圖。影響範圍：準心、生存條 BarBg/Fill、XP 條 Bg/Fill、法力條 BarBg/Fill、死亡遮罩、浮動 Tooltip、物品欄 Icon 色塊。**Bug H-4 Fill resize**：`BuildSurvivalBars` / `BuildLevelHud` 的 Fill 原本加在 BarBg(UBorder)->AddChild()，Slot=UBorderSlot，Cast<UCanvasPanelSlot> 永遠 null → UpdateSurvival/UpdateLevelHUD 的 SetSize 從未執行。修復：Fill 直接加到 Root(UCanvasPanel)，同座標疊在 BarBg 上，Slot=UCanvasPanelSlot ✓。法力條用 USizeBox+UCanvasPanel+BarBg+Fill 結構（HorizontalBox 內無法直接放 Root）。ManaHudVBox Align 改為 `{0,1}`（pivot=底邊），修正 VBox 超出畫面下緣的問題。**採掘高亮**：`ShowHighlight()` 補 `DrawDebugBox(Cyan,0.05s,1.5px)`，確保在任何材質狀態下均可見。**格子地面根因**：`BeginPlay()` 開頭補 `if(!TileMaterialRegistry) LoadObject<>(…)` 保護，防止 Blueprint CDO 將 Registry 覆蓋為 null 後 fallback 到 VoxelMaterial（WorldGridMaterial）。⚠️ 純 `.cpp`，需關閉 Editor + Rebuild。 |
| 物品熱鍵欄外框不可見 (Bug H-3) + 法術熱鍵欄文字強制隱藏 | `Source/SkillCreatorRuntime/Private/UPlayerHUDWidget.cpp` | **Bug H-3**：`BuildItemHotbar()` 對外框 `UBorder` 呼叫 `SetBrushColor(dark)`，之後 `UpdateItemHotbar()` 每幀呼叫 `SetBrush({RoundedBox, TintColor=dark})`，Slate 最終色 = `BrushColor × TintColor ≈ 0.01`（近黑不可見）。修復：移除 `BuildItemHotbar()` 的 `SetBrushColor` 呼叫，`BrushColor` 維持預設 White，最終色 = `White × TintColor = TintColor` ✓。**法術熱鍵欄文字**：`HotBarBox`（`UpdateSpellHotBar` 格式「[1] — ◄ [2] — [3]...」三行文字）強制設 `Collapsed`，施法 U/I/O/P 偵測獨立不受影響。⚠️ 純 `.cpp` 修改，需關閉 Editor + Rebuild。 |
| B-1~B-4 基本數值系統 + 浮空方塊 bug 修復 | `Source/SkillCreatorCore/Public/CharacterStats.h`（B-1：大改）`Source/SkillCreatorRuntime/Public/CharacterSaveData.h`（B-2）`Source/SkillCreatorRuntime/Public/ASkillCreatorCharacter.h`（B-3 API）`Source/SkillCreatorRuntime/Private/ASkillCreatorCharacter.cpp`（B-1g/B-3/B-4）`Plugins/VoxelWorld/Source/VoxelWorld/Private/MapGenerator3D.cpp`（GetHeightAt bug）`Source/SkillCreatorRuntime/Private/UStatsWidget.cpp` | ① GetHeightAt noise 參數對齊 ComputeChunkData（freq/octave/公式全改），修復敵人浮空/生成失敗。② B-1 拆分物理/能量防禦欄位，新增 Strength/EnergyAtk/抗超暴/社會-法則-能量三項 stub/能力點 stub（~30 個新 UPROPERTY）。③ B-2 CharacterSaveData 加年齡欄位。④ B-3 TakePhysicalDamage/TakeEnergyDamage 完整 4/2 步公式+暴擊/閃避。⑤ B-4 HP/MP regen 改為 0.5s FTimerHandle（rate 為 /秒，每跳 ×0.5f）。⚠️ 需關閉 Editor + 完整 Rebuild。B-5 HUD 生存條待後續。 |
| SpellList E 鍵/點擊卡死 3 個 bug 修復 | `Source/SkillCreatorRuntime/Private/ASkillCreatorPlayerController.cpp`（UIOnly→GameAndUI；OnAddSpellRequested BindLambda）`Source/SkillCreatorRuntime/Private/ASkillCreatorHUD.cpp`（DrawHUD 移除每幀 RefreshSpellList） | ① `FInputModeUIOnly()` 封鎖所有 legacy BindKey 事件（含 E 鍵 OnOpenEditor callback）→ 改 `FInputModeGameAndUI()`，兩處（SpellList 開啟、BlockEditor 開啟）都修。② `OnAddSpellRequested` delegate 未綁定 → `+` 圓球點擊空轉；在 OnOpenEditor 首次綁定時 `BindLambda`：掃第一個空 active slot 並呼叫 `OpenBlockEditorForSlot`（對應 Godot Main.cs:1801-1820 OnListAddSpellClicked）。③ DrawHUD 每幀 `RefreshSpellList()`（`ClearChildren`+重建全部圓球 widget）→ 移除，只在開啟面板/切換組別時刷新。Build 0 錯誤 0 警告。 |
| 採掘無反應 + 熱鍵欄佈局/邊框 3 個確定 bug 修復 | `Source/SkillCreatorRuntime/Private/ASkillCreatorCharacter.cpp`（採掘+高亮 raycast 距離）`Source/SkillCreatorRuntime/Private/UPlayerHUDWidget.cpp`（熱鍵欄 inner canvas + active 邊框） | ① 採掘完全無反應：Raycast 10 tiles = 62.5cm，射線根本打不到方塊（MiningRangeTiles=192 tiles=12m）；兩處改成 `static_cast<float>(WorldScale::MiningRangeTiles)`（OnMine + Tick highlight）。② 熱鍵欄 icon/數量/鍵位標籤位置全部失效：`UBorder` 單子節點、`AddChild` 後 Slot 是 `UBorderSlot` 不是 `UCanvasPanelSlot`，Cast 失敗 → `SetOffsets` 從未執行；新增中間層 `UCanvasPanel`（HotbarBorder 唯一子節點），Icon/Count/Key 改加到內層 canvas。③ active slot 缺金黃邊框（Godot BorderColor=0.95,0.80,0.20+Width=2）：改用 `SetBrush(FSlateBrush)` 含 `RoundedBox OutlineSettings`。Build 0 錯誤 0 警告。⚠️ 待 PIE 確認。 |
| USpellListWidget 重寫對齊 Godot SpellListUI.cs | `Source/SkillCreatorRuntime/Public/USpellCircleWidget.h`（新增）`Source/SkillCreatorRuntime/Private/USpellCircleWidget.cpp`（新增）`Source/SkillCreatorRuntime/Public/USpellListWidget.h`（全改）`Source/SkillCreatorRuntime/Private/USpellListWidget.cpp`（全改） | 原版完全憑記憶寫，與 Godot SpellListUI.cs 有 6 個主要差異（標題錯/固定 10 格/組別另起一行用符號/缺提示文字/缺底部欄/方形按鈕）。逐行對照 SpellListUI.cs(454 行)重寫：① 標題改「技能創建空間」(22px)；② 新增 `USpellCircleWidget` 260×260 全圓角按鈕（`ESlateBrushDrawType::RoundedBox`+`CornerRadius=130`，對應 Godot `CircleStyle(cs:438-446)`，主動藍框/被動青框/超限灰框/hover 淡色各對應 Godot 色值）；③ 圓球列表動態建立（`RebuildCircles()`）：只渲染有技能的槽位、被動後渲染、列尾加「+」新增圓球（對應 Godot cs:249-297）；④ 組別按鈕「1」~「5」移回標題列同一行（對應 Godot header HBox cs:94-111）；⑤ 提示文字「點擊圓球編輯技能　E 關閉」(13px 灰，對應 Godot cs:115-119)；⑥ 底部欄（對應 Godot cs:166-200）：「主動上限 10　被動上限 20」+暫時訊息 MsgLabel（黃色，`ShowMsg()` 後 3 秒消失）；⑦ Tooltip 從硬字串改為 `CanvasPanel` 子 `UBorder`，`NativeTick()` 追蹤滑鼠偏移 (14,14) 更新 `UCanvasPanelSlot::SetPosition()`（對應 Godot `_Process` cs:341-358）；新增 `OnAddSpellRequested` delegate。TFunction 模式同 `UFlowCardWidget`（dynamic delegate 不支援 AddLambda，UFUNCTION 中繼→TFunction）。Build 0 錯誤 0 警告（2 次修正：`FVector4f`→`FVector4` UE5.7 double 精度；`SetScrollbarVisibility`→`SetScrollBarVisibility`）。⚠️ 待使用者 PIE 確認圓球外觀/Tooltip 跟滑鼠/底部訊息列。 |
| 修復熱鍵欄/準心/敵人不出現 4 個真 bug（HUD class 被舊 Blueprint 覆寫、敵人生成高度查未載入 chunk、敵人完全無視覺） | `ASkillCreatorPlayerController.h/.cpp`（新增 `SpawnDefaultHUD()` override）`AMobSpawnController.cpp`（改用 `GetHeightAt` 純函數+同步排程 chunk）`AEnemy.h/.cpp`（新增 `MeshComp`+`ApplyBodyColor()`）`AEnemyManager.cpp`（Spawn 後補呼叫 `ApplyBodyColor()`） | 使用者回報熱鍵欄/準心/可挖掘提示/採掘/敵人全部消失，逐一查出 4 個獨立根因：①**熱鍵欄+準心完全不建立**——`Content/MainMap.umap` World Settings 的 `DefaultGameMode` 被存成 `BP_SkillCreatorGameMode_C`（覆寫 `GlobalDefaultGameMode`），其 `HUDClass` 又被覆寫成 `BP_SkillCreatorHUD_C`，`HUDWidgetClass` 又指到舊版 `WBP_PlayerHUD_C`（用 PowerShell 讀 uasset 二進位字串確認只有 `HpBar`/`MpBar`/`StaminaBar`/`MoodBar`/`HotBarBox` 幾個綁定元件，是 C++ 純建構 HUD 出現前的舊設計）；`UPlayerHUDWidget::NativeOnInitialized()`（`UPlayerHUDWidget.cpp:118-130`）偵測到 `HpBar` 非 null 就走「Blueprint 已綁定」分支，Pin 完位置直接 `return`，永遠不會呼叫 `BuildCrosshair()`/`BuildItemHotbar()`。World Settings 的 GameMode 覆寫序列化在 `.umap` 二進位裡，沒有安全的純文字/C++方式改回去，改在 `ASkillCreatorPlayerController` 覆寫 `SpawnDefaultHUD()`（`PlayerController.h:2002`），不呼叫 `Super::`，直接無條件 `SpawnActor<AHUD>(ASkillCreatorHUD::StaticClass())`，繞過 GameMode CDO 上不管哪個 Blueprint 子類設的 `HUDClass`。②**敵人從未真正生成**——對照 Godot `MobSpawnController.cs:124` `GetTerrainY?.Invoke(tx,tz)`→`Main.cs:413` `spawner.GetTerrainY=(wx,wz)=>_mapGen.GetHeightAt(wx,wz)`（純雜訊函數，不依賴 chunk 是否載入），UE5 `AMobSpawnController::TryFindSpawnPos` 原本用 `TW->HeightEstimator(TX,TZ)` 掃描「實際已載入的 tile 資料」找地表——但生成點距玩家 32~128 tile 外幾乎必定還沒被 streaming 載入（`GetTile` 對未載入 chunk 一律回傳 Air），導致 `HeightEstimator` 永遠掃到 `Height-1`、後續 `SurfaceY>=H-1` 檢查必定失敗、24 次嘗試全部 `continue`，`TryFindSpawnPos` 永遠回傳 false。改用 `AVoxelWorldActor::GetMapGenerator().GetHeightAt()`（`MapGenerator3D.cpp:31-42`，同樣是純 FastNoiseLite 函數，跟 Godot 對應）算地表高度，並在找到候選點後用 `EnsureChunksAround`+忙等 `ApplyPendingChunks`（比照 `FFlowSaveSystem::PregenerateSpawnArea` 既有的同步排程模式）確保該 chunk 真的有地形資料可供站立判定，而非只是空 chunk。③**敵人即使生成位置正確也完全不可見**——`AEnemy`（純 `APawn` 子類）C++ constructor 從未建立任何網格元件，`Content/BP_Enemy.uasset` 子類也只覆寫了 `AIControllerClass`（PowerShell 讀字串確認 SCS 只有一個 `DefaultSceneRoot`，無 mesh），對應 Godot 視覺其實是 `Main.cs:1716 CreateEnemyMesh()` 外部建立的純色 `BoxMesh`（per-type 顏色，`SyncEnemyMeshes()` 每幀同步位置），`Enemy.cs` 本體完全沒有視覺元件，UE5 卻把 `AEnemy` 做成有自己 Transform 的 `APawn`，沒有對應補上視覺。比照 `ASkillCreatorCharacter`/`AVoxelWorldActor::HighlightMesh` 既有模式，在 `AEnemy` constructor 新增 `UStaticMeshComponent`（引擎 Cube + `GrainCurrent×TileSizeCm` 縮放，相對位置往上半個單位對齊腳底）當 RootComponent，新增 `ApplyBodyColor()`（依 `Type` 設 `UMaterialInstanceDynamic` 顏色，對應 Godot `CreateEnemyMesh` 的 4 色表），`BeginPlay()`+`AEnemyManager::Spawn()`（`Type` 在 `SpawnActor` 後才設定，`BeginPlay` 已跑完預設色，需再呼叫一次）都呼叫一次。雙 target Rebuild 0 錯誤 0 警告。⚠️ **左鍵採掘無反應**仍待使用者實機驗證：`OnMine()`（`ASkillCreatorCharacter.cpp:818-940`）邏輯逐行比對 Godot `PlayerController.cs:42` `MiningRange`/`Main.cs:1197-1287` 後結構正確（範圍/形狀/工具判定/完美移除分流皆對齊），Enhanced Input 綁定（`IA_Mine`→`EKeys::LeftMouseButton`）與其他確認可用的按鍵（WASD/跳躍）走同一套機制，純讀程式碼找不到反向 bug；由於上述 HUD class 被整個替換掉，採掘/準心提示的「沒有視覺回饋」可能讓使用者誤判「沒有反應」，建議先確認本次 HUD 修復後準心/高亮是否正常出現，再重新測試左鍵採掘是否依然無效。 |
| UInventoryWidget hover tooltip | `Source/SkillCreatorRuntime/Private/UInventoryWidget.cpp` | `NativeTick` 新增每幀 `GetSlotUnderMouse()` 偵測：命中非空格時 `ShowFloatTooltip(FItemRegistry::Get(ItemId).DisplayName, MousePos)`，離開時 `HideFloatTooltip()`。對應 Godot Main.cs:2589-2594 `slot.MouseEntered/MouseExited → ShowFloatTooltip(DisplayName)`。純 .cpp 修改（無 .h 變更），Live Coding 安全。物品欄→熱鍵欄拖曳確認已正常（熱鍵欄為物品欄前 10 格，`OnSlotSwapRequested` 已接通 `SwapSlots`）。同步修復 SetOffsetsInViewport API 不存在編譯錯誤（`ASkillCreatorGameMode.cpp`+`ASkillCreatorPlayerController.cpp` 各移除一行）。Build 0 錯誤 0 警告（UE5.7）。 |
| W-10｜角色創建系統 11 步精靈（架構 Phase A~E 全完成，PIE 實測修了 4 個真 bug） | `UPlayerCreateWidget.h/.cpp`（新增）`UStatAllocatorWidget.h/.cpp`（新增）`RaceRegistry.h/.cpp`（新增，SkillCreatorCore；2026-06-22 重排序+補種族）`UFlowCardWidget.h/.cpp`（加 bShowDeleteButton 參數）`CharacterSaveData.h`（加 RaceId+7 項 BasePoint_*）`FlowSaveSystem.h/.cpp`（加 CreateNewCharacter overload）`UGameFlowWidget.h/.cpp`（移除舊 CharCreateScreen，串接新精靈；2026-06-22 修世界生成卡死 bug + 補進度條）`ASkillCreatorCharacter.cpp`（2026-06-22 修滑鼠上下反向） | 對應使用者規格 `origin text setting/about w - 10 playercreate.txt` + 實作計畫 `docs/plan-w10-character-creation.md`。獨立 `UPlayerCreateWidget` 取代原本一頁式「輸入名字+確認」：`TArray<FPlayerCreateStep>` 步驟陣列機制（每步驟只認索引，不認內容，方便日後增減/調整順序），11 步全部實裝——① 名字輸入、② 創建能力點 RandRange(50,100)+設最高 100、③ 種族選擇（`FRaceRegistry` 收錄 16 體系/164 種，讀完 `Import word setting/ExportBlock-game-word/` 全部 16 份體系文件整理而成；神界種族判定為飛升後狀態、不放入創建清單；體系泡泡用 `UWrapBox`+`UFlowCardWidget`(bShowDeleteButton=false) 取代真實散落視覺，種族清單同樣重用 `UFlowCardWidget`）、④/⑤ 外貌/喜好空白流程、⑥ 基礎能力點分配（`UStatAllocatorWidget`，−/數字輸入框/+ 控制項，仿「人生重開模擬器」）、⑦ 負面效果空白流程但保留驗證（創建能力點<0 跳警告彈窗擋下一步）、⑧/⑨/⑩ 靈魂萬象/體質/心靈眾影響空白流程、⑪ 最終資訊卡（4 個可編輯項目，點擊「編輯」用 `ReturnToStepIndex` 機制跳回對應步驟調整，調整完按該步驟「確認」鈕直接跳回資訊卡，不重跑中間步驟）。右上角「快速建立」可一鍵隨機跳過 1~10 直達 11。

**2026-06-22 第二次 PIE 實測再抓到 2 個真 bug，都已修復**：
④ **種族清單排序錯亂、人類人體系漏了非黃階種族**——使用者指出「新增的種族被排在後面」、「類人體系應該有非黃階種族」。逐一重新打開全部 16 份體系總覽來源文件直接核對（不再相信計畫文件的二手摘要——摘要本身就寫錯「人、類人體系（黃階，全部有現成敘述）」，這句話就是錯的），發現：(a) 上一輪新增的 ~40 種族全部用 Edit 加在每個體系清單最後面，沒有照文件原始順序插入正確位置；(b) 人、類人體系文件其實有玄階（瓦爾基利、龍人）跟地階（不滅族、古蘭族、阿修羅）共 5 族，文件裡是完整內文敘述、不是連結，上一輪完全沒讀到。`RaceRegistry.cpp` 整份重寫，所有 `R()` 呼叫改成跟文件同階層內出現順序一致，補上這 5 族；`RacesInSystem()` 走 `TMap` 插入順序疊代（無 remove，順序穩定），UI 端也沒有額外排序，確認改了 `Init()` 呼叫順序就會反映到畫面。
⑤ **滑鼠上下反向**——`ASkillCreatorCharacter.cpp` 的 `IA_Look` 對 `EKeys::MouseY` 的 Enhanced Input 映射只加了 `UInputModifierSwizzleAxis`，沒加 `UInputModifierNegate`。引擎原始碼 `PlayerController.h:1567` doc comment 寫明 `AddPitchInput` 正值＝「look up」，但 `EKeys::MouseY` 原始滑鼠往上移動時數值是負的（螢幕座標 Y 向下為正），沒 negate 就會變成滑鼠往上、鏡頭往下——照同檔案內 `S` 鍵映射已經在用的 Swizzle+Negate 組合模式補上。

**2026-06-22 使用者實機 PIE 測試抓到 2 個真 bug，都已修復**：
① **世界創建永遠卡在「正在生成世界」畫面，進不去遊戲**——根因是 `UGameFlowWidget::StartWorldGeneration()` 用 `FTimerManager::SetTimer()` 輪詢世界生成進度，但 `UWorld::Tick()`（`Engine/Private/LevelTick.cpp:1783-1788`）裡 `GetTimerManager().Tick()` 被 `if (... && !bIsPaused)` 包住——GameFlowWidget 顯示主選單流程時遊戲本來就是暫停的（`SkillCreatorGameMode: ...game paused`），所以這個 Timer 從頭到尾沒被觸發過，世界生成永遠卡在第一句文字（連「X/Y 區塊」進度都沒更新過）。改成用 `UGameFlowWidget::NativeTick()`（UMG widget 自己的 tick，確認沒有任何暫停閘門）驅動 `PollWorldGeneration()`，移除 `WorldGenPollTimer`/`FTimerManager` 依賴。
② **缺進度條**——「請稍候」文字底下原本只有純文字，沒有視覺進度條；新增 `UProgressBar`（`SizeBox` 固定 420×18 寬度，否則在 VerticalBox 裡會被壓成細線），`PollWorldGeneration()` 同步更新百分比。
③ **基礎能力點介面置中放大 + 數字改可輸入**：`BuildStepBaseStats()` 用 `SizeBox`(寬 420)+`Overlay`(置中) 包住整個 Step 6 內容（外層 `ShowStep()` 對所有步驟統一給 `HAlign_Fill`，不能單獨改，所以在自己回傳的 Widget 內層再包一層置中容器）；`UStatAllocatorWidget` 的數字欄位從 `UTextBlock`（純顯示）改成 `UEditableTextBox`，可以直接打字輸入，`OnTextCommitted` 解析後呼叫新增的 `UPlayerCreateWidget::SetStatValue()`，clamp 到 `[0, 目前值+剩餘點數]` 合法範圍；按鈕/字體/欄位尺寸全部加大（22px 字、44×44 按鈕）。

`UPlayerCreateWidget`/`UStatAllocatorWidget`/`UGameFlowWidget` 都用 `NativeOnInitialized()` 建 WidgetTree（沿用本次 session 剛修好的慣例，沒有重蹈 NativeConstruct 覆轍）。三個 `.h` 都改了（新增 UPROPERTY/虛擬函式宣告），確認關閉 Editor 後完整 Rebuild，雙 target 皆 0 錯誤 0 警告。**⚠️ 仍待使用者重新進 PIE 確認**：世界生成是否真的能順利跑完進遊戲、進度條是否正常跑動、能力點輸入框打字是否符合預期。 |
| AR-B｜世界材質「全格子狀」根因修復：補建 DA_TileMaterialRegistry + 16 個純色材質 | `Content/Data/DA_TileMaterialRegistry.uasset`（新增）`Content/VoxelMaterials/M_Tile_*.uasset`×16（新增） | 使用者回報「世界還是有明顯的格子狀」，一開始以為是 voxel 方塊邊角的預期外觀，追下去才發現：`AVoxelWorldActor.cpp:116-121` 找材質時 `TileMaterialRegistry` 是 null（因為 `/Game/Data/DA_TileMaterialRegistry` 這個資料資產從來沒被建立過，PIE log 一直有 `Failed to find /Game/Data/DA_TileMaterialRegistry` 警告，先前沒當作優先問題處理），導致**全部 17 種 EMaterialType 都 fallback 到同一個 `M_Voxel.uasset`**——整個世界不管哪種方塊，材質都一樣。用 Python 檢查確認 `M_Voxel` 是零參數的基礎 `Material`（`unreal.MaterialEditingLibrary.get_*_parameter_names()` 三項全空），且 `GreedyMesher.cpp:151-153` 早就把 MaterialID/Variant 寫進 VertexColor 等材質端讀取，只是從來沒有任何材質資產被建立——這部分屬「建立 Material 等純美術資產」，照 CLAUDE.md 規則需要使用者決定，問過使用者後選擇「先程式化純色頂上」這個選項。寫一次性 Python script（`unreal.MaterialEditingLibrary` + `AssetTools`），讀 `MaterialRegistry.cpp` 裡既有的 `GMatColors[]` 17 色資料，逐一建立 `M_Tile_<Name>` 材質（`Constant3Vector`→`BaseColor`），再建立並填好 `DA_TileMaterialRegistry.Entries[]`（index 0 Air 留空，1-16 各自指向對應材質），跑完後用第二個腳本讀回驗證全部 17 格內容正確。**之後若要換成真貼圖材質，直接告訴 Claude 要把哪個 EMaterialType 換成哪個資產路徑即可，用同一套 Python 腳本改 `Entries[]` 對應格，不需要手動開 Editor**。⚠️ 半透明材質（Water/Lava/Fire/Steam/Leaves/MagicCrystal）目前仍是 Opaque blend mode，純色但不透明，之後若要做真正透明效果需額外處理。一次性腳本跑完後已刪除，不留在專案裡。⚠️ 仍待使用者重新進 PIE 確認方塊顏色是否正確區分。 |
| M-10｜GPU CA Phase 0~3 完成但⚠️ Phase 3 暫時停用（PIE 實測嚴重卡頓） | `Plugins/VoxelWorld/Source/VoxelWorldShaders/`（新模組）`Plugins/VoxelWorld/Shaders/Private/CaSpike.usf`/`CaMargolus.usf`（新增）`VoxelWorld/Public/CaCellPacking.h`/`CaGpuSimulator.h`/`.cpp`/`TileWorld3D.h`/`.cpp`（改簽名/新增邏輯）`AVoxelWorldActor.cpp`（`BeginPlay()` 的 `InitGpu()` 呼叫 2026-06-22 註解掉）`M10MargolusTest.h/.cpp`/`M10GpuSimulatorTest.h/.cpp`（新增）`VoxelWorld.uplugin`/`VoxelWorld.Build.cs`（新增模組相依） | 對應 `docs/plan-m10-gpu-ca.md`，逐行讀過 Godot `CaGpuSimulator.cs`（469 行）後動手。**Phase 0**（RDG 管線驗證）+ **Phase 1**（Margolus shader 邏輯 port）+ **Phase 2**（`FCaGpuSimulator` 正式實作，Upload→Simulate→Download 三次分開呼叫間用 `TRefCountPtr<FRDGPooledBuffer>` 持久化 GPU buffer）+ **Phase 3**（串接 `Tick()`）程式碼全部完成，自動化測試（4×4×4 小 zone）全部 `Result={Success}`，過程撞到 8 個坑全部記錄在計畫文件。

**2026-06-22 PIE 實機測試發現嚴重卡頓，已停用 Phase 3**：根因是 `TileWorld3D::TickGpuZone()` 每個 tick 都對整個 GPU zone（`ZoneW×ZoneH×ZoneD` = 128×256×128 = **419 萬格**）做一次完整 `GetCell()` 掃描＋打包＋GPU Upload→Simulate→Download（含同步 `FlushRenderingCommands()` GPU stall）——這個熱路徑從 Phase 0~3 開始就只用 4×4×4（64 格）小 zone 測過正確性，從未在真實尺度量過效能，**這正是原計畫把「效能驗證」獨立列成 Phase 4 的原因（先求對再求快），但這次在 `BeginPlay()` 呼叫 `InitGpu()` 讓它在 Phase 4 完成前就提前上線了**。暫時把 `AVoxelWorldActor::BeginPlay()` 的 `TileWorld.InitGpu()` 呼叫註解掉，讓 `GpuSim.IsAvailable()` 維持 false，`Tick()` 整段跳過 GPU 路徑、照舊全 CPU 模擬（跟 Phase 3 之前一樣安全，純停用不影響任何功能）。Phase 0~3 程式碼全部保留原地，之後排進開發順序時，Phase 4 至少要做：① 不要每 tick 全量重建上傳資料（只在 zone 真的移動或內容真的變了才重建）、② GPU buffer 跨 tick 持續留在 GPU、③ 下載端只讀真正有 dirty bit 的格子。下一步：Phase 4（效能優化，目前無排期）。 |
| 修復 E 鍵開技能編輯器必崩潰（`AddDynamic` 巨集誤用） | `Source/SkillCreatorRuntime/Private/UBlockEditorWidget.cpp` | 使用者按 E 開 `UBlockEditorWidget` 直接崩潰，崩潰訊息：`Assertion failed: Result && Result[2] != '0'`、`'DotHandlers[i]' does not look like a member function`（`Delegate.h:474`）。根因：`AddDynamic` 是巨集，會把第二個參數**原樣字串化**去查 UFunction 名稱（不是執行期接受函式指標變數），程式碼裡用 `void (UBlockEditorWidget::*DotHandlers[N])() = {...}` 陣列、再迴圈 `Dot->OnClicked.AddDynamic(this, DotHandlers[i])`——巨集字串化後變成字面文字 `"DotHandlers[i]"`，引擎驗證函式名稱格式時直接 assert 崩潰。全專案搜尋同一個模式（`AddDynamic(this, \w+\[`）找到 3 處，都在這個檔案：① `BuildHeader()` 的 5 顆技能組切換 dot、② `BuildPalette()` 的 3 個主分頁、③ `RebuildSubTabColumn()` 的 11 個子分頁——全部改成迴圈內 `switch(i)` + 逐個字面 `&UBlockEditorWidget::OnXxxClicked` 呼叫，移除陣列變數轉發。純 .cpp 修改，Build 0 錯誤 0 警告。 |

## ⚠️ 稽核後續待修（已全部解決 ✅，留存歷史紀錄）

> Round 1 已修 14 項。以下 3 項於本輪（2026-06-19）全部解決：

1. ~~積木編輯器「套用到技能槽」按鈕缺失~~ ✅ `SBlockEditorWidget` 新增「儲存技能整構」按鈕，`OnBlockEditorSave` 寫入 `SpellGroups.GetActiveLoadout().Slots[i]`。
2. ~~FSpellArray.Blocks JSON 持久化缺失~~ ✅ 新增 `FBlockNodeSaveData`/`FBlockTreeSaveData`（攤平 index pool）+ `FSpellArray.BlocksJson` UPROPERTY + `SpellSaveSystem` 序列化邏輯。
3. ~~InvokeSpell 連段完整修復~~ ✅ Blocks 現在會被寫入，`OnBuildComboContext`/`TryCastSlot` 的 `Compile()` 不再拿到空陣列。

⚠️ 新發現的限制（非本輪引入，但本次工作中確認）：積木編輯器 `ToggleBlockEditorOverlay` 整段在 `#if WITH_EDITOR`（依賴 GraphEditor module），**只能在 Editor/PIE 開啟，封裝後的 Shipping build 完全無法編輯技能**。修正需把 SGraphEditor 換成 runtime UMG 方案，列入後續待辦。

## UE5 待辦

- [x] **M-0** 環境準備（repo、.gitignore、模組資料夾）
- [x] **M-1** 核心資料層（所有標頭檔）
- [x] **M-2** VM 型別預寫（Instruction.h）
- [x] **M-3** 世界模擬 CPU 版（Chunk3D / TileWorld3D / MapGenerator3D）
- [x] **M-4** 角色 / 敵人 / VM 執行層（SpellCompiler / SpellRunner / ExecutionLoop）
- [x] **M-5** 技能施放整合
  - [x] ElementalReactionTable（ESkillElementType / FElementalStatusEffect / 22 條反應 / UElementalAuraComponent）
  - [x] CA 元素反應：CheckElementalCaReactions + ApplyElementalImpact（MaterialRegistry NativeElement / TileWorld3D）
  - [x] Contact 系統：`ExecuteContactHit()`（3D 前向掃描 + ApplyImmediate + SpawnEffect）
  - [x] ActionBus：`FActionBus`（DamageShield/DeathGuard，DeltaTime 計時，pause-aware）
  - [x] SpellProjectile 元素命中（OnHitEnemy + ApplyImmediate + ApplyElementalImpact）；VM SpellRunner ↔ TileWorld3D/EnemyManager 全 delegate 接通
- [x] **M-6** 渲染（RMC + Mega-Chunk + Greedy Meshing）
- [x] **M-7** 存讀檔（FArchive binary chunk + JSON spell/character + ChunkStreamingManager）
- [x] **M-8** 基礎 HUD（UMG：HP/MP 條 / 熱鍵欄）
- [x] **M-9** 積木編輯器（Slate SGraphEditor）
- [ ] **M-10** GPU CA（Compute Shader + RDG，長期目標）
- [x] **雜務：補齊 CLAUDE.md 缺漏規則**（已完成 2026-06-16）

---

## 剩餘完全缺失清單（2026-06-16 全面核實更新）

> 所有項目均已逐一 grep 驗證原始碼。標記 ✅ = 原本誤判為缺失，現已確認完成。

### 🔴 高優先
- [x] **UI-1** GameFlowUI 世界管理：`UGameFlowWidget.h/.cpp` ✅ 含 BuildLayout + OnWorldListRefreshed_Implementation；CreateWidget\<UGameFlowWidget\> 可直接用，不需 WBP
- [x] **UI-2** InputBindings 執行時重綁：`UInputSettingsWidget.h/.cpp` ✅ 含 BuildLayout + OnBindingsChanged_Implementation；不需 WBP
- [x] **UI-3** SpellListUI：`USpellListWidget.h/.cpp` ✅ 含 BuildLayout + OnSpellListRefreshed_Implementation；不需 WBP
- [x] **UI-4** ScriptCanvas：`SkillCreatorUI` Plugin 已有 `SBlockEditorWidget` / `UBlockEdGraph` / `UBlockEdGraphNode` / `UBlockEdGraphSchema`（M-9）；細節拖拉互動為 M-9 後期優化
- [x] **UI-5** SpellDescriptionGenerator：`SpellDescriptionGenerator.h/.cpp`（2026-06-16）

### 🟡 中優先
- [x] **S-1** CharacterSaveData 補完：`CharacterSaveData.h` 新增 Level / Xp / InventorySlots / ActiveHotbar / ManaCurrents（2026-06-16）
- [x] **S-2** FlowSaveSystem：`FlowSaveSystem.h/.cpp` 已完整（驗證 2026-06-16）
- [x] **S-3** Snapshot 系統：`ISnapshottable.h` / `USnapshotManager.h/.cpp` / `SnapshotTypes.h` 全部完整（驗證 2026-06-16）
- [x] **C-1** CharacterState 生存系統：`UCharacterStateComponent.h` 已有 BodyTemperature / Thirst / Hunger / Oxygen（驗證 2026-06-16）
- [x] **C-2** CharacterState Setter：SetStamina / SetMentalEnergy / SetMood / SetHealthStatus + SocialFlag CRUD 均在 `UCharacterStateComponent.h`（驗證 2026-06-16）
- [x] **C-3** CharacterStats 元素屬性：ElemAffinity / ElemOutputMult / ElemResistance TMap 在 `CharacterStats.h`（驗證 2026-06-16）

### 🟢 低優先（技能系統深度）— 全部前 session 已完成
- [x] **A-1** SpellGroup / SpellLoadout：`SpellGroup.h` / `SpellLoadout.h` 完整（驗證 2026-06-16）
- [x] **A-2** EngraveData：`FEngraveData` 在 `SpellArray.h` 含 Color / Category / Trigger / Scaling / Effect / Points（驗證 2026-06-16）
- [x] **A-3** Enums：EEngraveColor / EEngraveCategory / EEngraveTrigger / EScalingType 均在 `SpellArray.h`（驗證 2026-06-16）
- [x] **A-4** ManaSlot：`FManaSlot` 在 `ManaSlot.h` 含 DefaultMax / DefaultRegenRate / Tick / Consume（驗證 2026-06-16）
- [x] **A-5** SpellSaveSystem：`SpellSaveSystem.h/.cpp` 完整（驗證 2026-06-16）
- [x] **A-6** SafetyGuard：`SafetyGuard.h` 含 MaxExecutionsPerTick=50 / MaxWhileIterations=500 / MaxEntityCount=100 / MaxContainerDepth=3（驗證 2026-06-16）

### ⏳ 後續待辦
- [ ] **M-10** GPU CA（Compute Shader + RDG，長期目標）
- [ ] **UI-4 評估**：SBlockEditorWidget（SGraphEditor）拖拉/磁吸/縮放平移確認在實際 UE5 Editor 執行無誤，並評估使用者體驗是否符合預期（架構上已由 SGraphEditor 內建提供）
- [ ] **積木編輯器 Shipping build 支援**：`ToggleBlockEditorOverlay`/`OnOpenEditor` 目前整段 `#if WITH_EDITOR`（依賴 GraphEditor module，UnrealEd-only），封裝後玩家無法編輯技能；需評估改用 runtime UMG（非 SGraphEditor）重做積木編輯器 UI，或接受「技能編輯僅供開發測試」的產品決策
- [ ] （可選）如需客製化美術外觀：建立 Blueprint 子類覆寫 `OnWorldListRefreshed` / `OnEnterGame` / `OnBindingsChanged` / `OnSpellListRefreshed`；C++ 預設實作已可正常運作
- [ ] `OnEnterGame_Implementation` 加入 `UGameplayStatics::OpenLevel()` 呼叫（需確認地圖名稱後修改）

---

# ▲▲▲ 以下為 Godot 階段歷史紀錄（封存，僅供邏輯對照） ▲▲▲

> Godot 原始碼位置：`C:\Users\譚揚勳\skill-creator\`（branch: main）

**完整歷史存檔**：[docs/history/phase1-2.md](docs/history/phase1-2.md) · [docs/history/phase3.md](docs/history/phase3.md)

---

## 目前狀態

Phase 1 ✅ · Phase 2 ✅ · 角色整合 ✅ · 全內容填入 ✅ · VM 接通 ✅
積木編輯器 ✅ · 敵人 AI ✅ · 技能槽/容器 ✅ · Contact/連段/存檔 ✅
敵人重生 ✅ · 命中位置確認 ✅ · 扁平位元組碼 VM ✅
多樣化敵人 AI ✅ · Scratch 積木編輯器 ✅ · SpellRunner ✅ · 程序生成地圖 ✅
採掘建築系統（A–F）✅ · 更大的世界 + Camera2D ✅ · 鏡頭縮放 ✅ · 熱鍵欄 UI 改版 ✅
Wait 路徑修正 ✅ · GameClock ✅ · §9-B 戰鬥狀態 ✅ · §7 被動觸發積木 ✅ · Category A 積木補完 ✅
角色端（XP/等級 + 裝備槽 + HUD）✅
世界觀導入 W-1（境界等級）✅ · W-2（屬性刻印 + 法則門檻）✅ · W-3（元素碰撞系統）✅ · W-4（礦石礦脈）✅
W-5a（完整數值系統 CharacterStats）✅ · W-5b（角色狀態 CharacterState）✅
Phase 4 行動攔截鉤子（第三層）✅ · Phase 4 狀態快照系統（第四層，S-1~S-14）✅
投射物跟隨游標 ✅ · 投射物精確直線飛行 ✅ · 熱鍵欄 Tooltip 修復 ✅ · 固定傷害積木修正 ✅ · 浮動傷害數字 ✅
熱鍵欄 10 格（數字鍵 1~9/0）✅
生存數值 HUD（體力/精力/心情/口渴/飢餓/氧氣/體溫條，左側）✅ · 角色數值面板（C 鍵開關）✅
缺失積木 UI 接入（SequentialGate/AlternateTrigger/Discard/GetComboCount/EndOfChain/DetectProjectile/DetectAttack/DetectStatusChange/SetActivation*/EffectLabel/OnEffectStart/OnEffectEnd）✅
物品欄面板（I）✅ · 裝備欄面板（P）✅ · InputBindings 鍵位系統 ✅ · Tooltip 修復 ✅

---

## 最新完成（本輪）

| 功能 | 關鍵檔案 | 摘要 |
|------|---------|------|
| 敵人視覺 Y 修正 | `Scripts/Main.cs` | `SyncEnemyMeshes` 公式 `Position.Y * T + mh/2` → `(Position.Y+1) * T - mh/2`；敵人 `Position.Y` 為腳底格，視覺改對齊地表上方，不再嵌入地底 |
| SpellArray 錯誤預設移除 | `Scripts/AbilitySystem/Data/SpellArray.cs` | `ActivationType` 改預設 `Instant`（原 `Declare`）；`BaseMpCost` 改預設 `0f`（原 `10f`）；右側面板加「基礎消耗」SpinBox；`RefreshCost` 以 `SetValueNoSignal` 同步 SpinBox |
| preflight WARN 7/8 修正 | `SaveSystem.cs` / `AbilityEditorUI.cs` | WARN 7：`FromDto` 加 `ManaTypeRegistry.Get()` 驗證，未知 key 清除並 PushWarning；WARN 8：`RefreshMpBreakdown` 加 `HasUnboundMpBlocks` 橘色提示；PASS 32 / WARN 6 / FAIL 0 |
| W-6E Canvas ManaTypeKey OptionButton | `ScratchCanvas.cs` / `AbilityEditorUI.cs` | Totem 積木卡右側加「MP 類型」OptionButton（18 種 + ─）；鍵值存 `BlockNode.Params["manaTypeKey"]`；`SyncSlotsFromBlocks` 改從 params 讀取，首次載入時自動從 `oldSlots` 遷移；`RefreshAll` 前置 `SyncSlotsFromBlocks` 確保 Canvas 建立前 params 已補全；build 0 errors 0 warnings |
| W-6C/E 正確實作 | `AbilityPointCalculator.cs` / `SpellCaster.cs` / `AbilityEditorUI.cs` | 加 `CalculateSlotCostByType()`（W-6C）；右側面板 MP 區改為逐類型 read-only VBoxContainer（W-6E）；移除錯置的 `_manaKeyRows` OptionButton；`TryConsumeMp` 明確走 `ActiveManaSlots[0]` |

---

## Phase 3 ✅ VM 功能完成 / 世界端進行中

### VM Phase 2（全部完成）
- ~~Compare + RepeatWhile~~ ✅
- ~~ForEachNearby + QueryNearest~~ ✅
- ~~ListCreate / Append / Pop / Get~~ ✅
- ~~Broadcast / OnReceive~~ ✅

### VM Phase 3（全部完成）
- ~~Group 1：Die / Evaluate / Sleep~~ ✅
- ~~Group 2：LoopcastIndex / SuccessCount~~ ✅
- ~~Group 3：List 補完（Dequeue/Set/Length/Contains/RemoveAt/Clear）~~ ✅
- ~~Group 4：Vector 11 個積木~~ ✅
- ~~Group 5：Raycast~~ ✅
- ~~Group 5：FocalPoint~~  ✅

### 世界端（進行中）
- ~~程序生成地圖（Heightmap + CA）~~ ✅
- ~~更大的世界（600×200 格 + Camera2D 跟隨 + Ctrl+滾輪縮放 1–10x）~~ ✅
- ~~採掘建築系統（A–F 全部完成）~~ ✅（設計文件：[docs/history/採掘建築系統設計.md](docs/history/採掘建築系統設計.md)）
- ~~遊戲內時間系統（GameClock）~~ ✅
- ~~§9-B 戰鬥狀態系統（CombatState）~~ ✅
- ~~§7 全局戰鬥統計積木（GetBattleStat）~~ ✅
- ~~§7 被動反應觸發積木（DetectHp/Mp/Hit + WaitingCondition）~~ ✅
- ~~積木完整性補完（Category A 全清：SetVarBool/QueryNear/RandomChoice/TaskCounter/RisingEdge/FallingEdge/SinglePulse/DetectEntityEnter）~~ ✅
- ~~角色端（XP/等級 + AP 上限 + 裝備槽 + 屬性加成 + HUD）~~ ✅
- NPC 系統 ⬜（依賴世界觀設計，延後）
- 合成系統 ⬜（依賴世界觀設計，延後）

#### 放置物件系統（PlacedObjectRegistry）
- [x] **R-6a 核心**：`PlacedUnit`（Id / Mat / HashSet\<GridPos\> Tiles）+ `PlacedObjectRegistry`（tileToUnit / units）；放置時 Register、`OnTileDestroyed` 時 NotifyDestroyed（Unit 空則清除）
- [x] **R-6b 採掘分流**：挖中已登記格 → 設定「完美移除」開啟則整組移除 + 返還 1 物品；關閉則走現有形狀採掘路線；設定面板加 toggle
- [x] **R-6c 殘餘格處理**：Damage = 1 - Tiles/Original；Damage ≥ 0.5 → 立即解體（剩餘 tiles 變世界原生，Unit 刪除）；Damage < 0.5 → Unit 保有身份，可完美移除（返還 1 物品）或拉伸；完美移除模式不因解體邊界案例 fall-through 到形狀採掘
- [x] **R-6d 跨 Session 持久化**：世界資料夾新增 `placed-registry.json`（`[{id,mat,tiles:[{x,y,z}]}]`）；世界載入時讀入、存檔時寫出；**與 R-6a~c 同批實作**，不可延後（否則重開後放置物件身份全滅，R-6c 殘餘格機制失效）
- [ ] **R-6e 拉伸系統**（未來）：以 PlacedUnit 為操作目標，執行類 Blender 體素操作
  - 點擊已放置 tile → 進入「選取模式」，顯示包圍盒（AABB）與操作控制點
  - **拉伸**：沿 X/Y/Z 軸延伸 Unit.Tiles，新增 tiles 登記進同一 Unit
  - **縮減**：沿軸裁切，被裁切 tiles 從 Unit 移除（不返還物品）
  - **鏡像**：對 Unit 的 AABB 中心軸做 XZ 鏡像，新增 tiles 登記進同一 Unit
  - 先決條件：R-6a~d 全部完成 ✅，細節待另開 `docs/plan-placed-objects-r6e.md`

#### 3D 切換
- [x] **X-ray 模式**：Tab 鍵切換；`TileWorldRenderer3D.XrayZRadius` 控制 Z 可見半徑；LOD pass 雙向
- [x] **CA 特效 3D 化**：`SpellCaster` 所有 2D shim 改為 3D API，技能爆炸/元素/射線全部作用於玩家真實 Z 層
- [x] **投射物 3D 飛行**：`SpellProjectile` 加 `_dirZ`/`_posZ`，方向從滑鼠游標 3D 座標取得
- [x] **敵人物理 3D 化**：`TryMoveXZ` 取代 `TryMoveX`；`ApplyGravity` / 巡邏路徑全部補 Z；`EnemyProjectile` 弓箭修正 Z 座標
- [x] **敵人 3D AI**：3D 曼哈頓距離；`dz` 計算與 `FacingZ` 更新；所有 UpdateX 方法傳入 `dz` 並呼叫 `TryMoveXZ`

#### 基礎設施
- [x] **自動存檔機制**：退出視窗存檔（`_Notification` WMCloseRequest）+ 每 30 秒存檔 + 玩家死亡立即存檔；三路共用 `SaveAll()`（SaveAllLoadedChunks + placed registry + FlowSaveSystem）
- [x] **存檔效能優化**：`Chunk3D.NeedsSave`（`MarkDirty` 設、`SaveChunk` 清）；定期/死亡存檔改呼叫 `SaveDirtyChunks()`，只寫本輪異動 chunks；退出鉤子保留全存（`fullChunkSave:true`）
- [x] **玩家資料持久化**：`CharacterSaveData` 新增 Mp/InventorySlots/ActiveHotbar；存檔時同步 Hp/Mp/Level/Xp/Inventory/Position；載入時非首次進入自動還原

### Phase 4（進行中）
- ~~行動攔截鉤子（第三層：ActionBus + GameAction + DamageShield / DeathGuard）~~ ✅
- ~~狀態快照系統（第四層）~~ ✅（S-1~S-14 全部完成）

  **實作決策（已實作）**
  - Tile 世界納入快照（圓形小範圍，radius 由積木填入，S-13 TileWorldSnapshot）
  - 實體狀態快照 + Rollback + SpellRunner PruneAfter 退還 MP（S-1~S-12）
  - 棧式多層 Anchor（D1），死亡敵人完整復活（D2，ForceRevive），radius 積木參數（D3）
  - Rollback 後法陣繼續執行（D4），CharacterStats 納入快照（D5）
  - 延後：冷卻退還（需 SpellLoadout 記錄每個技能最後施放時間）、連段樹追蹤

  **S-1~S-14 全部完成**（詳見「最新完成」表格中 S-1~S-4b / S-5~S-7 / S-8~S-9~S-13 / S-10~S-11~S-12 四條記錄）
- 實體定義層（第五層）⬜（等 NPC 系統 / W-6+）

### 世界觀導入（獨立計畫，詳見 [docs/plan-worldlore-integration.md](docs/plan-worldlore-integration.md)）
- ~~W-1：境界等級（星盟通用戰力等級 + AP 上限重寫 + HUD 境界名稱/突破通知）~~ ✅
- ~~W-2：屬性刻印補完（ElementType enum + EngraveData.Element + 法則門檻 + 刻印庫鎖定）~~ ✅
- ~~W-3：元素碰撞系統（W-3a 核心框架 + W-3b CA 材質效果 + W-3c 技能元素接入）~~ ✅
- ~~W-4：資源與礦物系統（礦脈生成 + 掉落物 + 鐵鎬）~~ ✅
- ~~W-5a：完整數值系統框架（CharacterStats + PlayerController 管線接入）~~ ✅
- ~~W-5b：角色狀態系統（CharacterState：Stamina/MentalEnergy/Mood/HealthCondition/SocialStatus）~~ ✅
- ~~W-6A–F：MP 多類型框架（ManaType/ManaSlot/SpellGroup/HUD/存檔）~~ ✅
- W-6 暫緩（獨立待辦，見 `docs/plan-w6-implementation.md` §五）：
  - [ ] **走火入魔 / ManaCompatibility.cs**：觸發時機（獲得 MP 時）、嚴重度分級、公式數值；依賴 W-10 種族親和係數
  - [ ] **儲能瓶**：物品系統範疇；W-6 已確保 ManaSlot.RegenRate 機制，道具互動留後期
  - [ ] **複合體系（W-13，53 種）**：ManaTypeRegistry 已預留接口（IsComposite flag，SortOrder 從 19 起）；W-6 勿硬限 18 種 ✅
  - [ ] **MP 上限 3 種彈性調整**：種族/特異體質可提升至 5 種；現用 `SpellArray.MaxManaTypes = 3` 常數，不 hardcode，直接改常數即可
  - [ ] **HasAnyMpBlocks 精確判斷**：`SpellSlot.HasAnyMpBlocks` 初期保守返回 true；後期依 BlockType 列舉細化（只有真正消耗 MP 的積木才算）
  - [ ] **冷卻退還（Phase 4 延後）**：Rollback 時退還已消耗的施法 CD；需 SpellLoadout 記錄每個技能最後施放時間
- W-7 以後：法則刻印 / 種族 / 角色創建 / 合成 / NPC … ⬜（詳見計畫文件）

### ⚠️ 待實作（Design B 後續）

**SpellCaster Action 刻印接通** ✅（`ExecuteSlot` + `DispatchAction` 已完成）

剩餘：各 `act_*` 對應的真實效果實作仍多為 TODO-STUB，下一步優先項：
- `act_area_fan/around/distant/beam`：真實形狀（目前以 Explode 佔位）
- `act_fire_projectile`：圖騰層投射物（目前以爆炸佔位）
- `act_morph_apply`：真實 buff 接入 CharacterStats
- `ReadMods` 擴充：讀取更多 Modifier 刻印（orange/red/yellow）

---

### 編輯器
- ~~Scratch 積木拖拉修復~~ ✅
- UI 重構 Stage 1（資料層）✅
- ~~UI 重構 Stage 2（技能創建空間圓球列表）~~ ✅
- ~~UI 重構 Stage 3-A：ScriptCanvas 自由畫布架構骨架~~  ✅
- ~~UI 重構 Stage 3-B：C-shape 容器展開 + Reporter 積木槽 + SnapPoint 視覺高亮 + 調色盤拖入畫布~~ ✅
- ~~UI 重構 Stage 4：容器效果編輯頁面（深度上限 3 + 容器導覽棧）~~ ✅
- ~~UI 重構 Stage 5：SpellDescriptionGenerator 效果文案自動生成~~ ✅

---

## 已知技術債

| 項目 | 位置 | 說明 |
|------|------|------|
| 境界 AP 上限數值 | `PlayerController.TierApCap` | ⚠️ 8 段（50/120/200/350/500/700/900/1500）暫定，待平衡；XP 升級速率（level×100）同待調整 |
| 裝備屬性數值 | `ItemRegistry.cs` | ⚠️ AtkMult 1.3×、DefFlat 5、MpBonus 30 為暫定原型值，待平衡 |
| 被動技能上限 20 | `docs/plan-ability-system.md §8` | 暫定 |
| 實體數量上限 100 | `SafetyGuard.cs` | 暫定 |
| 施放延遲 0.3f | `SpellArray.cs` | 待平衡測試 |
| ~~Wait 計時（Projectile/Contact 仍同步）~~ | ✅ 已修正 | Projectile/Contact 命中時現在走 SpellRunner 跨幀路徑，Wait 真實計時 |
| 敵人重生時間 8 秒 | `Enemy.cs RespawnTime` | ⚠️ 待平衡 |
| Contact 直接傷害 20 | `SpellCaster.ExecuteContactHit` | ⚠️ 待平衡 |
| CharacterStats 初始數值 | `CharacterStats.cs` | ⚠️ MaxHpBase(100) / MpRegenRate(8) / CritRate(5%) / CritDmgMult(1.5×) / Stamina消耗速率等全部待平衡 |
| CharacterState 消耗速率 | `CharacterState.cs` | ⚠️ StaminaDrainCombat(2/s) / MentalEnergyDrainCombat(1/s) / 各回復速率待平衡 |
| 生存四值速率 | `CharacterState.cs` | ⚠️ ThirstDrain(0.05/s) / HungerDrain(0.02/s) / OxygenDrain(10/s) / BodyTempAdaptRate(1.5°C/s) / 傷害數值全部待平衡 |
| Aura 溫度偏移數值 | `ElementalAuraComponent.cs` | ⚠️ FireAuraTempShift(+15) / IceAuraTempShift(-20) / WaterAuraTempShift(-8) 待平衡 |
| 元素 CA 反應機率 | `TileWorld.cs` | ⚠️ CaChanceSlow(0.5%) / CaChanceFast(3%) 待平衡 |

---

## 程式碼待辦（Code Review 發現）

### 🔴 Bug / 正確性（已全部修完 ✅）

**B-1｜`If` 積木的 `ElseBranch` 在 UI 完全不可用** ✅
- 位置：`ScratchCanvas.cs` `BuildBlockCard`（只渲染 `ThenBranch`，未渲染 `ElseBranch`）
- 問題：`SpellCompiler` 已正確編譯 `ElseBranch` 的跳轉邏輯，但使用者無法從 UI 加入積木到 else 分支，`If` 事實上變成沒有 else 的條件。
- 修法：在 `BuildBlockCard` 的 `If` 判斷區新增 `BuildBranch(block.ElseBranch, "ELSE", indent + 1)`。

**B-2｜`SpellCompiler.EmitBlock` 靜默丟棄未知 `BlockType`** ✅
- 位置：`SpellCompiler.cs` `EmitBlock`（switch 無 `default` 分支）
- 問題：新增 `BlockType` 若忘記加 compiler case，積木在執行時無聲消失，沒有任何錯誤或警告。
- 修法：加入 `default: GD.PushWarning($"[SpellCompiler] 未處理的 BlockType: {block.Type}");`

**B-3｜`EntityInfo.Hp` 在 `ForEachNearby` 迭代中陳腐** ✅
- 位置：`ExecutionLoop.cs` `ForEachStart` / `ForEachStep`（`EntityInfo` 為施放時快照）
- 問題：`SetEntityProp` 扣血後實際 `Enemy.Hp` 更新，但 `CurrentIterEntity.Value.Hp` 和 `_e.hp` 變數仍是舊值。同次迭代呼叫 `GetEntityProp "hp"` 會讀到錯誤數值，依此計算的後續 `SetEntityProp` 也會錯。
- 修法：`SpellCaster` / `SpellRunner` 消費 `PendingEntityDamageIdx` 後，同步更新 `ctx.CurrentIterEntity` 的 Hp 欄位（需重建 record）並重寫 `_e.hp`。

**B-4｜`GlobalVars` / `GlobalLists` 從不重置** ✅
- 位置：`ExecutionContext.cs`（兩個 `static Dictionary`）；`EventBus.cs`（`static HashSet _active`）
- 問題：這三個靜態集合在遊戲重啟或場景重載時不會清除。同一 process 跑第二局，全域變數帶著上局狀態。`EventBus` 有 `ClearFrame()` 但沒有 session-level reset。
- 修法：在 `Main._Ready()` 加入 `ExecutionContext.GlobalVars.Clear(); ExecutionContext.GlobalLists.Clear(); EventBus.ClearAll();`（需在 EventBus 新增 `ClearAll` 方法）。

**B-5｜`MaxComboDepth = 5` 重複定義** ✅
- 位置：`SpellCaster.cs:11` 與 `SpellRunner.cs:23` 各有一份
- 問題：若只改一處，兩條執行路徑的連段上限悄悄走岔。
- 修法：移至 `SafetyGuard.cs` 作 `public const int MaxComboDepth = 5;`，兩處改為引用。

---

### 🟡 設計 / 技術債（已全部修完 ✅）

**D-1｜`SpellCaster.ExecuteEffects` 與 `SpellRunner.Advance` 高度重複** ✅
- 位置：`SpellCaster.cs`（`ExecuteEffects` 方法）、`SpellRunner.cs`（`Advance` 方法）
- 問題：兩者都做「建 ctx → 驅動 loop.Step → 消費 PendingInvokeTotem / InvokeSpell / EntityDamage → 處理 ForEach 位置覆蓋」。每次新增 pending 機制（目前三種），兩個地方都要同步修改。
- 修法：提取 `static void AdvanceContext(ExecutionContext ctx, ExecutionLoop loop, ContextRuntime rt, float delta)` 共用方法，`rt` 封裝 player / world / enemies / loadout 等依賴。

**D-2｜`ForEachNearby` 的 `player.Position` 暫時覆蓋** ✅
- 位置：`SpellCaster.cs` 與 `SpellRunner.cs` 的 `PendingInvokeTotem` 處理段
- 問題：用 save/restore 手法改寫共享物件欄位。目前 `SpellRunner` 的多個 `ActiveSpell` 是同幀序列執行，不存在競態。但若未來加入真正並行（Task / async），此處是 race condition 的埋雷點。
- 修法：在 `ExecutionContext` 加入 `GridPos? EffectOriginOverride`，讓 `SpellCaster.ResolveTotem / ExecuteSlot` 讀取此欄位決定效果中心，不再直接修改 `player.Position`。

**D-3｜安全迴圈魔法數字 `300`** ✅
- 位置：`SpellCaster.cs:115` (`while (!ctx.IsFinished && safety++ < 300)`) 與 `SpellRunner.cs:98`（相同）
- 問題：`300` 散落兩處，語義不明。
- 修法：加入 `SafetyGuard.MaxStepsPerCast = 300`，兩處改為引用。

**D-4｜`QueryEnemies` 重複計算 `DistanceTo`** ✅
- 位置：`SpellCaster.cs` `QueryEnemies`（`Where` 與 `OrderBy` 各算一次，含 sqrt）
- 問題：每個敵人被計算兩次距離。對象少時無感，若敵人數量增加會浪費。
- 修法：改為 `.Select((e, i) => (e, i, dist: e.Position.DistanceTo(player.Position))).Where(x => x.dist <= radius).OrderBy(x => x.dist).Select(x => new EntityInfo(x.i, x.e.Position, x.e.Hp, x.e.MaxHp))`。

**D-5｜`BroadcastAndWait` 行為與 `Broadcast` 完全相同** ✅
- 位置：`SpellCompiler.cs`（兩個 BlockType 編譯成同一個 `OpCode.Broadcast`）
- 問題：UI 和 BlockType enum 暗示有不同語意（廣播後等待回應），但實際不是。可能讓使用者困惑。
- 修法：短期在 `ScratchCanvas.BlockName` 加標注「（等同廣播）」；長期實作真正的 request/reply 語意（廣播後在 EventBus 等待一個 reply 訊號）。

**D-6｜新增積木需同步修改 `ScratchCanvas` 四個 switch** ✅
- 位置：`ScratchCanvas.cs` 的 `AddParams`、`BlockColor`、`BlockName`、`MakeDefaultBlock`
- 問題：每加一個 `BlockType`，需在四個獨立 switch 分別新增，容易漏改，且散落於 ~700 行範圍內。
- 修法：定義 `BlockDescriptor record`（color, name, defaultParams, buildParamsUI delegate），改用靜態字典 `Dictionary<BlockType, BlockDescriptor>` 集中管理，四個 switch 統一查表。

---

### 🟢 低優先（已全部修完 ✅）

**L-1｜`WhileIterationsTotal` 跨巢狀循環累計** ✅
- 位置：`ExecutionContext.cs`（單一計數器）、`ExecutionLoop.cs` `WhileCheck`
- 問題：`RepeatN(20)` 包 `RepeatWhile` 時，500 次上限跨所有外層迭代累計，而非單次 while 迴圈獨立計算。可能使正常設計的技能提前 Fizzle。
- 修法：改用 `Stack<int> WhileIterCounters`，`WhileCheck` push/pop 並分別計數；或將上限改為更寬鬆的值後標注文件。

**L-2｜`EntityInfo.Index` 依賴 `EnemyManager.Enemies` 列表穩定性** ✅
- 位置：`SpellCaster.cs` `QueryEnemies`（Index = 列表位置）、`SpellCaster.cs` / `SpellRunner.cs`（`enemies.Enemies[idx].TakeDamage`）
- 問題：Index 在查詢時捕捉。目前重生是原地覆蓋，安全。若未來動態插入 / 移除敵人，Index 失效指向錯誤目標。
- 修法：為 `Enemy` 加入穩定的 `int Id`（遞增），`EntityInfo` 改存 `Id`，查傷害時用 `enemies.Enemies.First(e => e.Id == id)` 查找。

**L-3｜`SetEntityProp` 只支援 "hp" 扣血** ✅
- 位置：`ExecutionLoop.cs` `StoreEntityProp`（僅處理 `prop == "hp"`）、`ScratchCanvas.cs` AddParams（下拉只有 "HP"）
- 問題：`GetEntityProp` 可讀 x/y/hp/maxhp，但 `SetEntityProp` 只能扣血，無法修改位置或其他屬性。
- 修法：`StoreEntityProp` 新增 `"x"` / `"y"` case，寫入 `PendingEntityMove`（類似 `PendingEntityDamage`），SpellCaster/Runner 消費後呼叫 `ApplyForce` 或直接設定位置。

**L-4｜`AbilityEditorUI.BuildBlockRow_UNUSED` 仍留在檔案內** ✅
- 位置：`AbilityEditorUI.cs`（已改名為 `_UNUSED`，約 270 行）
- 問題：已被 ScratchCanvas 取代，是死碼，增加閱讀噪音。
- 修法：整段刪除（從方法宣告到 `return container;\n    }` 的閉括號）。

---

## 已知問題分析（2026-06-09 截圖回饋）

### 問題 2｜法陣編輯器 UI 現狀分析

**現狀是否預期？** 是，屬於過渡狀態，但有兩個根本限制尚未處理：

**限制 A：積木無法拖動（Godot ScrollContainer 衝突）** ✅ **已修復（2026-06-09）**
- 根因：ScratchCanvas 的積木拖拉把手（色條）使用 `GuiInput` + `ForceDrag` 實作。但 ScratchCanvas 被包在 `ScrollContainer` 內，Godot 的 `ScrollContainer` 會優先攔截 `MouseButton` 事件，導致 `GuiInput` 裡的 `ForceDrag` 幾乎無法觸發。
- **修復方式**：改用 Godot 4 原生虛擬方法 `_GetDragData` / `_CanDropData` / `_DropData`（在 Viewport 層直接處理，不經過 ScrollContainer 事件鏈）。`DragHandle : Panel`（partial sealed）覆寫 `_GetDragData`，`DropZone : Control`（partial sealed）覆寫 `_CanDropData` / `_DropData`，刪除原有 `GuiInput` 與 `ForceDrag` 呼叫。commit: `0c20fcb`

**限制 B：與 Scratch 模式相去甚遠**
- 根因：目前的 `AbilityEditorUI` 是「舊版插槽面板 + 嵌入式 ScratchCanvas」的混合體。ScratchCanvas 被擠在畫面下半部，左側仍是圖騰庫（插槽系統），積木類型 palette 按鈕埋在 AbilityEditorUI 的某個區域，不突出。
- 真正的 Scratch 模式需要的架構：左側改為積木類型 palette → 右側為純 canvas 工作區 → 移除插槽面板主結構。
- 這是**架構層面的改動**，非現有 Phase 3 範圍，應列為下一輪的獨立任務。

**✅ 已完成（UI 重構 Stage 1–5）**
- 全 Scratch 模式：左側積木 palette（技能因子/積木/刻印 tabs）+ 中央純 ScriptCanvas + 右側能力點統計面板；插槽面板已移除

---

---

### ⚠️ 視覺尺度・世界擴張・採掘 Raycast（[docs/plan-scale-world.md](docs/plan-scale-world.md)）

> TileSize 座標系已正確（1/32）。視覺無改善原因：玩家仍是 1×1×1 tile。需依序完成 P → E → W → R。

**設計核心**：每個 tile = 一個物理粒子。目標 tile ≈ 1~2 px/螢幕，玩家 BodyH = Grain×2 tiles。
全部數值由 `WorldScale.Grain` 一個常數控制，未來升級只改這裡。

**Phase A（Grain=16，近期）** ✅
- [x] **P-0** `WorldScale.cs`（新）：Grain=16，派生 TileSize/BodyH/WorldH/OrthoSize 等
- [x] **E-1** `CaGpuSimulator.cs`：Upload byte[] 預配置，消除每幀 GC ⚠️ 必須最先
- [x] **P-1** `PlayerController.cs`：BodyH=32，TryMove/Physics/IsOnGround 掃整列
- [x] **P-3** `MapGenerator3D.cs`：洞穴 ≥ Grain+4 tiles 高，出生點上移
- [x] **P-2** `Main.cs`：mesh/相機從 WorldScale 派生
- [x] **W-1** `Main.cs`：世界尺寸改為 WorldScale.WorldW/H/D（3200×1600×3200）
- [x] **W-2** `MapGenerator3D.cs`：simplex noise + 大洞穴
- [x] **E-2** `CaGpuSimulator.cs`：Upload 跳過全 Air chunk
- [x] **E-3** `TileWorld3D.cs`：分幀 CA 模擬（每幀 1/4 zone）
- [x] **R-1/2/3**：3D Raycast 採掘 + face normal 放置

**V 系列 — 次像素視覺層（R 系列後）**
> 架構細節見 [`docs/plan-scale-world.md § V 系列`](docs/plan-scale-world.md)

- [ ] **V-1** `VfxManager.cs`（新）：GPUParticles3D 粒子池管理，`OnTileDestroyed`/`OnExplosion` 事件呼叫 `SpawnVfx(pos, type)`
  - 爆炸：碎片飛散 50~200 顆 / 0.8s
  - Sand/Powder 落地：微塵揚起 5~20 顆 / 0.3s
  - Lava 接 Water：蒸汽爆裂 30~80 顆 / 1.0s
  - 採掘命中：火花 + 碎屑 10~30 顆 / 0.5s
- [ ] **V-2** `TileWorldRenderer3D.cs`：Water tile 改 `ShaderMaterial`（`.gdshader` 內嵌 uv sin 偏移），液面漣漪，不新增粒子
- [ ] **V-3** 粒子沉積（最終目標）：`SubpixelParticle`（純 CPU struct：Pos/Vel/Payload/Mass/Life）+ `DepositMap`（稀疏 Dictionary）；超過閾值 → `SetTile`；同時存活上限 `MaxDepositParticles=2000`
  - 應用：灰燼粒子飄落 → 地面 Ash；水花落地 → WeakWater；岩漿冷卻 → Stone
  - **先決條件**：R 系列完成、Grain 穩定後再啟動

**Phase B（Grain=32，遠期）**
> 效能數據與先決條件細節見 [`docs/plan-scale-world.md § 效能展望`](docs/plan-scale-world.md)

**先決條件**：Sparse GPU CA（Phase B 的 CA 主動格數 ≈ 210M，是 Phase A 的 ×8，E-3 分幀無法解決）。

| Grain | CA 主動格數 | 相對 Phase A | 需求 |
|-------|------------|-------------|------|
| **16（Phase A）** | ≈26M | ×1 | E-3 分幀 ✅ |
| 32（Phase B） | ≈210M | ×8 | **Sparse GPU CA**（尚未實作）|
| 64（未來） | ≈1.68B | ×64 | 未來硬體 |

- [ ] **Sparse GPU CA**：只 dispatch 非空 chunk 的 GPU 任務；Phase B 的獨立先決條件
- [ ] **Phase B 升級**：改 `WorldScale.Grain = 32`，其餘自動跟進；改後驗證效能與視覺

**物件破碎（未來可加）**
> 詳見 [`docs/plan-scale-world.md § 物件破碎`](docs/plan-scale-world.md)

基礎已就緒（每個 tile 是獨立 CA 物理粒子）。不需任何物理引擎改動：
- [ ] `TileWorld3D.Explode(center, r)` 方法：邊緣 tile 改為 `Sand`（Powder）→ CA 重力自然散落模擬破碎

**採掘放置系統：未來方向（預留架構點，不急著做）**
> 詳見 [`docs/plan-mining-placement.md § 🔴 第三層`](docs/plan-mining-placement.md)

| 項目 | 說明 | 預留方式 |
|------|------|---------|
| 坑洞不規則像素邊緣 | 需 V 系列 sub-tile 噪音貼圖 / GPUParticles3D 飛散 | V-1 VfxManager 預留介面 |
| 重塑動作（Blender 式 Push/Pull） | 體素雕刻，需 sub-tile 變形或 SDF 近似 | `PlacementShape` enum 可加 `Sculpt` 值 |
| 100 碎片 → 1 材質（合成） | 合成系統範疇，需合成 UI + 配方 registry | Fragment MaxStack 已設 99999；待合成系統 |
| 應力/崩塌模擬 | 結構完整性：懸空大區塊 tile-group FloodFill 判斷 | `DestroyReason.Collapse` 已預留 |

**⚠️ P/E/W/R 系列全部完成後必做：驗證 Preflight Check 7**
> 詳見 [`docs/plan-scale-world.md § 計畫完成後必做：驗證 Check 7`](docs/plan-scale-world.md)

- [ ] 跑 `preflight-check.ps1` Check 7：確認所有 `MeshInstance3D`/`Node3D` 的 `Vector3` 均已乘 `TileSize`；UI/Control 的 `Vector2` 可豁免
- [ ] 審查 `$tsSafe` 豁免清單是否需要更新（新增 UI 識別字或刪除已消失的豁免）
- [ ] 若 WW 輸出大幅增加，檢查 P-2 / R-2 / 新程式碼是否帶入裸 float

### Phase 3 渲染效能待辦（世界端）

- ~~**1. 多執行緒 Chunk Mesh 重建**（中期）~~ ✅（`TileWorldRenderer3D.cs` Task.Run worker）
- ~~**2. GPU Compute Shader CA**~~ ✅（`CaGpuSimulator.cs` — Margolus Block CA，active zone 64×200×64，Powder+Liquid GPU，Gas+Static CPU）
- ~~**3. X-ray 視野（Tab 切換）**~~ ✅（`ToggleXray` = Tab；`XrayZRadius` property + LOD pass 已接通；技能觸發版本另計）
- ~~**4. Chunk render 半徑對齊**~~ ✅（`MeshRadiusChunks=13` = 208 tile，已遠大於最大縮放 80 tile）
- ~~**6. 敵人 AI 感知距離早退**~~ ✅（`Enemy.Update` 開頭水平距離 > DespawnSoftDist=192 tile 時跳過 AI）
- **5. AddSurfaceWater BFS 分塊（超大世界）** ⬜（低優先，不急）
  - 現況：`MapGenerator3D.AddSurfaceWater` 的 Grain-scale BFS 在生成時跑一次，結果存 `_waterGrainSet`，無執行期開銷
  - 問題：世界極大時（例如 4096×256）BFS 集合可能佔用大量記憶體，且單次生成時間偏長
  - 修法方向：依 chunk 區域分塊跑 BFS，懶加載時才計算該 chunk 的水域
- **6. 敵人 AI 感知距離早退** ⬜（低優先，不急）
  - 現況：`EnemyManager.Update` 每幀對所有敵人（含遠在 500+ tile 外）跑完整 AI 邏輯
  - 問題：當地圖很大且存在多組遠端敵人時，每幀無謂的 AI 計算量線性成長
  - 修法：在 `Enemy.Update` 最前端加水平距離早退（建議閾值 `MobSpawnController.DespawnSoftDist`），超出則跳過本幀 AI 更新

---

### 問題 3｜地圖呈現（已解決）

地圖已改為 **Heightmap + CA 混合算法**（見最新完成表格），有機洞穴地形，非迷宮。

---

## 臨時待辦（2026-06-10 用戶需求，依序實作）

### ~~T-1｜積木顯示名稱白話化~~ ✅

- **範圍**：`ScratchCanvas.cs` 的 `BlockDescriptor` registry（目前 82 個積木）
- **目標**：審查每個 `Name` 欄位，凡是英文、過度技術性、縮寫、或語意不直觀的，改成白話中文
- **注意**：只改顯示名稱，不動 `BlockType` enum 值；也不改 ScratchCanvas 以外的顯示邏輯

---

### ~~T-2｜筆刷歸入開發者工具（保留 F1）+ 開發者工具列表按鈕~~ ✅

**2a — F1 開發者筆刷面板（鍵位不變）**
- `InputBindings.TogglePaint` 保持 F1，鍵位不動
- 筆刷面板（`_paintToolPanel`）：將原工具列中的「材質選擇」按鈕群 + 筆刷大小控制從右側工具列抽離，改為一個獨立面板；F1 切換面板顯示，並同步設定 `_world.PaintingEnabled`
- `_paintModeLabel`（頂部「畫筆模式」提示標籤）保持原行為，F1 開啟時顯示

**2b — 開發者工具列表按鈕（右上角）**
- 右上角工具列新增「🛠 開發者工具」按鈕（放在現有按鈕群最下方，`HSeparator` 之後）
- 點擊後顯示/隱藏一個清單 Panel，列出所有偵錯快速鍵說明：

  | 鍵位 | 功能 |
  |------|------|
  | F1   | 開發者筆刷面板（材質繪製） |
  | F2   | 座標驗證 overlay（格/畫布/螢幕座標 + 玩家方向） |
  | F3   | VM 執行追蹤（每步印 OpCode） |
  | F4   | 生存速率 overlay（8 項數值 + /s 速率） |
  | F5   | 快照取樣（半徑 5 圓形） |
  | F6   | 快照回滾並比對 |

- 後續新增偵錯工具時，同步維護此清單

---

### ~~T-3｜熱鍵欄右方開啟物品欄按鈕 + 改 Z 鍵~~ ✅

- `InputBindings`：`OpenInventory` 預設鍵 `Key.I` → `Key.Z`
- `Main.cs BuildHotbar`：熱鍵欄最右側新增一個小按鈕（顯示「背包」或「▣」），點擊呼叫 `ToggleInventoryPanel()`
- 注意：I 鍵空出來，供 T-5 使用

---

### ~~T-4｜裝備欄預設顯示在畫面右側 + 改 X 鍵~~ ✅

- `InputBindings`：`OpenEquipment` 預設鍵 `Key.P` → `Key.X`
- `Main.cs BuildEquipPanel`：Panel 定位改為**右側**（AnchorLeft = AnchorRight = 1f，Position.X 設負值使其靠右顯示）
- `Main.cs _Ready` 或 `BuildHUD` 之後：`_equipPanelOpen = true; _equipPanel.Visible = true;`（預設開啟）
- 注意：P 鍵空出來，供 T-5 使用

---

### ~~T-5｜10 個技能欄位 + U/I/O/P 組合鍵施放（架構最複雜）~~ ✅

**5a — 擴充技能欄位數**
- `SpellLoadout.MaxSlots`：5 → 10
- `AbilityEditorUI`：技能槽 UI 從 5 格擴充為 10 格
- `Main.cs _slotLabels`：對應擴充，顯示 10 個欄位標籤

**5b — 移除舊施放機制**
- 移除 `InputBindings.CastSpell`（Space 鍵施放）及相關 `_Input` 邏輯
- 移除 `SpellSlot 1–5`（1–5 數字鍵切換 Loadout.ActiveIndex）及 `_Input` 中對應迴圈
- `InputBindings`：刪除 `CastSpell` / `SpellSlot1–5` 常數與 `_defaults` 條目

**5c — InputBindings 擴充支援複數鍵組合**
- `_defaults` 改型：`Dictionary<string, Key[]>`（單鍵以單元素陣列儲存，組合鍵以多元素陣列）
- `ApplyToInputMap`：對多鍵組合，Godot InputMap 無法直接表達，**不注入 InputMap**；改以自訂邏輯在 `_Process` 內偵測（見下）
- `Rebind` / `SaveToFile` / `LoadFromFile`：格式更新為 `string[]`（JSON 陣列）

**5d — 組合鍵施放偵測邏輯（在 `_Process` 中實作）**

10 個施放鍵位對應（優先順序從多到少，避免子集誤觸發）：

| 欄位 | 組合 |
|------|------|
| 1    | U |
| 2    | I |
| 3    | O |
| 4    | P |
| 5    | U + I |
| 6    | I + O |
| 7    | O + P |
| 8    | U + I + O |
| 9    | I + O + P |
| 10   | U + I + O + P |

- 偵測方式：每幀計算目前 U/I/O/P 按下狀態（`Input.IsKeyPressed`），比對上幀狀態，**上幀未全按、本幀全按 = 觸發一次**（rising edge，避免持按重複施放）
- 匹配從「最多鍵」往「最少鍵」優先：本幀偵測到 U+I+O 全按時，只施放欄位 8，不施放 1/2/3/5/6

**5e — 鍵位說明同步更新**
- T-2b 的開發者工具列表 Panel 中，不需列出施放組合（那是玩家功能，非偵錯工具）
- `AbilityEditorUI` 或 HUD 的技能槽標籤旁，顯示對應組合鍵提示（例如「U+I」）

---

### R-4-RENDER｜形狀接合視覺問題

> 來源：`docs/plan-mining-placement.md § R-4-RENDER`

**問題描述**：玩家先放一顆石頭球體，再放一個石頭圓錐貼在球體旁，兩者接觸邊是否有「縫隙感」？

**分析（三種案例）**：

| 案例 | 結果 | 原因 |
|------|------|------|
| 同材質、形狀連通（tile 相鄰） | ✅ 無縫 | Greedy Meshing 合併相鄰同材質面，渲染層完全無縫 |
| 同材質、形狀相切但有 1-tile 空隙 | ❌ 有空隙 | 空隙處是 Air tile，本來就該空著（設計問題，非 bug） |
| 不同材質相鄰 | 顯示材質邊界 | 符合預期（石頭 ≠ 泥土） |

**關鍵結論**：「形狀工具」只是一次放置多個 tile 的快捷鍵。一旦 tile 落地，世界不記得它是球還是錐。Greedy Meshing 處理的對象是 tile，不是形狀——**相同材質、tile 相鄰 → 自動無縫，不需要額外程式碼**。

**真正的接合問題（幾何層）**：

當玩家把一個球體（半徑 3）與圓錐底部（底面半徑 5）相接時：
- 球體的「頂點」截面只有約 1 tile 寬
- 圓錐底面卻有 5×5 tile 寬
- 結果：圓錐底面外圈的 tile 懸在球體外側，只有中心 1 tile 連接

這是**幾何設計問題**，不是渲染 bug。

**解法選項**：

| 方案 | 說明 | 狀態 |
|------|------|------|
| 玩家自主選型（預設） | 文件說明「圓弧形狀的接合面需半徑匹配」；圓柱-圓柱同半徑天然無縫 | ✅ 目前行為 |
| V 系列 Junction Fill | 放置形狀時，偵測面法線方向接觸面，自動補上接合 tile；實作為 `PlacementShape.cs` 中的 `FillJunction(world, from, to, mat)` 靜態方法 | ⬜ V 系列再加 |
| R-6 雕刻工具（Sculpt） | Push/Pull 工具讓玩家手動補坑，更靈活 | ⬜ R-6 規劃中 |

**待辦行動**：

- [ ] **V-4**（V 系列）：`PlacementShape.cs` 新增 `FillJunction(TileWorld3D world, GridPos from, GridPos to, MaterialType mat)` 靜態方法；放置時偵測接觸面法線，自動補 tile
- [ ] **R-6**（未來）：雕刻工具 Sculpt，Push/Pull 手動修形
- `PlacementShape.cs` 中已預留 `// TODO: V-4 FillJunction` 注釋（接合補填）

---

### R-5 延伸｜爆炸摧毀材質時接入 SpawnFragments

**背景**：R-5 已完成架構（`DestroyReason`、`SpawnFragments` helper、`DroppedItemManager.Spawn` 依 reason 分流）。
目前爆炸邏輯（技能積木 / `TileWorld3D.Explode`）仍用 `SetTile` 直接清格，不觸發 `OnTileDestroyed`，故不掉落碎片。

**接入規則**：任何會批次摧毀 tile 的邏輯（爆炸積木、AOE 傷害、崩塌等），都應：
1. 改用 `world.DestroyTile(pos, DestroyReason.Explosion)` 取代 `SetTile(pos, Air)`
2. 爆炸結束後，依材質分組統計 tileCount，對每種材質呼叫：
   ```csharp
   _droppedItems.SpawnFragments(center, mat, tileCount, DestroyReason.Explosion);
   // 換算公式：1 unit = 1000 tiles；Explosion = 20~80 碎片/unit
   ```
3. `DroppedItemManager.Spawn` 遇 `Explosion` reason 已 return early，不重複掉落 DefaultDrops

**待辦**：
- [ ] 爆炸技能積木（`act_explode` 等）實作時，改用 `DestroyTile(Explosion)` 並在批次結束呼叫 `SpawnFragments`
- [ ] 若未來加入 `TileWorld3D.Explode(center, r)` 方法（物件破碎用），在其中一併呼叫 `SpawnFragments`

---

## 設計對照待辦（積木 × 刻印一致性）

> 2026-06-10 交叉比對發現；需設計決策後再實作。

### 🔴 功能完全重疊（需決定保留哪層）

#### O-1｜`DeathGuard` 積木 ↔ `green_death_replace` 刻印（死亡替代）
- `DeathGuard` 積木：`ActionBus` 攔截 `PlayerDeathAction` → 存活 1 HP ✅ 已實作
- `green_death_replace` 刻印：「致命傷改為 HP 剩 1」TODO-STUB，備註「未接傷害攔截」
- 兩者效果字面相同，底層機制相同（ActionBus）
- **待決策**：A（保留兩層：刻印作宣告式捷徑，實作時直接呼叫 ActionBus）／B（移除刻印，功能已被積木覆蓋）

#### O-2｜`DamageShield` 積木（mode=cancel）↔ `green_invincible` 刻印（無敵）
- `DamageShield` 積木：`ActionBus` 攔截 `PlayerDamageAction` → 完全免傷 ✅ 已實作
- `green_invincible` 刻印：「短暫無敵幀，**未接傷害攔截**」TODO-STUB
- 差異：刻印預期為持續時間型（duration），積木預設 one-shot；底層機制相同
- **待決策**：同 O-1；若保留，實作刻印時在 ActionBus 掛持續型攔截器

### 🟡 基礎設施共用（機制重疊，設計層次不同，不衝突）

#### O-3｜`Anchor`/`Rollback` 積木 ↔ `law_time` 法則刻印（時間）
- Anchor/Rollback 已實作時間倒流語義，`SnapshotManager` 基礎設施已建
- `law_time` 未來實作必定需要同一套 SnapshotManager
- 積木 = 可程式化接口；法則刻印 = 宣告式高層包裝 → **不衝突，共用底層**

#### O-4｜`DamageShield` 積木 ↔ `green_reflect` 刻印（反彈）
- 兩者均掛 ActionBus，但效果不同（減傷 vs 傷害重導至攻擊者）
- `green_reflect` TODO-STUB 備註「未接 ActionBus」——ActionBus 已建
- 實作 `green_reflect` 時：`ActionBus.Register` 攔截 `PlayerDamageAction` → 對攻擊者 ApplyDamage

### 🟢 技能操控積木（設計規格存在，程式碼完全缺席）

#### O-5｜`red_ability_copy` 刻印（能力複製）＋ `red_ability_steal` 刻印（能力奪取）
- 設計規格：[docs/plan-ability-system.md §技能操控積木](docs/plan-ability-system.md)
  - **複製積木**：命中後取得目標最後宣告法陣的副本，持續最多 30 秒，原主人不受限，以施法者屬性結算
  - **掠奪積木**：強制取走使用權，原主人持續期間無法使用，持續最多 15 秒
  - 共同規則：即時型法陣不可被複製/掠奪；禁止鏈式複製；原主人死亡時掠奪即消失
- **現狀**：`BlockType` / `OpCode` / `SpellCompiler` / `ExecutionLoop` / `ScratchCanvas` 均無對應條目，連 enum 佔位都沒有
- **所需前置基礎設施**（目前完全缺席）：
  1. `EntityInfo` 加入法陣欄位，或敵人支援 `SpellLoadout`
  2. 玩家端臨時法陣注入機制（帶倒數計時的「臨時格」UI）
  3. 敵人法陣被封鎖狀態（掠奪用）
  4. 宣告窗口偵測（識別即時型並 Fizzle）
- **依賴**：NPC 系統 / W-6+ 敵人 Loadout 設計完成後才可進入實作




